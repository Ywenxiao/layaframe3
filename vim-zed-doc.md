# Zed 中 Vim 模式完整使用文档

> 基于 [Zed 官方 Vim 文档](https://zed.dev/docs/vim) 整理，并补充文档中未提及的 Vim 常用命令。

---

## 目录

1. [概述](#1-概述)
2. [启用 Vim 模式](#2-启用-vim-模式)
3. [模式切换](#3-模式切换)
4. [光标移动](#4-光标移动)
5. [编辑操作](#5-编辑操作)
6. [可视模式操作](#6-可视模式操作)
7. [搜索与替换](#7-搜索与替换)
8. [LSP / 代码导航](#8-lsp--代码导航)
9. [Git 集成](#9-git-集成)
10. [Tree-sitter 导航与文本对象](#10-tree-sitter-导航与文本对象)
11. [多光标操作](#11-多光标操作)
12. [窗格与窗口管理](#12-窗格与窗口管理)
13. [Insert 模式命令](#13-insert-模式命令)
14. [Ex 命令](#14-ex-命令)
15. [内置插件功能](#15-内置插件功能)
16. [标记与寄存器](#16-标记与寄存器)
17. [宏](#17-宏)
18. [配置](#18-配置)
19. [自定义快捷键](#19-自定义快捷键)
20. [正则表达式差异](#20-正则表达式差异)
21. [补充：文档未提及的 Vim 常用命令](#21-补充文档未提及的-vim-常用命令)

---

## 1. 概述

Zed 内置了 Vim 模拟层（Vim mode），目标是：

- **"做你期望的事"**：在合理的地方精确复制 Vim 的行为
- **利用 Zed 的现代特性**：Tree-sitter 语法解析、LSP、多光标、CRDT 协作等
- **不是 100% Vim 兼容**，但追求 **100% Vim 熟悉感**

### 与原生 Vim 的核心差异

| 方面       | Vim          | Zed                     |
| ---------- | ------------ | ----------------------- |
| 光标模型   | 定位在字符上 | 定位在字符之间          |
| 视觉块模式 | 原生块选择   | 通过多光标模拟          |
| 宏         | `q` / `@`    | 使用 Zed 的录制系统     |
| 搜索替换   | Vim 正则     | Zed 正则引擎            |
| 词语定义   | 固定规则     | 语言感知（Tree-sitter） |

---

## 2. 启用 Vim 模式

### 方法一：欢迎屏幕

首次启动 Zed 时，勾选 "Vim Mode" 复选框。

### 方法二：命令面板

`Ctrl+Shift+P` → 输入 `toggle vim mode` → 回车。

### 方法三：配置文件

在 `settings.json` 中添加：

```json
{
    "vim_mode": true
}
```

### 按发布渠道启用

```json
{
    "vim_mode": false,
    "preview": {
        "vim_mode": true
    },
    "nightly": {
        "vim_mode": true
    }
}
```

---

## 3. 模式切换

| 快捷键     | 模式         | 说明                       |
| ---------- | ------------ | -------------------------- |
| `Esc`      | Normal       | 从任何模式返回 Normal 模式 |
| `Ctrl+c`   | Normal       | 替代 Esc                   |
| `i`        | Insert       | 在当前字符前插入           |
| `a`        | Insert       | 在当前字符后插入           |
| `I`        | Insert       | 在行首第一个非空字符前插入 |
| `A`        | Insert       | 在行尾插入                 |
| `o`        | Insert       | 在下方新建一行             |
| `O`        | Insert       | 在上方新建一行             |
| `v`        | Visual       | 字符选择模式               |
| `V`        | Visual (行)  | 整行选择模式               |
| `Ctrl+v`   | Visual Block | 块选择模式（多光标模拟）   |
| `R`        | Replace      | 替换模式（覆盖输入）       |
| `r`        | Normal       | 替换单个字符               |
| `s`        | Insert       | 删除当前字符并进入 Insert  |
| `S` / `cc` | Insert       | 删除整行并进入 Insert      |
| `C`        | Insert       | 删除到行尾并进入 Insert    |

### Insert 模式下返回 Normal

除 `Esc` 和 `Ctrl+c` 外，可自定义快捷键：

```json
{
    "context": "vim_mode == insert",
    "bindings": {
        "j k": "vim::NormalBefore",
        "j j": "vim::NormalBefore"
    }
}
```

---

## 4. 光标移动

### 4.1 基本移动

| 快捷键                | 说明                    |
| --------------------- | ----------------------- |
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右       |
| `w`                   | 下一个单词开头          |
| `W`                   | 下一个 WORD（空白分隔） |
| `b`                   | 上一个单词开头          |
| `B`                   | 上一个 WORD             |
| `e`                   | 下一个单词结尾          |
| `E`                   | 下一个 WORD 结尾        |
| `ge`                  | 上一个单词结尾          |
| `gE`                  | 上一个 WORD 结尾        |

### 4.2 行内移动

| 快捷键    | 说明                              |
| --------- | --------------------------------- |
| `0`       | 行首（含空白）                    |
| `^`       | 行首第一个非空白字符              |
| `$`       | 行尾                              |
| `f{char}` | 向右查找字符（光标停在字符上）    |
| `F{char}` | 向左查找字符                      |
| `t{char}` | 向右查找字符（光标停在字符前）    |
| `T{char}` | 向左查找字符（光标停在字符后）    |
| `;`       | 重复上次 `f` / `t` 查找（同方向） |
| `,`       | 重复上次 `f` / `t` 查找（反方向） |

### 4.3 页面滚动

| 快捷键   | 说明                 |
| -------- | -------------------- |
| `Ctrl+d` | 向下翻半页           |
| `Ctrl+u` | 向上翻半页           |
| `Ctrl+f` | 向下翻一页           |
| `Ctrl+b` | 向上翻一页           |
| `zt`     | 将当前行置于屏幕顶部 |
| `zz`     | 将当前行置于屏幕中央 |
| `zb`     | 将当前行置于屏幕底部 |

### 4.4 文件级移动

| 快捷键           | 说明                                             |
| ---------------- | ------------------------------------------------ |
| `gg`             | 跳到文件第一行                                   |
| `G`              | 跳到文件最后一行                                 |
| `{n}gg` / `{n}G` | 跳到第 n 行                                      |
| `H`              | 屏幕顶部                                         |
| `M`              | 屏幕中间                                         |
| `L`              | 屏幕底部                                         |
| `{`              | 上一段（空行分隔）                               |
| `}`              | 下一段                                           |
| `%`              | 跳转到匹配的括号（语言感知，Rust 中可匹配 `\|`） |

### 4.5 跳转列表

| 快捷键   | 说明             |
| -------- | ---------------- |
| `Ctrl+o` | 跳到上一个位置   |
| `Ctrl+i` | 跳到下一个位置   |
| `Ctrl+t` | 从标签栈弹出     |
| `g;`     | 跳到上次修改位置 |
| `g,`     | 跳到下次修改位置 |

### 4.6 自定义子词移动（CamelCase / snake_case）

```json
{
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
        "w": "vim::NextSubwordStart",
        "b": "vim::PreviousSubwordStart",
        "e": "vim::NextSubwordEnd",
        "g e": "vim::PreviousSubwordEnd"
    }
}
```

---

## 5. 编辑操作

### 5.1 删除

| 快捷键     | 说明                       |
| ---------- | -------------------------- |
| `x`        | 删除当前字符               |
| `X`        | 删除前一个字符（退格）     |
| `dd`       | 删除当前行                 |
| `D`        | 删除到行尾                 |
| `dw`       | 删除到单词结尾             |
| `db`       | 删除到单词开头             |
| `daw`      | 删除整个单词（含周围空格） |
| `diw`      | 删除单词内部               |
| `d$` / `D` | 删除到行尾                 |
| `d0`       | 删除到行首                 |
| `dG`       | 删除到文件末尾             |
| `dgg`      | 删除到文件开头             |
| `dt{char}` | 删除直到指定字符           |
| `df{char}` | 删除到指定字符（含）       |

### 5.2 复制（Yank）

| 快捷键     | 说明           |
| ---------- | -------------- |
| `yy` / `Y` | 复制当前行     |
| `yw`       | 复制单词       |
| `yaw`      | 复制整个单词   |
| `y$`       | 复制到行尾     |
| `y0`       | 复制到行首     |
| `yG`       | 复制到文件末尾 |
| `ygg`      | 复制到文件开头 |

### 5.3 粘贴

| 快捷键 | 说明                       |
| ------ | -------------------------- |
| `p`    | 在光标后粘贴               |
| `P`    | 在光标前粘贴               |
| `gp`   | 粘贴并将光标移到粘贴内容后 |
| `gP`   | 粘贴并将光标移到粘贴内容前 |

### 5.4 修改（Change）

| 快捷键                | 说明                     |
| --------------------- | ------------------------ |
| `cw`                  | 修改到单词结尾           |
| `ciw`                 | 修改整个单词             |
| `caw`                 | 修改整个单词（含空格）   |
| `cc` / `S`            | 修改整行                 |
| `C`                   | 修改到行尾               |
| `ci(` / `ci[` / `ci{` | 修改括号内内容           |
| `ci"` / `ci'`         | 修改引号内内容           |
| `cit`                 | 修改 HTML/XML 标签内内容 |

### 5.5 其他编辑

| 快捷键     | 说明                     |
| ---------- | ------------------------ |
| `u`        | 撤销                     |
| `Ctrl+r`   | 重做                     |
| `.`        | 重复上次修改             |
| `>>`       | 增加缩进                 |
| `<<`       | 减少缩进                 |
| `>` + 移动 | 缩进指定范围             |
| `<` + 移动 | 减少缩进指定范围         |
| `~`        | 切换大小写               |
| `J`        | 将下一行连接到当前行     |
| `gJ`       | 连接行（不加空格）       |
| `r{char}`  | 替换当前字符             |
| `xp`       | 交换当前字符与下一个字符 |
| `ddp`      | 交换当前行与下一行       |

### 5.6 数字前缀（Count）

几乎所有命令都支持数字前缀：

| 示例                  | 效果               |
| --------------------- | ------------------ |
| `5j`                  | 向下移动 5 行      |
| `3dd`                 | 删除 3 行          |
| `4x`                  | 删除 4 个字符      |
| `2dw`                 | 删除 2 个单词      |
| `5i` + `text` + `Esc` | 插入 `text` 5 次   |
| `10.`                 | 重复上次修改 10 次 |

> **注意：** `5dj` 和 `d5j` 的区别 — `5dj` 表示"删除 5 次 = 5 行"，而 `d5j` 表示"删除操作应用于向下 5 行"。效果相同但语义不同。

---

## 6. 可视模式操作

| 快捷键                  | 说明                   |
| ----------------------- | ---------------------- |
| `v`                     | 进入字符可视模式       |
| `V`                     | 进入行可视模式         |
| `Ctrl+v`                | 进入块可视模式         |
| `gv`                    | 重新选择上次的可视区域 |
| `o` (visual 模式)       | 跳转到选择区域的另一端 |
| `O` (visual block 模式) | 跳转到对角线另一端     |
| `u` (visual 模式)       | 将选中文本转为小写     |
| `U` (visual 模式)       | 将选中文本转为大写     |
| `>`                     | 缩进选中文本           |
| `<`                     | 减少缩进选中文本       |
| `I` (block 模式)        | 在块前插入（多行同时） |
| `A` (block 模式)        | 在块后插入（多行同时） |
| `c` (visual 模式)       | 修改选中文本           |
| `d` / `x` (visual 模式) | 删除选中文本           |
| `y` (visual 模式)       | 复制选中文本           |

---

## 7. 搜索与替换

### 7.1 搜索

| 快捷键 | 说明                               |
| ------ | ---------------------------------- |
| `/`    | 向下搜索                           |
| `?`    | 向上搜索                           |
| `n`    | 下一个匹配项                       |
| `N`    | 上一个匹配项                       |
| `*`    | 搜索光标下的单词（向下）           |
| `#`    | 搜索光标下的单词（向上）           |
| `g*`   | 搜索光标下的单词（部分匹配，向下） |
| `g#`   | 搜索光标下的单词（部分匹配，向上） |
| `gn`   | 选中下一个匹配项                   |
| `gN`   | 选中上一个匹配项                   |
| `cgn`  | 修改下一个匹配项，然后 `.` 重复    |

> **搜索配置：**
>
> ```json
> {
>     "vim": {
>         "use_smartcase_find": true, // 智能大小写
>         "use_regex_search": true // 使用正则搜索
>     }
> }
> ```

### 7.2 替换

| 命令               | 说明                               |
| ------------------ | ---------------------------------- |
| `:s/old/new/`      | 替换当前行第一个匹配               |
| `:s/old/new/g`     | 替换当前行所有匹配                 |
| `:%s/old/new/`     | 替换全文（`/g` 在 Zed 中默认隐含） |
| `:%s/old/new/gc`   | 替换全文（逐个确认）               |
| `:5,10s/old/new/`  | 替换第 5-10 行                     |
| `:'<,'>s/old/new/` | 替换选中的范围                     |

> **Zed 正则差异** — 详见[第 20 节](#20-正则表达式差异)。

---

## 8. LSP / 代码导航

Zed 将 Vim 快捷键与 LSP 功能深度集成：

| 快捷键        | 说明                    |
| ------------- | ----------------------- |
| `g d`         | 跳转到定义              |
| `g D`         | 跳转到声明              |
| `g y`         | 跳转到类型定义          |
| `g I`         | 跳转到实现              |
| `c d`         | 重命名符号              |
| `g A`         | 查找所有引用            |
| `g s`         | 文件内查找符号          |
| `g S`         | 项目内查找符号          |
| `g ]` / `] d` | 下一个诊断（错误/警告） |
| `g [` / `[ d` | 上一个诊断              |
| `g h`         | 显示悬停信息            |
| `g .`         | 打开代码操作菜单        |
| `g r`         | 重命名（与 `c d` 类似） |

---

## 9. Git 集成

| 快捷键 | 说明                 |
| ------ | -------------------- |
| `] c`  | 下一个 Git 更改      |
| `[ c`  | 上一个 Git 更改      |
| `d o`  | 展开 diff hunk       |
| `d O`  | 切换暂存状态         |
| `d u`  | 暂存并移到下一个     |
| `d U`  | 取消暂存并移到下一个 |
| `d p`  | 还原更改             |

---

## 10. Tree-sitter 导航与文本对象

### 10.1 语法节点导航

| 快捷键        | 说明                         |
| ------------- | ---------------------------- |
| `] m` / `[ m` | 下一个/上一个方法            |
| `] M` / `[ M` | 下一个/上一个方法结尾        |
| `] ]` / `[ [` | 下一个/上一个段落（Section） |
| `] /` / `[ /` | 下一个/上一个注释            |
| `] *` / `[ *` | 下一个/上一个注释（同上）    |
| `[ x`         | 选择更大的语法节点           |
| `] x`         | 选择更小的语法节点           |

### 10.2 文本对象

| 快捷键                | 说明                |
| --------------------- | ------------------- |
| `a c` / `i c`         | 整个类 / 类内部     |
| `a f` / `i f`         | 整个函数 / 函数内部 |
| `g c`                 | 注释                |
| `i a` / `a a`         | 参数内部 / 整个参数 |
| `a t` / `i t`         | HTML 标签           |
| `a I` / `a i` / `i i` | 缩进级别            |
| `i w` / `a w`         | 单词                |
| `i W` / `a W`         | WORD                |
| `i s` / `a s`         | 句子                |
| `i p` / `a p`         | 段落                |
| `i (` / `a (`         | 圆括号内            |
| `i [` / `a [`         | 方括号内            |
| `i {` / `a {`         | 花括号内            |
| `i "` / `a "`         | 双引号内            |
| `i '` / `a '`         | 单引号内            |
| `i \`` / `a \``       | 反引号内            |
| `i t` / `a t`         | HTML/XML 标签内     |

### 10.3 缩进文本对象

| 快捷键      | 说明                      |
| ----------- | ------------------------- |
| `[-`        | 上一个缩进更少的行        |
| `]-`        | 下一个缩进更少的行        |
| `[+` / `]+` | 上一个/下一个缩进更多的行 |
| `[=` / `]=` | 上一个/下一个相同缩进的行 |

---

## 11. 多光标操作

Zed 在 Vim 模式下保留了多光标功能：

| 快捷键 | 说明                         |
| ------ | ---------------------------- |
| `g l`  | 添加光标到下一个相同单词     |
| `g L`  | 添加光标到上一个相同单词     |
| `g a`  | 选中当前单词的所有出现       |
| `g A`  | 在选中区域的每行末尾添加光标 |
| `g I`  | 在选中区域的每行开头添加光标 |
| `g >`  | 跳过并添加下一个             |
| `g <`  | 跳过并添加上一个             |
| `Esc`  | 退出多光标模式，回到 Normal  |

---

## 12. 窗格与窗口管理

### 12.1 分屏操作

| 快捷键            | 说明                       |
| ----------------- | -------------------------- |
| `:vs` / `:vsplit` | 垂直分屏                   |
| `:sp` / `:split`  | 水平分屏                   |
| `:new`            | 水平分屏打开新文件         |
| `:vnew`           | 垂直分屏打开新文件         |
| `Ctrl+w v`        | 垂直分屏                   |
| `Ctrl+w s`        | 水平分屏                   |
| `Ctrl+w q`        | 关闭当前窗格               |
| `Ctrl+w c`        | 关闭当前窗格               |
| `Ctrl+w o`        | 仅保留当前窗格（关闭其他） |

### 12.2 窗格间导航

| 快捷键                  | 说明             |
| ----------------------- | ---------------- |
| `Ctrl+w h` / `Ctrl+w j` | 左 / 下          |
| `Ctrl+w k` / `Ctrl+w l` | 上 / 右          |
| `Ctrl+w w`              | 循环切换窗格     |
| `Ctrl+w p`              | 切换到上一个窗格 |
| `Ctrl+w t`              | 切换到最顶部窗格 |
| `Ctrl+w b`              | 切换到最底部窗格 |

### 12.3 窗格调整

| 快捷键      | 说明         |
| ----------- | ------------ |
| `Ctrl+w =`  | 均分窗格大小 |
| `Ctrl+w >`  | 增加宽度     |
| `Ctrl+w <`  | 减少宽度     |
| `Ctrl+w +`  | 增加高度     |
| `Ctrl+w -`  | 减少高度     |
| `Ctrl+w _`  | 最大化高度   |
| `Ctrl+w \|` | 最大化宽度   |

### 12.4 标签页

| 快捷键                 | 说明                |
| ---------------------- | ------------------- |
| `:tabnew` / `:tabedit` | 新建标签页          |
| `:tabn` / `gt`         | 下一个标签页        |
| `:tabp` / `gT`         | 上一个标签页        |
| `:tabc`                | 关闭当前标签页      |
| `{n}gt`                | 跳转到第 n 个标签页 |

### 12.5 Zed 特有窗格快捷键

| 快捷键             | 说明                   |
| ------------------ | ---------------------- |
| `g /`              | 项目全局搜索           |
| `g <space>`        | 打开搜索结果面板       |
| `<ctrl-w> <space>` | 在分屏中打开搜索结果   |
| `<ctrl-w> g d`     | 在分屏中跳转到定义     |
| `<ctrl-w> g D`     | 在分屏中跳转到类型定义 |

---

## 13. Insert 模式命令

| 快捷键          | 说明                |
| --------------- | ------------------- |
| `Ctrl+x Ctrl+o` | 打开补全菜单        |
| `Ctrl+x Ctrl+c` | 请求 GitHub Copilot |
| `Ctrl+x Ctrl+a` | 打开 AI 助手        |
| `Ctrl+x Ctrl+l` | 打开代码操作        |
| `Ctrl+x Ctrl+z` | 隐藏建议            |
| `Ctrl+n`        | 下一个补全项        |
| `Ctrl+p`        | 上一个补全项        |
| `Ctrl+e`        | 取消补全            |
| `Ctrl+h`        | 删除前一个字符      |
| `Ctrl+w`        | 删除前一个单词      |
| `Ctrl+u`        | 删除到行首          |

---

## 14. Ex 命令（`:` 命令）

Zed 的 Vim 模式将 `:` 映射到命令面板，提供 Vim 风格的别名：

### 14.1 文件操作

| 命令               | 说明           |
| ------------------ | -------------- |
| `:w` / `:write`    | 保存           |
| `:wa` / `:wall`    | 保存所有文件   |
| `:q` / `:quit`     | 关闭           |
| `:wq`              | 保存并关闭     |
| `:wqa` / `:wqall`  | 保存全部并关闭 |
| `:qa` / `:qall`    | 关闭所有       |
| `:cq`              | 强制关闭       |
| `:bd` / `:bdelete` | 关闭 Buffer    |
| `:e {file}`        | 打开文件       |
| `:ls`              | 列出 Buffers   |

### 14.2 分屏与标签

| 命令                   | 说明           |
| ---------------------- | -------------- |
| `:vs` / `:vsplit`      | 垂直分屏       |
| `:sp` / `:split`       | 水平分屏       |
| `:new`                 | 水平分屏新文件 |
| `:vnew`                | 垂直分屏新文件 |
| `:tabedit` / `:tabnew` | 新标签页       |
| `:tabn` / `:tabnext`   | 下一个标签页   |
| `:tabp` / `:tabprev`   | 上一个标签页   |
| `:tabc` / `:tabclose`  | 关闭标签页     |

### 14.3 跳转与导航

| 命令        | 说明             |
| ----------- | ---------------- |
| `:{number}` | 跳转到指定行     |
| `:$`        | 跳转到文件末尾   |
| `:/foo`     | 搜索 foo（向下） |
| `:?foo`     | 搜索 foo（向上） |

### 14.4 诊断

| 命令             | 说明           |
| ---------------- | -------------- |
| `:cn` / `:cnext` | 下一个诊断     |
| `:cp` / `:cprev` | 上一个诊断     |
| `:cc`            | 显示当前诊断   |
| `:clist` / `:cl` | 列出所有诊断   |
| `:ln` / `:lnext` | 下一个位置     |
| `:lp` / `:lprev` | 上一个位置     |
| `:ll`            | 当前定位列表项 |

### 14.5 替换

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `:s/foo/bar/`     | 替换当前行（Zed 默认全局） |
| `:%s/foo/bar/`    | 全文替换                   |
| `:5,10s/foo/bar/` | 范围替换                   |

### 14.6 编辑命令

| 命令             | 说明                         |
| ---------------- | ---------------------------- |
| `:j` / `:join`   | 连接行                       |
| `:d` / `:delete` | 删除行                       |
| `:s` / `:sort`   | 排序（`:sort i` 忽略大小写） |
| `:y` / `:yank`   | 复制行                       |

### 14.7 设置选项

| 命令                        | 说明       |
| --------------------------- | ---------- |
| `:set wrap` / `:set nowrap` | 自动换行   |
| `:set nu` / `:set nonu`     | 行号       |
| `:set rnu` / `:set nornu`   | 相对行号   |
| `:set ic` / `:set noic`     | 忽略大小写 |

### 14.8 Zed 特有命令

| 命令                   | 说明               |
| ---------------------- | ------------------ |
| `:E` / `:Explore`      | 打开项目面板       |
| `:C` / `:Collab`       | 协作面板           |
| `:Ch` / `:Chat`        | 聊天面板           |
| `:A` / `:AI`           | AI 助手            |
| `:No` / `:Notif`       | 通知面板           |
| `:fe` / `:feedback`    | 反馈               |
| `:te` / `:term`        | 终端               |
| `:Ext` / `:Extensions` | 扩展面板           |
| `:G` / `:Git`          | Git 面板           |
| `:diffs`               | 切换 Hunk Diff     |
| `:cpp`                 | 复制路径           |
| `:crp`                 | 复制相对路径       |
| `:reveal`              | 在文件管理器中显示 |
| `:zlog`                | 打开 Zed 日志      |
| `:clank`               | 取消语言服务器任务 |

### 14.9 自定义命令别名

```json
{
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q",
        "Wa": "wa"
    }
}
```

---

## 15. 内置插件功能

Zed 内置了常用 Vim 插件功能的等价实现：

### 15.1 Surround（vim-surround）

| 快捷键             | 说明                                     |
| ------------------ | ---------------------------------------- |
| `ys{motion}{char}` | 添加包围（如 `ysiw"` 用双引号包围单词）  |
| `cs{old}{new}`     | 修改包围（如 `cs"'` 将双引号改为单引号） |
| `ds{char}`         | 删除包围（如 `ds[` 删除方括号）          |
| `S{char}` (visual) | 用字符包围选中文本                       |

> Visual 模式下需要自定义绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
>     }
> }
> ```

### 15.2 Comment（vim-commentary）

| 快捷键        | 说明                            |
| ------------- | ------------------------------- |
| `gcc`         | 切换当前行注释                  |
| `gc` (visual) | 切换选中行注释                  |
| `gc{motion}`  | 对移动范围切换注释（如 `gc3j`） |

### 15.3 Exchange（vim-exchange）

| 快捷键        | 说明             |
| ------------- | ---------------- |
| `cx{motion}`  | 标记要交换的文本 |
| `cx` (第二次) | 执行交换         |

> Visual 模式下需手动绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-x": "vim::Exchange"
>     }
> }
> ```

### 15.4 ReplaceWithRegister

| 快捷键       | 说明                                 |
| ------------ | ------------------------------------ |
| `gR{motion}` | 用寄存器内容替换文本（不覆盖寄存器） |

### 15.5 netrw 风格项目面板

在项目面板中使用 `hjkl` 导航：

| 快捷键                | 说明           |
| --------------------- | -------------- |
| `h` / `j` / `k` / `l` | 导航           |
| `o`                   | 打开文件       |
| `t`                   | 在新标签页打开 |

### 15.6 Sneak 导航

需自定义绑定：

```json
{
    "context": "vim_mode == normal || vim_mode == visual",
    "bindings": {
        "s": "vim::PushSneak",
        "shift-s": "vim::PushSneakBackward"
    }
}
```

### 15.7 AnyQuotes / AnyBrackets 文本对象

```json
{
    "context": "vim_operator == a || vim_operator == i || vim_operator == cs",
    "bindings": {
        "q": "vim::AnyQuotes",
        "b": "vim::AnyBrackets",
        "Q": "vim::MiniQuotes",
        "B": "vim::MiniBrackets"
    }
}
```

---

## 16. 标记与寄存器

### 16.1 标记（Marks）

| 快捷键       | 说明                       |
| ------------ | -------------------------- |
| `m{a-z}`     | 设置缓冲区本地标记         |
| `'{a-z}`     | 跳转到标记所在行           |
| `` `{a-z} `` | 跳转到标记精确位置         |
| `''`         | 跳转到上次位置             |
| `'<` / `'>`  | 上次可视选择的开始/结束    |
| `'[` / `']`  | 上次修改/复制的开始/结束   |
| `'{` / `'}`  | 段落跳转标记               |
| `'.`         | 上次修改位置               |
| `'^`         | 上次 Insert 模式退出的位置 |

### 16.2 寄存器（Registers）

| 寄存器      | 说明                                  |
| ----------- | ------------------------------------- |
| `""`        | 无名寄存器（默认）                    |
| `"0`        | 最近复制的文本                        |
| `"1` - `"9` | 最近删除的文本（从新到旧）            |
| `"a` - `"z` | 命名寄存器                            |
| `"+`        | 系统剪贴板                            |
| `"*`        | 选择剪贴板（Linux/Windows 等同 `"+`） |
| `"_`        | 黑洞寄存器（删除不保存）              |
| `"%`        | 当前文件名                            |
| `".`        | 上次插入的文本                        |

使用方式：

- `"ayw` — 将单词复制到寄存器 a
- `"ap` — 粘贴寄存器 a 的内容
- `"+y` — 复制到系统剪贴板
- `"_dd` — 删除行但不保存到寄存器

### 16.3 剪贴板配置

```json
{
    "vim": {
        "use_system_clipboard": "always"
        // "always"  — 始终使用系统剪贴板
        // "never"   — 永远不使用
        // "on_yank" — 仅复制时使用
    }
}
```

---

## 17. 宏

Zed 使用自己的录制系统实现 Vim 宏功能：

| 快捷键       | 说明                    |
| ------------ | ----------------------- |
| `q{a-z}`     | 开始录制到寄存器 {a-z}  |
| `q` (录制中) | 停止录制                |
| `Q`          | 重复上次录制的宏        |
| `@{a-z}`     | 执行寄存器 {a-z} 中的宏 |
| `@@`         | 重复上次执行的宏        |
| `{n}@{a-z}`  | 执行宏 n 次             |

> **注意：** Zed 的宏系统与原生 Vim 不同，宏存储与复制寄存器是独立的命名空间。

---

## 18. 配置

### 18.1 Vim 专属配置

在 `settings.json` 的 `"vim"` 键下配置：

```json
{
    "vim": {
        // 系统剪贴板策略："always" | "never" | "on_yank"
        "use_system_clipboard": "always",

        // 多行搜索
        "use_multiline_find": true,

        // 智能大小写搜索（全小写时忽略大小写，含大写时精确匹配）
        "use_smartcase_find": true,

        // 切换相对行号
        "toggle_relative_line_numbers": true,

        // 自定义有向图（digraph），如 Ctrl+k f z → 🧟
        "custom_digraphs": { "fz": "🧟♀️" },

        // 启动时的默认模式
        "default_mode": "normal",

        // 复制后高亮持续时间（毫秒），0 禁用
        "highlight_on_yank_duration": 200,

        // 使用正则搜索
        "use_regex_search": true,

        // 替换默认全局（相当于始终 /g）
        "gdefault": false
    }
}
```

### 18.2 推荐的伴随设置

```json
{
    // 禁用光标闪烁（像原生 Vim）
    "cursor_blink": false,

    // 相对行号
    "relative_line_numbers": true,

    // 滚动条
    "scrollbar": { "show": "never" },

    // 不允许滚动超过最后一行
    "scroll_beyond_last_line": "off",

    // 垂直滚动边距
    "vertical_scroll_margin": 0,

    // 行号
    "gutter": { "line_numbers": false },

    // 命令别名
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q"
    }
}
```

---

## 19. 自定义快捷键

### 19.1 Vim 上下文

自定义快捷键需要理解 Zed 的上下文系统：

| 上下文                 | 含义                                          |
| ---------------------- | --------------------------------------------- |
| `VimControl`           | `normal` \|\| `visual` \|\| `operator` 的别名 |
| `vim_mode == normal`   | Normal 模式                                   |
| `vim_mode == visual`   | Visual 模式                                   |
| `vim_mode == insert`   | Insert 模式                                   |
| `vim_mode == replace`  | Replace 模式                                  |
| `vim_mode == waiting`  | 等待（如 `f`/`t` 后等待字符）                 |
| `vim_mode == operator` | 操作符挂起（如 `c`/`d` 后等待移动）           |
| `vim_operator == d`    | 当前操作符为 `d`                              |
| `vim_operator == c`    | 当前操作符为 `c`                              |
| `vim_operator == y`    | 当前操作符为 `y`                              |

### 19.2 示例

```json
[
    // Normal 模式下的自定义
    {
        "context": "vim_mode == normal && !menu",
        "bindings": {
            "shift-y": ["workspace::SendKeystrokes", "y $"],
            "space w": "workspace::Save"
        }
    },

    // Visual 模式下的自定义
    {
        "context": "vim_mode == visual",
        "bindings": {
            "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
        }
    },

    // Insert 模式退出
    {
        "context": "vim_mode == insert",
        "bindings": {
            "j k": "vim::NormalBefore",
            "j j": "vim::NormalBefore"
        }
    },

    // 窗格中
    {
        "context": "EmptyPane || SharedScreen",
        "bindings": {
            "space f": "file_finder::Toggle"
        }
    }
]
```

### 19.3 恢复系统剪贴板快捷键（Linux/Windows）

```json
{
    "context": "Editor && !menu",
    "bindings": {
        "ctrl-c": "editor::Copy",
        "ctrl-x": "editor::Cut",
        "ctrl-v": "editor::Paste",
        "ctrl-y": "editor::Undo",
        "ctrl-f": "buffer_search::Deploy",
        "ctrl-o": "workspace::Open",
        "ctrl-a": "editor::SelectAll"
    }
}
```

---

## 20. 正则表达式差异

Zed 使用不同的正则引擎（Rust `regex` crate），与 Vim 的语法有差异：

| 特性         | Vim       | Zed                                |
| ------------ | --------- | ---------------------------------- |
| 捕获组       | `\(` `\)` | `(` `)`                            |
| 字面括号     | `(` `)`   | `\(` `\)`                          |
| 完整匹配引用 | `\0`      | `$0`                               |
| 编号组引用   | `\1`      | `$1`                               |
| 全局标志     | 需 `/g`   | 默认全局（可设 `gdefault: false`） |
| 大小写不敏感 | `/i` 标志 | `(?i)` 或 `cmd-option-c`           |

> `:s` 命令面板会自动转换 Vim 风格的语法。

---

## 21. 补充：文档未提及的 Vim 常用命令

以下命令在 Zed 官方文档中未明确列出，但在日常 Vim 使用中非常重要。**部分在 Zed 中可直接使用，部分可能需要验证。**

### 21.1 Normal 模式进阶命令

| 快捷键             | 说明                              | Zed 支持      |
| ------------------ | --------------------------------- | ------------- |
| `g_`               | 跳转到行尾最后一个非空白字符      | ⚠️ 待验证     |
| `g^`               | 跳转到行首第一个非空白字符        | ⚠️ 待验证     |
| `gm`               | 跳转到行中间                      | ⚠️ 待验证     |
| `g0`               | 跳转到行首（即使是换行行）        | ⚠️ 待验证     |
| `g$`               | 跳转到行尾（即使是换行行）        | ⚠️ 待验证     |
| `g~` / `gu` / `gU` | 切换/小写/大写 + 移动             | ✅ 基本支持   |
| `g?`               | Rot13 编码                        | ❌ 通常不支持 |
| `ga`               | 显示光标下字符的 ASCII/Unicode 值 | ⚠️ 待验证     |
| `g Ctrl+g`         | 显示光标位置统计                  | ⚠️ 待验证     |
| `Ctrl+g`           | 显示文件信息和当前行号            | ⚠️ 待验证     |
| `del`              | 删除光标后字符（同 `x`）          | ⚠️ 待验证     |
| `D`                | 删除到行尾                        | ✅ 支持       |
| `Y`                | 复制整行（同 `yy`）               | ✅ 支持       |
| `C`                | 修改到行尾                        | ✅ 支持       |
| `s`                | 删除字符并进入 Insert             | ✅ 支持       |
| `S` / `cc`         | 删除整行并进入 Insert             | ✅ 支持       |
| `~`                | 切换字符大小写                    | ✅ 支持       |

### 21.2 窗口/标签页进阶

| 快捷键                   | 说明                   | Zed 支持           |
| ------------------------ | ---------------------- | ------------------ |
| `Ctrl+w T`               | 将当前窗格移到新标签页 | ⚠️ 待验证          |
| `Ctrl+w x`               | 交换窗格位置           | ⚠️ 待验证          |
| `Ctrl+w r`               | 旋转窗格               | ⚠️ 待验证          |
| `Ctrl+w H`               | 将窗格移到左侧（全高） | ⚠️ 待验证          |
| `Ctrl+w J`               | 将窗格移到底部（全宽） | ⚠️ 待验证          |
| `Ctrl+w K`               | 将窗格移到顶部（全宽） | ⚠️ 待验证          |
| `Ctrl+w L`               | 将窗格移到右侧（全高） | ⚠️ 待验证          |
| `:on` / `:only`          | 仅保留当前窗格         | ✅ 通过 `Ctrl+w o` |
| `:tabm {n}`              | 移动标签页到位置 n     | ⚠️ 待验证          |
| `:tabfirst` / `:tablast` | 第一个/最后一个标签页  | ⚠️ 待验证          |

### 21.3 折叠命令

Zed 使用 Tree-sitter 的代码折叠，以下命令**可能需要验证**：

| 快捷键       | 说明                     |
| ------------ | ------------------------ |
| `za`         | 切换当前折叠             |
| `zA`         | 递归切换折叠             |
| `zc`         | 关闭折叠                 |
| `zC`         | 递归关闭折叠             |
| `zo`         | 打开折叠                 |
| `zO`         | 递归打开折叠             |
| `zm`         | 增加折叠级别（关闭更多） |
| `zM`         | 关闭所有折叠             |
| `zr`         | 减少折叠级别（打开更多） |
| `zR`         | 打开所有折叠             |
| `zf{motion}` | 手动创建折叠             |
| `zd`         | 删除折叠                 |
| `zE`         | 删除所有折叠             |

### 21.4 插入模式进阶

| 快捷键              | 说明                                | Zed 支持           |
| ------------------- | ----------------------------------- | ------------------ |
| `Ctrl+v {code}`     | 插入任意字符（如 `Ctrl+v 065` → A） | ⚠️ 待验证          |
| `Ctrl+k {digraph}`  | 插入有向图（如 `Ctrl+k a:` → ä）    | ✅ 自定义 digraphs |
| `Ctrl+a`            | 插入上次 Insert 模式的文本          | ⚠️ 待验证          |
| `Ctrl+@`            | 插入上次 Insert 文本并退出          | ⚠️ 待验证          |
| `Ctrl+d`            | 减少缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+t`            | 增加缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+y`            | 插入上一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+e`            | 插入下一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+j` / `Ctrl+m` | 换行（同 Enter）                    | ✅ 支持            |
| `Ctrl+r {reg}`      | 插入寄存器内容                      | ⚠️ 待验证          |

### 21.5 有用但常被忽视的命令

| 快捷键      | 说明                      | Zed 支持        |
| ----------- | ------------------------- | --------------- |
| `g&`        | 在所有行上重复上次替换    | ⚠️ 待验证       |
| `&`         | 在当前行重复上次替换      | ⚠️ 待验证       |
| `@:`        | 重复上次 Ex 命令          | ⚠️ 待验证       |
| `q:`        | 打开命令历史窗口          | ❌ Zed 无此概念 |
| `q/` / `q?` | 打开搜索历史窗口          | ❌ Zed 无此概念 |
| `K`         | 查看光标下关键字的 man 页 | ❌ 被重映射     |
| `zi`        | 切换折叠启用/禁用         | ⚠️ 待验证       |
| `zv`        | 打开足够折叠以显示光标    | ⚠️ 待验证       |
| `zx` / `zX` | 更新折叠/重新应用折叠     | ⚠️ 待验证       |
| `:retab`    | 转换 tab 为空格（或反之） | ⚠️ 待验证       |

### 21.6 快速修复窗口操作

| 快捷键               | 说明                 | Zed 支持        |
| -------------------- | -------------------- | --------------- |
| `:copen`             | 打开快速修复窗口     | ❌ 诊断面板替代 |
| `:cclose`            | 关闭快速修复窗口     | ❌              |
| `:cfirst` / `:clast` | 第一个/最后一个错误  | ⚠️ 待验证       |
| `:cdo {cmd}`         | 对每个错误项执行命令 | ❌              |

### 21.7 Buffer 管理进阶

| 快捷键               | 说明                   | Zed 支持  |
| -------------------- | ---------------------- | --------- |
| `:bufdo {cmd}`       | 对所有 Buffer 执行命令 | ❌        |
| `:b {num}`           | 切换到指定编号 Buffer  | ⚠️ 待验证 |
| `:bnext` / `:bprev`  | 同 `:bn` / `:bp`       | ✅ 支持   |
| `:bfirst` / `:blast` | 第一个/最后一个 Buffer | ⚠️ 待验证 |
| `ctrl+^` / `ctrl+6`  | 切换交替文件           | ⚠️ 待验证 |

### 21.8 实用技巧组合

这些都是 Vim 中常用的高效编辑组合：

| 组合        | 效果                             |
| ----------- | -------------------------------- |
| `ci"`       | 修改双引号内容                   |
| `ca"`       | 修改双引号包含引号本身           |
| `yi(`       | 复制括号内容                     |
| `da]`       | 删除方括号及内容                 |
| `vit` + `c` | 修改 HTML 标签内容               |
| `vat` + `d` | 删除整个 HTML 标签               |
| `gUiw`      | 将单词转为大写                   |
| `guiw`      | 将单词转为小写                   |
| `g~iw`      | 切换单词大小写                   |
| `guG`       | 从当前位置到文件末转为小写       |
| `gUgg`      | 从当前位置到文件头转为大写       |
| `=%`        | 缩进整个匹配块（光标在括号上时） |
| `>i{`       | 缩进花括号内容                   |
| `<a{`       | 减少花括号块缩进                 |

---

## 参考资源

- [Zed 官方 Vim 文档](https://zed.dev/docs/vim)
- [Zed 官方 Key Bindings 文档](https://zed.dev/docs/key-bindings)
- [Zed Decoded: Vim (博客)](https://zed.dev/blog/zed-decoded-vim)
- [Zed Vim Roadmap 2025](https://zed.dev/blog/vim-2025)
- [Zed GitHub 仓库](https://github.com/zed-industries/zed)
- [Zed Vim 模式源码文档](https://git.iko.soy/zed-industries/zed/src/commit/174e12568669e70abb15934597a3f5df335eaf08/docs/src/vim.md)

> **注意：** 标记为 ⚠️ 的条目需要在实际使用中验证。Zed 的 Vim 模式在持续更新中，如有缺失功能，可在 [Zed GitHub Issues](https://github.com/zed-industries/zed/issues) 中查找或提交。
>
> 本文档基于 2025 年 7 月的 Zed Vim 模式状态整理。# Zed 中 Vim 模式完整使用文档

> 基于 [Zed 官方 Vim 文档](https://zed.dev/docs/vim) 整理，并补充文档中未提及的 Vim 常用命令。

---

## 目录

1. [概述](#1-概述)
2. [启用 Vim 模式](#2-启用-vim-模式)
3. [模式切换](#3-模式切换)
4. [光标移动](#4-光标移动)
5. [编辑操作](#5-编辑操作)
6. [可视模式操作](#6-可视模式操作)
7. [搜索与替换](#7-搜索与替换)
8. [LSP / 代码导航](#8-lsp--代码导航)
9. [Git 集成](#9-git-集成)
10. [Tree-sitter 导航与文本对象](#10-tree-sitter-导航与文本对象)
11. [多光标操作](#11-多光标操作)
12. [窗格与窗口管理](#12-窗格与窗口管理)
13. [Insert 模式命令](#13-insert-模式命令)
14. [Ex 命令](#14-ex-命令)
15. [内置插件功能](#15-内置插件功能)
16. [标记与寄存器](#16-标记与寄存器)
17. [宏](#17-宏)
18. [配置](#18-配置)
19. [自定义快捷键](#19-自定义快捷键)
20. [正则表达式差异](#20-正则表达式差异)
21. [补充：文档未提及的 Vim 常用命令](#21-补充文档未提及的-vim-常用命令)

---

## 1. 概述

Zed 内置了 Vim 模拟层（Vim mode），目标是：

- **"做你期望的事"**：在合理的地方精确复制 Vim 的行为
- **利用 Zed 的现代特性**：Tree-sitter 语法解析、LSP、多光标、CRDT 协作等
- **不是 100% Vim 兼容**，但追求 **100% Vim 熟悉感**

### 与原生 Vim 的核心差异

| 方面       | Vim          | Zed                     |
| ---------- | ------------ | ----------------------- |
| 光标模型   | 定位在字符上 | 定位在字符之间          |
| 视觉块模式 | 原生块选择   | 通过多光标模拟          |
| 宏         | `q` / `@`    | 使用 Zed 的录制系统     |
| 搜索替换   | Vim 正则     | Zed 正则引擎            |
| 词语定义   | 固定规则     | 语言感知（Tree-sitter） |

---

## 2. 启用 Vim 模式

### 方法一：欢迎屏幕

首次启动 Zed 时，勾选 "Vim Mode" 复选框。

### 方法二：命令面板

`Ctrl+Shift+P` → 输入 `toggle vim mode` → 回车。

### 方法三：配置文件

在 `settings.json` 中添加：

```json
{
    "vim_mode": true
}
```

### 按发布渠道启用

```json
{
    "vim_mode": false,
    "preview": {
        "vim_mode": true
    },
    "nightly": {
        "vim_mode": true
    }
}
```

---

## 3. 模式切换

| 快捷键     | 模式         | 说明                       |
| ---------- | ------------ | -------------------------- |
| `Esc`      | Normal       | 从任何模式返回 Normal 模式 |
| `Ctrl+c`   | Normal       | 替代 Esc                   |
| `i`        | Insert       | 在当前字符前插入           |
| `a`        | Insert       | 在当前字符后插入           |
| `I`        | Insert       | 在行首第一个非空字符前插入 |
| `A`        | Insert       | 在行尾插入                 |
| `o`        | Insert       | 在下方新建一行             |
| `O`        | Insert       | 在上方新建一行             |
| `v`        | Visual       | 字符选择模式               |
| `V`        | Visual (行)  | 整行选择模式               |
| `Ctrl+v`   | Visual Block | 块选择模式（多光标模拟）   |
| `R`        | Replace      | 替换模式（覆盖输入）       |
| `r`        | Normal       | 替换单个字符               |
| `s`        | Insert       | 删除当前字符并进入 Insert  |
| `S` / `cc` | Insert       | 删除整行并进入 Insert      |
| `C`        | Insert       | 删除到行尾并进入 Insert    |

### Insert 模式下返回 Normal

除 `Esc` 和 `Ctrl+c` 外，可自定义快捷键：

```json
{
    "context": "vim_mode == insert",
    "bindings": {
        "j k": "vim::NormalBefore",
        "j j": "vim::NormalBefore"
    }
}
```

---

## 4. 光标移动

### 4.1 基本移动

| 快捷键                | 说明                    |
| --------------------- | ----------------------- |
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右       |
| `w`                   | 下一个单词开头          |
| `W`                   | 下一个 WORD（空白分隔） |
| `b`                   | 上一个单词开头          |
| `B`                   | 上一个 WORD             |
| `e`                   | 下一个单词结尾          |
| `E`                   | 下一个 WORD 结尾        |
| `ge`                  | 上一个单词结尾          |
| `gE`                  | 上一个 WORD 结尾        |

### 4.2 行内移动

| 快捷键    | 说明                              |
| --------- | --------------------------------- |
| `0`       | 行首（含空白）                    |
| `^`       | 行首第一个非空白字符              |
| `$`       | 行尾                              |
| `f{char}` | 向右查找字符（光标停在字符上）    |
| `F{char}` | 向左查找字符                      |
| `t{char}` | 向右查找字符（光标停在字符前）    |
| `T{char}` | 向左查找字符（光标停在字符后）    |
| `;`       | 重复上次 `f` / `t` 查找（同方向） |
| `,`       | 重复上次 `f` / `t` 查找（反方向） |

### 4.3 页面滚动

| 快捷键   | 说明                 |
| -------- | -------------------- |
| `Ctrl+d` | 向下翻半页           |
| `Ctrl+u` | 向上翻半页           |
| `Ctrl+f` | 向下翻一页           |
| `Ctrl+b` | 向上翻一页           |
| `zt`     | 将当前行置于屏幕顶部 |
| `zz`     | 将当前行置于屏幕中央 |
| `zb`     | 将当前行置于屏幕底部 |

### 4.4 文件级移动

| 快捷键           | 说明                                             |
| ---------------- | ------------------------------------------------ |
| `gg`             | 跳到文件第一行                                   |
| `G`              | 跳到文件最后一行                                 |
| `{n}gg` / `{n}G` | 跳到第 n 行                                      |
| `H`              | 屏幕顶部                                         |
| `M`              | 屏幕中间                                         |
| `L`              | 屏幕底部                                         |
| `{`              | 上一段（空行分隔）                               |
| `}`              | 下一段                                           |
| `%`              | 跳转到匹配的括号（语言感知，Rust 中可匹配 `\|`） |

### 4.5 跳转列表

| 快捷键   | 说明             |
| -------- | ---------------- |
| `Ctrl+o` | 跳到上一个位置   |
| `Ctrl+i` | 跳到下一个位置   |
| `Ctrl+t` | 从标签栈弹出     |
| `g;`     | 跳到上次修改位置 |
| `g,`     | 跳到下次修改位置 |

### 4.6 自定义子词移动（CamelCase / snake_case）

```json
{
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
        "w": "vim::NextSubwordStart",
        "b": "vim::PreviousSubwordStart",
        "e": "vim::NextSubwordEnd",
        "g e": "vim::PreviousSubwordEnd"
    }
}
```

---

## 5. 编辑操作

### 5.1 删除

| 快捷键     | 说明                       |
| ---------- | -------------------------- |
| `x`        | 删除当前字符               |
| `X`        | 删除前一个字符（退格）     |
| `dd`       | 删除当前行                 |
| `D`        | 删除到行尾                 |
| `dw`       | 删除到单词结尾             |
| `db`       | 删除到单词开头             |
| `daw`      | 删除整个单词（含周围空格） |
| `diw`      | 删除单词内部               |
| `d$` / `D` | 删除到行尾                 |
| `d0`       | 删除到行首                 |
| `dG`       | 删除到文件末尾             |
| `dgg`      | 删除到文件开头             |
| `dt{char}` | 删除直到指定字符           |
| `df{char}` | 删除到指定字符（含）       |

### 5.2 复制（Yank）

| 快捷键     | 说明           |
| ---------- | -------------- |
| `yy` / `Y` | 复制当前行     |
| `yw`       | 复制单词       |
| `yaw`      | 复制整个单词   |
| `y$`       | 复制到行尾     |
| `y0`       | 复制到行首     |
| `yG`       | 复制到文件末尾 |
| `ygg`      | 复制到文件开头 |

### 5.3 粘贴

| 快捷键 | 说明                       |
| ------ | -------------------------- |
| `p`    | 在光标后粘贴               |
| `P`    | 在光标前粘贴               |
| `gp`   | 粘贴并将光标移到粘贴内容后 |
| `gP`   | 粘贴并将光标移到粘贴内容前 |

### 5.4 修改（Change）

| 快捷键                | 说明                     |
| --------------------- | ------------------------ |
| `cw`                  | 修改到单词结尾           |
| `ciw`                 | 修改整个单词             |
| `caw`                 | 修改整个单词（含空格）   |
| `cc` / `S`            | 修改整行                 |
| `C`                   | 修改到行尾               |
| `ci(` / `ci[` / `ci{` | 修改括号内内容           |
| `ci"` / `ci'`         | 修改引号内内容           |
| `cit`                 | 修改 HTML/XML 标签内内容 |

### 5.5 其他编辑

| 快捷键     | 说明                     |
| ---------- | ------------------------ |
| `u`        | 撤销                     |
| `Ctrl+r`   | 重做                     |
| `.`        | 重复上次修改             |
| `>>`       | 增加缩进                 |
| `<<`       | 减少缩进                 |
| `>` + 移动 | 缩进指定范围             |
| `<` + 移动 | 减少缩进指定范围         |
| `~`        | 切换大小写               |
| `J`        | 将下一行连接到当前行     |
| `gJ`       | 连接行（不加空格）       |
| `r{char}`  | 替换当前字符             |
| `xp`       | 交换当前字符与下一个字符 |
| `ddp`      | 交换当前行与下一行       |

### 5.6 数字前缀（Count）

几乎所有命令都支持数字前缀：

| 示例                  | 效果               |
| --------------------- | ------------------ |
| `5j`                  | 向下移动 5 行      |
| `3dd`                 | 删除 3 行          |
| `4x`                  | 删除 4 个字符      |
| `2dw`                 | 删除 2 个单词      |
| `5i` + `text` + `Esc` | 插入 `text` 5 次   |
| `10.`                 | 重复上次修改 10 次 |

> **注意：** `5dj` 和 `d5j` 的区别 — `5dj` 表示"删除 5 次 = 5 行"，而 `d5j` 表示"删除操作应用于向下 5 行"。效果相同但语义不同。

---

## 6. 可视模式操作

| 快捷键                  | 说明                   |
| ----------------------- | ---------------------- |
| `v`                     | 进入字符可视模式       |
| `V`                     | 进入行可视模式         |
| `Ctrl+v`                | 进入块可视模式         |
| `gv`                    | 重新选择上次的可视区域 |
| `o` (visual 模式)       | 跳转到选择区域的另一端 |
| `O` (visual block 模式) | 跳转到对角线另一端     |
| `u` (visual 模式)       | 将选中文本转为小写     |
| `U` (visual 模式)       | 将选中文本转为大写     |
| `>`                     | 缩进选中文本           |
| `<`                     | 减少缩进选中文本       |
| `I` (block 模式)        | 在块前插入（多行同时） |
| `A` (block 模式)        | 在块后插入（多行同时） |
| `c` (visual 模式)       | 修改选中文本           |
| `d` / `x` (visual 模式) | 删除选中文本           |
| `y` (visual 模式)       | 复制选中文本           |

---

## 7. 搜索与替换

### 7.1 搜索

| 快捷键 | 说明                               |
| ------ | ---------------------------------- |
| `/`    | 向下搜索                           |
| `?`    | 向上搜索                           |
| `n`    | 下一个匹配项                       |
| `N`    | 上一个匹配项                       |
| `*`    | 搜索光标下的单词（向下）           |
| `#`    | 搜索光标下的单词（向上）           |
| `g*`   | 搜索光标下的单词（部分匹配，向下） |
| `g#`   | 搜索光标下的单词（部分匹配，向上） |
| `gn`   | 选中下一个匹配项                   |
| `gN`   | 选中上一个匹配项                   |
| `cgn`  | 修改下一个匹配项，然后 `.` 重复    |

> **搜索配置：**
>
> ```json
> {
>     "vim": {
>         "use_smartcase_find": true, // 智能大小写
>         "use_regex_search": true // 使用正则搜索
>     }
> }
> ```

### 7.2 替换

| 命令               | 说明                               |
| ------------------ | ---------------------------------- |
| `:s/old/new/`      | 替换当前行第一个匹配               |
| `:s/old/new/g`     | 替换当前行所有匹配                 |
| `:%s/old/new/`     | 替换全文（`/g` 在 Zed 中默认隐含） |
| `:%s/old/new/gc`   | 替换全文（逐个确认）               |
| `:5,10s/old/new/`  | 替换第 5-10 行                     |
| `:'<,'>s/old/new/` | 替换选中的范围                     |

> **Zed 正则差异** — 详见[第 20 节](#20-正则表达式差异)。

---

## 8. LSP / 代码导航

Zed 将 Vim 快捷键与 LSP 功能深度集成：

| 快捷键        | 说明                    |
| ------------- | ----------------------- |
| `g d`         | 跳转到定义              |
| `g D`         | 跳转到声明              |
| `g y`         | 跳转到类型定义          |
| `g I`         | 跳转到实现              |
| `c d`         | 重命名符号              |
| `g A`         | 查找所有引用            |
| `g s`         | 文件内查找符号          |
| `g S`         | 项目内查找符号          |
| `g ]` / `] d` | 下一个诊断（错误/警告） |
| `g [` / `[ d` | 上一个诊断              |
| `g h`         | 显示悬停信息            |
| `g .`         | 打开代码操作菜单        |
| `g r`         | 重命名（与 `c d` 类似） |

---

## 9. Git 集成

| 快捷键 | 说明                 |
| ------ | -------------------- |
| `] c`  | 下一个 Git 更改      |
| `[ c`  | 上一个 Git 更改      |
| `d o`  | 展开 diff hunk       |
| `d O`  | 切换暂存状态         |
| `d u`  | 暂存并移到下一个     |
| `d U`  | 取消暂存并移到下一个 |
| `d p`  | 还原更改             |

---

## 10. Tree-sitter 导航与文本对象

### 10.1 语法节点导航

| 快捷键        | 说明                         |
| ------------- | ---------------------------- |
| `] m` / `[ m` | 下一个/上一个方法            |
| `] M` / `[ M` | 下一个/上一个方法结尾        |
| `] ]` / `[ [` | 下一个/上一个段落（Section） |
| `] /` / `[ /` | 下一个/上一个注释            |
| `] *` / `[ *` | 下一个/上一个注释（同上）    |
| `[ x`         | 选择更大的语法节点           |
| `] x`         | 选择更小的语法节点           |

### 10.2 文本对象

| 快捷键                | 说明                |
| --------------------- | ------------------- |
| `a c` / `i c`         | 整个类 / 类内部     |
| `a f` / `i f`         | 整个函数 / 函数内部 |
| `g c`                 | 注释                |
| `i a` / `a a`         | 参数内部 / 整个参数 |
| `a t` / `i t`         | HTML 标签           |
| `a I` / `a i` / `i i` | 缩进级别            |
| `i w` / `a w`         | 单词                |
| `i W` / `a W`         | WORD                |
| `i s` / `a s`         | 句子                |
| `i p` / `a p`         | 段落                |
| `i (` / `a (`         | 圆括号内            |
| `i [` / `a [`         | 方括号内            |
| `i {` / `a {`         | 花括号内            |
| `i "` / `a "`         | 双引号内            |
| `i '` / `a '`         | 单引号内            |
| `i \`` / `a \``       | 反引号内            |
| `i t` / `a t`         | HTML/XML 标签内     |

### 10.3 缩进文本对象

| 快捷键      | 说明                      |
| ----------- | ------------------------- |
| `[-`        | 上一个缩进更少的行        |
| `]-`        | 下一个缩进更少的行        |
| `[+` / `]+` | 上一个/下一个缩进更多的行 |
| `[=` / `]=` | 上一个/下一个相同缩进的行 |

---

## 11. 多光标操作

Zed 在 Vim 模式下保留了多光标功能：

| 快捷键 | 说明                         |
| ------ | ---------------------------- |
| `g l`  | 添加光标到下一个相同单词     |
| `g L`  | 添加光标到上一个相同单词     |
| `g a`  | 选中当前单词的所有出现       |
| `g A`  | 在选中区域的每行末尾添加光标 |
| `g I`  | 在选中区域的每行开头添加光标 |
| `g >`  | 跳过并添加下一个             |
| `g <`  | 跳过并添加上一个             |
| `Esc`  | 退出多光标模式，回到 Normal  |

---

## 12. 窗格与窗口管理

### 12.1 分屏操作

| 快捷键            | 说明                       |
| ----------------- | -------------------------- |
| `:vs` / `:vsplit` | 垂直分屏                   |
| `:sp` / `:split`  | 水平分屏                   |
| `:new`            | 水平分屏打开新文件         |
| `:vnew`           | 垂直分屏打开新文件         |
| `Ctrl+w v`        | 垂直分屏                   |
| `Ctrl+w s`        | 水平分屏                   |
| `Ctrl+w q`        | 关闭当前窗格               |
| `Ctrl+w c`        | 关闭当前窗格               |
| `Ctrl+w o`        | 仅保留当前窗格（关闭其他） |

### 12.2 窗格间导航

| 快捷键                  | 说明             |
| ----------------------- | ---------------- |
| `Ctrl+w h` / `Ctrl+w j` | 左 / 下          |
| `Ctrl+w k` / `Ctrl+w l` | 上 / 右          |
| `Ctrl+w w`              | 循环切换窗格     |
| `Ctrl+w p`              | 切换到上一个窗格 |
| `Ctrl+w t`              | 切换到最顶部窗格 |
| `Ctrl+w b`              | 切换到最底部窗格 |

### 12.3 窗格调整

| 快捷键      | 说明         |
| ----------- | ------------ |
| `Ctrl+w =`  | 均分窗格大小 |
| `Ctrl+w >`  | 增加宽度     |
| `Ctrl+w <`  | 减少宽度     |
| `Ctrl+w +`  | 增加高度     |
| `Ctrl+w -`  | 减少高度     |
| `Ctrl+w _`  | 最大化高度   |
| `Ctrl+w \|` | 最大化宽度   |

### 12.4 标签页

| 快捷键                 | 说明                |
| ---------------------- | ------------------- |
| `:tabnew` / `:tabedit` | 新建标签页          |
| `:tabn` / `gt`         | 下一个标签页        |
| `:tabp` / `gT`         | 上一个标签页        |
| `:tabc`                | 关闭当前标签页      |
| `{n}gt`                | 跳转到第 n 个标签页 |

### 12.5 Zed 特有窗格快捷键

| 快捷键             | 说明                   |
| ------------------ | ---------------------- |
| `g /`              | 项目全局搜索           |
| `g <space>`        | 打开搜索结果面板       |
| `<ctrl-w> <space>` | 在分屏中打开搜索结果   |
| `<ctrl-w> g d`     | 在分屏中跳转到定义     |
| `<ctrl-w> g D`     | 在分屏中跳转到类型定义 |

---

## 13. Insert 模式命令

| 快捷键          | 说明                |
| --------------- | ------------------- |
| `Ctrl+x Ctrl+o` | 打开补全菜单        |
| `Ctrl+x Ctrl+c` | 请求 GitHub Copilot |
| `Ctrl+x Ctrl+a` | 打开 AI 助手        |
| `Ctrl+x Ctrl+l` | 打开代码操作        |
| `Ctrl+x Ctrl+z` | 隐藏建议            |
| `Ctrl+n`        | 下一个补全项        |
| `Ctrl+p`        | 上一个补全项        |
| `Ctrl+e`        | 取消补全            |
| `Ctrl+h`        | 删除前一个字符      |
| `Ctrl+w`        | 删除前一个单词      |
| `Ctrl+u`        | 删除到行首          |

---

## 14. Ex 命令（`:` 命令）

Zed 的 Vim 模式将 `:` 映射到命令面板，提供 Vim 风格的别名：

### 14.1 文件操作

| 命令               | 说明           |
| ------------------ | -------------- |
| `:w` / `:write`    | 保存           |
| `:wa` / `:wall`    | 保存所有文件   |
| `:q` / `:quit`     | 关闭           |
| `:wq`              | 保存并关闭     |
| `:wqa` / `:wqall`  | 保存全部并关闭 |
| `:qa` / `:qall`    | 关闭所有       |
| `:cq`              | 强制关闭       |
| `:bd` / `:bdelete` | 关闭 Buffer    |
| `:e {file}`        | 打开文件       |
| `:ls`              | 列出 Buffers   |

### 14.2 分屏与标签

| 命令                   | 说明           |
| ---------------------- | -------------- |
| `:vs` / `:vsplit`      | 垂直分屏       |
| `:sp` / `:split`       | 水平分屏       |
| `:new`                 | 水平分屏新文件 |
| `:vnew`                | 垂直分屏新文件 |
| `:tabedit` / `:tabnew` | 新标签页       |
| `:tabn` / `:tabnext`   | 下一个标签页   |
| `:tabp` / `:tabprev`   | 上一个标签页   |
| `:tabc` / `:tabclose`  | 关闭标签页     |

### 14.3 跳转与导航

| 命令        | 说明             |
| ----------- | ---------------- |
| `:{number}` | 跳转到指定行     |
| `:$`        | 跳转到文件末尾   |
| `:/foo`     | 搜索 foo（向下） |
| `:?foo`     | 搜索 foo（向上） |

### 14.4 诊断

| 命令             | 说明           |
| ---------------- | -------------- |
| `:cn` / `:cnext` | 下一个诊断     |
| `:cp` / `:cprev` | 上一个诊断     |
| `:cc`            | 显示当前诊断   |
| `:clist` / `:cl` | 列出所有诊断   |
| `:ln` / `:lnext` | 下一个位置     |
| `:lp` / `:lprev` | 上一个位置     |
| `:ll`            | 当前定位列表项 |

### 14.5 替换

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `:s/foo/bar/`     | 替换当前行（Zed 默认全局） |
| `:%s/foo/bar/`    | 全文替换                   |
| `:5,10s/foo/bar/` | 范围替换                   |

### 14.6 编辑命令

| 命令             | 说明                         |
| ---------------- | ---------------------------- |
| `:j` / `:join`   | 连接行                       |
| `:d` / `:delete` | 删除行                       |
| `:s` / `:sort`   | 排序（`:sort i` 忽略大小写） |
| `:y` / `:yank`   | 复制行                       |

### 14.7 设置选项

| 命令                        | 说明       |
| --------------------------- | ---------- |
| `:set wrap` / `:set nowrap` | 自动换行   |
| `:set nu` / `:set nonu`     | 行号       |
| `:set rnu` / `:set nornu`   | 相对行号   |
| `:set ic` / `:set noic`     | 忽略大小写 |

### 14.8 Zed 特有命令

| 命令                   | 说明               |
| ---------------------- | ------------------ |
| `:E` / `:Explore`      | 打开项目面板       |
| `:C` / `:Collab`       | 协作面板           |
| `:Ch` / `:Chat`        | 聊天面板           |
| `:A` / `:AI`           | AI 助手            |
| `:No` / `:Notif`       | 通知面板           |
| `:fe` / `:feedback`    | 反馈               |
| `:te` / `:term`        | 终端               |
| `:Ext` / `:Extensions` | 扩展面板           |
| `:G` / `:Git`          | Git 面板           |
| `:diffs`               | 切换 Hunk Diff     |
| `:cpp`                 | 复制路径           |
| `:crp`                 | 复制相对路径       |
| `:reveal`              | 在文件管理器中显示 |
| `:zlog`                | 打开 Zed 日志      |
| `:clank`               | 取消语言服务器任务 |

### 14.9 自定义命令别名

```json
{
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q",
        "Wa": "wa"
    }
}
```

---

## 15. 内置插件功能

Zed 内置了常用 Vim 插件功能的等价实现：

### 15.1 Surround（vim-surround）

| 快捷键             | 说明                                     |
| ------------------ | ---------------------------------------- |
| `ys{motion}{char}` | 添加包围（如 `ysiw"` 用双引号包围单词）  |
| `cs{old}{new}`     | 修改包围（如 `cs"'` 将双引号改为单引号） |
| `ds{char}`         | 删除包围（如 `ds[` 删除方括号）          |
| `S{char}` (visual) | 用字符包围选中文本                       |

> Visual 模式下需要自定义绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
>     }
> }
> ```

### 15.2 Comment（vim-commentary）

| 快捷键        | 说明                            |
| ------------- | ------------------------------- |
| `gcc`         | 切换当前行注释                  |
| `gc` (visual) | 切换选中行注释                  |
| `gc{motion}`  | 对移动范围切换注释（如 `gc3j`） |

### 15.3 Exchange（vim-exchange）

| 快捷键        | 说明             |
| ------------- | ---------------- |
| `cx{motion}`  | 标记要交换的文本 |
| `cx` (第二次) | 执行交换         |

> Visual 模式下需手动绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-x": "vim::Exchange"
>     }
> }
> ```

### 15.4 ReplaceWithRegister

| 快捷键       | 说明                                 |
| ------------ | ------------------------------------ |
| `gR{motion}` | 用寄存器内容替换文本（不覆盖寄存器） |

### 15.5 netrw 风格项目面板

在项目面板中使用 `hjkl` 导航：

| 快捷键                | 说明           |
| --------------------- | -------------- |
| `h` / `j` / `k` / `l` | 导航           |
| `o`                   | 打开文件       |
| `t`                   | 在新标签页打开 |

### 15.6 Sneak 导航

需自定义绑定：

```json
{
    "context": "vim_mode == normal || vim_mode == visual",
    "bindings": {
        "s": "vim::PushSneak",
        "shift-s": "vim::PushSneakBackward"
    }
}
```

### 15.7 AnyQuotes / AnyBrackets 文本对象

```json
{
    "context": "vim_operator == a || vim_operator == i || vim_operator == cs",
    "bindings": {
        "q": "vim::AnyQuotes",
        "b": "vim::AnyBrackets",
        "Q": "vim::MiniQuotes",
        "B": "vim::MiniBrackets"
    }
}
```

---

## 16. 标记与寄存器

### 16.1 标记（Marks）

| 快捷键       | 说明                       |
| ------------ | -------------------------- |
| `m{a-z}`     | 设置缓冲区本地标记         |
| `'{a-z}`     | 跳转到标记所在行           |
| `` `{a-z} `` | 跳转到标记精确位置         |
| `''`         | 跳转到上次位置             |
| `'<` / `'>`  | 上次可视选择的开始/结束    |
| `'[` / `']`  | 上次修改/复制的开始/结束   |
| `'{` / `'}`  | 段落跳转标记               |
| `'.`         | 上次修改位置               |
| `'^`         | 上次 Insert 模式退出的位置 |

### 16.2 寄存器（Registers）

| 寄存器      | 说明                                  |
| ----------- | ------------------------------------- |
| `""`        | 无名寄存器（默认）                    |
| `"0`        | 最近复制的文本                        |
| `"1` - `"9` | 最近删除的文本（从新到旧）            |
| `"a` - `"z` | 命名寄存器                            |
| `"+`        | 系统剪贴板                            |
| `"*`        | 选择剪贴板（Linux/Windows 等同 `"+`） |
| `"_`        | 黑洞寄存器（删除不保存）              |
| `"%`        | 当前文件名                            |
| `".`        | 上次插入的文本                        |

使用方式：

- `"ayw` — 将单词复制到寄存器 a
- `"ap` — 粘贴寄存器 a 的内容
- `"+y` — 复制到系统剪贴板
- `"_dd` — 删除行但不保存到寄存器

### 16.3 剪贴板配置

```json
{
    "vim": {
        "use_system_clipboard": "always"
        // "always"  — 始终使用系统剪贴板
        // "never"   — 永远不使用
        // "on_yank" — 仅复制时使用
    }
}
```

---

## 17. 宏

Zed 使用自己的录制系统实现 Vim 宏功能：

| 快捷键       | 说明                    |
| ------------ | ----------------------- |
| `q{a-z}`     | 开始录制到寄存器 {a-z}  |
| `q` (录制中) | 停止录制                |
| `Q`          | 重复上次录制的宏        |
| `@{a-z}`     | 执行寄存器 {a-z} 中的宏 |
| `@@`         | 重复上次执行的宏        |
| `{n}@{a-z}`  | 执行宏 n 次             |

> **注意：** Zed 的宏系统与原生 Vim 不同，宏存储与复制寄存器是独立的命名空间。

---

## 18. 配置

### 18.1 Vim 专属配置

在 `settings.json` 的 `"vim"` 键下配置：

```json
{
    "vim": {
        // 系统剪贴板策略："always" | "never" | "on_yank"
        "use_system_clipboard": "always",

        // 多行搜索
        "use_multiline_find": true,

        // 智能大小写搜索（全小写时忽略大小写，含大写时精确匹配）
        "use_smartcase_find": true,

        // 切换相对行号
        "toggle_relative_line_numbers": true,

        // 自定义有向图（digraph），如 Ctrl+k f z → 🧟
        "custom_digraphs": { "fz": "🧟♀️" },

        // 启动时的默认模式
        "default_mode": "normal",

        // 复制后高亮持续时间（毫秒），0 禁用
        "highlight_on_yank_duration": 200,

        // 使用正则搜索
        "use_regex_search": true,

        // 替换默认全局（相当于始终 /g）
        "gdefault": false
    }
}
```

### 18.2 推荐的伴随设置

```json
{
    // 禁用光标闪烁（像原生 Vim）
    "cursor_blink": false,

    // 相对行号
    "relative_line_numbers": true,

    // 滚动条
    "scrollbar": { "show": "never" },

    // 不允许滚动超过最后一行
    "scroll_beyond_last_line": "off",

    // 垂直滚动边距
    "vertical_scroll_margin": 0,

    // 行号
    "gutter": { "line_numbers": false },

    // 命令别名
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q"
    }
}
```

---

## 19. 自定义快捷键

### 19.1 Vim 上下文

自定义快捷键需要理解 Zed 的上下文系统：

| 上下文                 | 含义                                          |
| ---------------------- | --------------------------------------------- |
| `VimControl`           | `normal` \|\| `visual` \|\| `operator` 的别名 |
| `vim_mode == normal`   | Normal 模式                                   |
| `vim_mode == visual`   | Visual 模式                                   |
| `vim_mode == insert`   | Insert 模式                                   |
| `vim_mode == replace`  | Replace 模式                                  |
| `vim_mode == waiting`  | 等待（如 `f`/`t` 后等待字符）                 |
| `vim_mode == operator` | 操作符挂起（如 `c`/`d` 后等待移动）           |
| `vim_operator == d`    | 当前操作符为 `d`                              |
| `vim_operator == c`    | 当前操作符为 `c`                              |
| `vim_operator == y`    | 当前操作符为 `y`                              |

### 19.2 示例

```json
[
    // Normal 模式下的自定义
    {
        "context": "vim_mode == normal && !menu",
        "bindings": {
            "shift-y": ["workspace::SendKeystrokes", "y $"],
            "space w": "workspace::Save"
        }
    },

    // Visual 模式下的自定义
    {
        "context": "vim_mode == visual",
        "bindings": {
            "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
        }
    },

    // Insert 模式退出
    {
        "context": "vim_mode == insert",
        "bindings": {
            "j k": "vim::NormalBefore",
            "j j": "vim::NormalBefore"
        }
    },

    // 窗格中
    {
        "context": "EmptyPane || SharedScreen",
        "bindings": {
            "space f": "file_finder::Toggle"
        }
    }
]
```

### 19.3 恢复系统剪贴板快捷键（Linux/Windows）

```json
{
    "context": "Editor && !menu",
    "bindings": {
        "ctrl-c": "editor::Copy",
        "ctrl-x": "editor::Cut",
        "ctrl-v": "editor::Paste",
        "ctrl-y": "editor::Undo",
        "ctrl-f": "buffer_search::Deploy",
        "ctrl-o": "workspace::Open",
        "ctrl-a": "editor::SelectAll"
    }
}
```

---

## 20. 正则表达式差异

Zed 使用不同的正则引擎（Rust `regex` crate），与 Vim 的语法有差异：

| 特性         | Vim       | Zed                                |
| ------------ | --------- | ---------------------------------- |
| 捕获组       | `\(` `\)` | `(` `)`                            |
| 字面括号     | `(` `)`   | `\(` `\)`                          |
| 完整匹配引用 | `\0`      | `$0`                               |
| 编号组引用   | `\1`      | `$1`                               |
| 全局标志     | 需 `/g`   | 默认全局（可设 `gdefault: false`） |
| 大小写不敏感 | `/i` 标志 | `(?i)` 或 `cmd-option-c`           |

> `:s` 命令面板会自动转换 Vim 风格的语法。

---

## 21. 补充：文档未提及的 Vim 常用命令

以下命令在 Zed 官方文档中未明确列出，但在日常 Vim 使用中非常重要。**部分在 Zed 中可直接使用，部分可能需要验证。**

### 21.1 Normal 模式进阶命令

| 快捷键             | 说明                              | Zed 支持      |
| ------------------ | --------------------------------- | ------------- |
| `g_`               | 跳转到行尾最后一个非空白字符      | ⚠️ 待验证     |
| `g^`               | 跳转到行首第一个非空白字符        | ⚠️ 待验证     |
| `gm`               | 跳转到行中间                      | ⚠️ 待验证     |
| `g0`               | 跳转到行首（即使是换行行）        | ⚠️ 待验证     |
| `g$`               | 跳转到行尾（即使是换行行）        | ⚠️ 待验证     |
| `g~` / `gu` / `gU` | 切换/小写/大写 + 移动             | ✅ 基本支持   |
| `g?`               | Rot13 编码                        | ❌ 通常不支持 |
| `ga`               | 显示光标下字符的 ASCII/Unicode 值 | ⚠️ 待验证     |
| `g Ctrl+g`         | 显示光标位置统计                  | ⚠️ 待验证     |
| `Ctrl+g`           | 显示文件信息和当前行号            | ⚠️ 待验证     |
| `del`              | 删除光标后字符（同 `x`）          | ⚠️ 待验证     |
| `D`                | 删除到行尾                        | ✅ 支持       |
| `Y`                | 复制整行（同 `yy`）               | ✅ 支持       |
| `C`                | 修改到行尾                        | ✅ 支持       |
| `s`                | 删除字符并进入 Insert             | ✅ 支持       |
| `S` / `cc`         | 删除整行并进入 Insert             | ✅ 支持       |
| `~`                | 切换字符大小写                    | ✅ 支持       |

### 21.2 窗口/标签页进阶

| 快捷键                   | 说明                   | Zed 支持           |
| ------------------------ | ---------------------- | ------------------ |
| `Ctrl+w T`               | 将当前窗格移到新标签页 | ⚠️ 待验证          |
| `Ctrl+w x`               | 交换窗格位置           | ⚠️ 待验证          |
| `Ctrl+w r`               | 旋转窗格               | ⚠️ 待验证          |
| `Ctrl+w H`               | 将窗格移到左侧（全高） | ⚠️ 待验证          |
| `Ctrl+w J`               | 将窗格移到底部（全宽） | ⚠️ 待验证          |
| `Ctrl+w K`               | 将窗格移到顶部（全宽） | ⚠️ 待验证          |
| `Ctrl+w L`               | 将窗格移到右侧（全高） | ⚠️ 待验证          |
| `:on` / `:only`          | 仅保留当前窗格         | ✅ 通过 `Ctrl+w o` |
| `:tabm {n}`              | 移动标签页到位置 n     | ⚠️ 待验证          |
| `:tabfirst` / `:tablast` | 第一个/最后一个标签页  | ⚠️ 待验证          |

### 21.3 折叠命令

Zed 使用 Tree-sitter 的代码折叠，以下命令**可能需要验证**：

| 快捷键       | 说明                     |
| ------------ | ------------------------ |
| `za`         | 切换当前折叠             |
| `zA`         | 递归切换折叠             |
| `zc`         | 关闭折叠                 |
| `zC`         | 递归关闭折叠             |
| `zo`         | 打开折叠                 |
| `zO`         | 递归打开折叠             |
| `zm`         | 增加折叠级别（关闭更多） |
| `zM`         | 关闭所有折叠             |
| `zr`         | 减少折叠级别（打开更多） |
| `zR`         | 打开所有折叠             |
| `zf{motion}` | 手动创建折叠             |
| `zd`         | 删除折叠                 |
| `zE`         | 删除所有折叠             |

### 21.4 插入模式进阶

| 快捷键              | 说明                                | Zed 支持           |
| ------------------- | ----------------------------------- | ------------------ |
| `Ctrl+v {code}`     | 插入任意字符（如 `Ctrl+v 065` → A） | ⚠️ 待验证          |
| `Ctrl+k {digraph}`  | 插入有向图（如 `Ctrl+k a:` → ä）    | ✅ 自定义 digraphs |
| `Ctrl+a`            | 插入上次 Insert 模式的文本          | ⚠️ 待验证          |
| `Ctrl+@`            | 插入上次 Insert 文本并退出          | ⚠️ 待验证          |
| `Ctrl+d`            | 减少缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+t`            | 增加缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+y`            | 插入上一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+e`            | 插入下一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+j` / `Ctrl+m` | 换行（同 Enter）                    | ✅ 支持            |
| `Ctrl+r {reg}`      | 插入寄存器内容                      | ⚠️ 待验证          |

### 21.5 有用但常被忽视的命令

| 快捷键      | 说明                      | Zed 支持        |
| ----------- | ------------------------- | --------------- |
| `g&`        | 在所有行上重复上次替换    | ⚠️ 待验证       |
| `&`         | 在当前行重复上次替换      | ⚠️ 待验证       |
| `@:`        | 重复上次 Ex 命令          | ⚠️ 待验证       |
| `q:`        | 打开命令历史窗口          | ❌ Zed 无此概念 |
| `q/` / `q?` | 打开搜索历史窗口          | ❌ Zed 无此概念 |
| `K`         | 查看光标下关键字的 man 页 | ❌ 被重映射     |
| `zi`        | 切换折叠启用/禁用         | ⚠️ 待验证       |
| `zv`        | 打开足够折叠以显示光标    | ⚠️ 待验证       |
| `zx` / `zX` | 更新折叠/重新应用折叠     | ⚠️ 待验证       |
| `:retab`    | 转换 tab 为空格（或反之） | ⚠️ 待验证       |

### 21.6 快速修复窗口操作

| 快捷键               | 说明                 | Zed 支持        |
| -------------------- | -------------------- | --------------- |
| `:copen`             | 打开快速修复窗口     | ❌ 诊断面板替代 |
| `:cclose`            | 关闭快速修复窗口     | ❌              |
| `:cfirst` / `:clast` | 第一个/最后一个错误  | ⚠️ 待验证       |
| `:cdo {cmd}`         | 对每个错误项执行命令 | ❌              |

### 21.7 Buffer 管理进阶

| 快捷键               | 说明                   | Zed 支持  |
| -------------------- | ---------------------- | --------- |
| `:bufdo {cmd}`       | 对所有 Buffer 执行命令 | ❌        |
| `:b {num}`           | 切换到指定编号 Buffer  | ⚠️ 待验证 |
| `:bnext` / `:bprev`  | 同 `:bn` / `:bp`       | ✅ 支持   |
| `:bfirst` / `:blast` | 第一个/最后一个 Buffer | ⚠️ 待验证 |
| `ctrl+^` / `ctrl+6`  | 切换交替文件           | ⚠️ 待验证 |

### 21.8 实用技巧组合

这些都是 Vim 中常用的高效编辑组合：

| 组合        | 效果                             |
| ----------- | -------------------------------- |
| `ci"`       | 修改双引号内容                   |
| `ca"`       | 修改双引号包含引号本身           |
| `yi(`       | 复制括号内容                     |
| `da]`       | 删除方括号及内容                 |
| `vit` + `c` | 修改 HTML 标签内容               |
| `vat` + `d` | 删除整个 HTML 标签               |
| `gUiw`      | 将单词转为大写                   |
| `guiw`      | 将单词转为小写                   |
| `g~iw`      | 切换单词大小写                   |
| `guG`       | 从当前位置到文件末转为小写       |
| `gUgg`      | 从当前位置到文件头转为大写       |
| `=%`        | 缩进整个匹配块（光标在括号上时） |
| `>i{`       | 缩进花括号内容                   |
| `<a{`       | 减少花括号块缩进                 |

---

## 参考资源

- [Zed 官方 Vim 文档](https://zed.dev/docs/vim)
- [Zed 官方 Key Bindings 文档](https://zed.dev/docs/key-bindings)
- [Zed Decoded: Vim (博客)](https://zed.dev/blog/zed-decoded-vim)
- [Zed Vim Roadmap 2025](https://zed.dev/blog/vim-2025)
- [Zed GitHub 仓库](https://github.com/zed-industries/zed)
- [Zed Vim 模式源码文档](https://git.iko.soy/zed-industries/zed/src/commit/174e12568669e70abb15934597a3f5df335eaf08/docs/src/vim.md)

> **注意：** 标记为 ⚠️ 的条目需要在实际使用中验证。Zed 的 Vim 模式在持续更新中，如有缺失功能，可在 [Zed GitHub Issues](https://github.com/zed-industries/zed/issues) 中查找或提交。
>
> 本文档基于 2025 年 7 月的 Zed Vim 模式状态整理。# Zed 中 Vim 模式完整使用文档

> 基于 [Zed 官方 Vim 文档](https://zed.dev/docs/vim) 整理，并补充文档中未提及的 Vim 常用命令。

---

## 目录

1. [概述](#1-概述)
2. [启用 Vim 模式](#2-启用-vim-模式)
3. [模式切换](#3-模式切换)
4. [光标移动](#4-光标移动)
5. [编辑操作](#5-编辑操作)
6. [可视模式操作](#6-可视模式操作)
7. [搜索与替换](#7-搜索与替换)
8. [LSP / 代码导航](#8-lsp--代码导航)
9. [Git 集成](#9-git-集成)
10. [Tree-sitter 导航与文本对象](#10-tree-sitter-导航与文本对象)
11. [多光标操作](#11-多光标操作)
12. [窗格与窗口管理](#12-窗格与窗口管理)
13. [Insert 模式命令](#13-insert-模式命令)
14. [Ex 命令](#14-ex-命令)
15. [内置插件功能](#15-内置插件功能)
16. [标记与寄存器](#16-标记与寄存器)
17. [宏](#17-宏)
18. [配置](#18-配置)
19. [自定义快捷键](#19-自定义快捷键)
20. [正则表达式差异](#20-正则表达式差异)
21. [补充：文档未提及的 Vim 常用命令](#21-补充文档未提及的-vim-常用命令)

---

## 1. 概述

Zed 内置了 Vim 模拟层（Vim mode），目标是：

- **"做你期望的事"**：在合理的地方精确复制 Vim 的行为
- **利用 Zed 的现代特性**：Tree-sitter 语法解析、LSP、多光标、CRDT 协作等
- **不是 100% Vim 兼容**，但追求 **100% Vim 熟悉感**

### 与原生 Vim 的核心差异

| 方面       | Vim          | Zed                     |
| ---------- | ------------ | ----------------------- |
| 光标模型   | 定位在字符上 | 定位在字符之间          |
| 视觉块模式 | 原生块选择   | 通过多光标模拟          |
| 宏         | `q` / `@`    | 使用 Zed 的录制系统     |
| 搜索替换   | Vim 正则     | Zed 正则引擎            |
| 词语定义   | 固定规则     | 语言感知（Tree-sitter） |

---

## 2. 启用 Vim 模式

### 方法一：欢迎屏幕

首次启动 Zed 时，勾选 "Vim Mode" 复选框。

### 方法二：命令面板

`Ctrl+Shift+P` → 输入 `toggle vim mode` → 回车。

### 方法三：配置文件

在 `settings.json` 中添加：

```json
{
    "vim_mode": true
}
```

### 按发布渠道启用

```json
{
    "vim_mode": false,
    "preview": {
        "vim_mode": true
    },
    "nightly": {
        "vim_mode": true
    }
}
```

---

## 3. 模式切换

| 快捷键     | 模式         | 说明                       |
| ---------- | ------------ | -------------------------- |
| `Esc`      | Normal       | 从任何模式返回 Normal 模式 |
| `Ctrl+c`   | Normal       | 替代 Esc                   |
| `i`        | Insert       | 在当前字符前插入           |
| `a`        | Insert       | 在当前字符后插入           |
| `I`        | Insert       | 在行首第一个非空字符前插入 |
| `A`        | Insert       | 在行尾插入                 |
| `o`        | Insert       | 在下方新建一行             |
| `O`        | Insert       | 在上方新建一行             |
| `v`        | Visual       | 字符选择模式               |
| `V`        | Visual (行)  | 整行选择模式               |
| `Ctrl+v`   | Visual Block | 块选择模式（多光标模拟）   |
| `R`        | Replace      | 替换模式（覆盖输入）       |
| `r`        | Normal       | 替换单个字符               |
| `s`        | Insert       | 删除当前字符并进入 Insert  |
| `S` / `cc` | Insert       | 删除整行并进入 Insert      |
| `C`        | Insert       | 删除到行尾并进入 Insert    |

### Insert 模式下返回 Normal

除 `Esc` 和 `Ctrl+c` 外，可自定义快捷键：

```json
{
    "context": "vim_mode == insert",
    "bindings": {
        "j k": "vim::NormalBefore",
        "j j": "vim::NormalBefore"
    }
}
```

---

## 4. 光标移动

### 4.1 基本移动

| 快捷键                | 说明                    |
| --------------------- | ----------------------- |
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右       |
| `w`                   | 下一个单词开头          |
| `W`                   | 下一个 WORD（空白分隔） |
| `b`                   | 上一个单词开头          |
| `B`                   | 上一个 WORD             |
| `e`                   | 下一个单词结尾          |
| `E`                   | 下一个 WORD 结尾        |
| `ge`                  | 上一个单词结尾          |
| `gE`                  | 上一个 WORD 结尾        |

### 4.2 行内移动

| 快捷键    | 说明                              |
| --------- | --------------------------------- |
| `0`       | 行首（含空白）                    |
| `^`       | 行首第一个非空白字符              |
| `$`       | 行尾                              |
| `f{char}` | 向右查找字符（光标停在字符上）    |
| `F{char}` | 向左查找字符                      |
| `t{char}` | 向右查找字符（光标停在字符前）    |
| `T{char}` | 向左查找字符（光标停在字符后）    |
| `;`       | 重复上次 `f` / `t` 查找（同方向） |
| `,`       | 重复上次 `f` / `t` 查找（反方向） |

### 4.3 页面滚动

| 快捷键   | 说明                 |
| -------- | -------------------- |
| `Ctrl+d` | 向下翻半页           |
| `Ctrl+u` | 向上翻半页           |
| `Ctrl+f` | 向下翻一页           |
| `Ctrl+b` | 向上翻一页           |
| `zt`     | 将当前行置于屏幕顶部 |
| `zz`     | 将当前行置于屏幕中央 |
| `zb`     | 将当前行置于屏幕底部 |

### 4.4 文件级移动

| 快捷键           | 说明                                             |
| ---------------- | ------------------------------------------------ |
| `gg`             | 跳到文件第一行                                   |
| `G`              | 跳到文件最后一行                                 |
| `{n}gg` / `{n}G` | 跳到第 n 行                                      |
| `H`              | 屏幕顶部                                         |
| `M`              | 屏幕中间                                         |
| `L`              | 屏幕底部                                         |
| `{`              | 上一段（空行分隔）                               |
| `}`              | 下一段                                           |
| `%`              | 跳转到匹配的括号（语言感知，Rust 中可匹配 `\|`） |

### 4.5 跳转列表

| 快捷键   | 说明             |
| -------- | ---------------- |
| `Ctrl+o` | 跳到上一个位置   |
| `Ctrl+i` | 跳到下一个位置   |
| `Ctrl+t` | 从标签栈弹出     |
| `g;`     | 跳到上次修改位置 |
| `g,`     | 跳到下次修改位置 |

### 4.6 自定义子词移动（CamelCase / snake_case）

```json
{
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
        "w": "vim::NextSubwordStart",
        "b": "vim::PreviousSubwordStart",
        "e": "vim::NextSubwordEnd",
        "g e": "vim::PreviousSubwordEnd"
    }
}
```

---

## 5. 编辑操作

### 5.1 删除

| 快捷键     | 说明                       |
| ---------- | -------------------------- |
| `x`        | 删除当前字符               |
| `X`        | 删除前一个字符（退格）     |
| `dd`       | 删除当前行                 |
| `D`        | 删除到行尾                 |
| `dw`       | 删除到单词结尾             |
| `db`       | 删除到单词开头             |
| `daw`      | 删除整个单词（含周围空格） |
| `diw`      | 删除单词内部               |
| `d$` / `D` | 删除到行尾                 |
| `d0`       | 删除到行首                 |
| `dG`       | 删除到文件末尾             |
| `dgg`      | 删除到文件开头             |
| `dt{char}` | 删除直到指定字符           |
| `df{char}` | 删除到指定字符（含）       |

### 5.2 复制（Yank）

| 快捷键     | 说明           |
| ---------- | -------------- |
| `yy` / `Y` | 复制当前行     |
| `yw`       | 复制单词       |
| `yaw`      | 复制整个单词   |
| `y$`       | 复制到行尾     |
| `y0`       | 复制到行首     |
| `yG`       | 复制到文件末尾 |
| `ygg`      | 复制到文件开头 |

### 5.3 粘贴

| 快捷键 | 说明                       |
| ------ | -------------------------- |
| `p`    | 在光标后粘贴               |
| `P`    | 在光标前粘贴               |
| `gp`   | 粘贴并将光标移到粘贴内容后 |
| `gP`   | 粘贴并将光标移到粘贴内容前 |

### 5.4 修改（Change）

| 快捷键                | 说明                     |
| --------------------- | ------------------------ |
| `cw`                  | 修改到单词结尾           |
| `ciw`                 | 修改整个单词             |
| `caw`                 | 修改整个单词（含空格）   |
| `cc` / `S`            | 修改整行                 |
| `C`                   | 修改到行尾               |
| `ci(` / `ci[` / `ci{` | 修改括号内内容           |
| `ci"` / `ci'`         | 修改引号内内容           |
| `cit`                 | 修改 HTML/XML 标签内内容 |

### 5.5 其他编辑

| 快捷键     | 说明                     |
| ---------- | ------------------------ |
| `u`        | 撤销                     |
| `Ctrl+r`   | 重做                     |
| `.`        | 重复上次修改             |
| `>>`       | 增加缩进                 |
| `<<`       | 减少缩进                 |
| `>` + 移动 | 缩进指定范围             |
| `<` + 移动 | 减少缩进指定范围         |
| `~`        | 切换大小写               |
| `J`        | 将下一行连接到当前行     |
| `gJ`       | 连接行（不加空格）       |
| `r{char}`  | 替换当前字符             |
| `xp`       | 交换当前字符与下一个字符 |
| `ddp`      | 交换当前行与下一行       |

### 5.6 数字前缀（Count）

几乎所有命令都支持数字前缀：

| 示例                  | 效果               |
| --------------------- | ------------------ |
| `5j`                  | 向下移动 5 行      |
| `3dd`                 | 删除 3 行          |
| `4x`                  | 删除 4 个字符      |
| `2dw`                 | 删除 2 个单词      |
| `5i` + `text` + `Esc` | 插入 `text` 5 次   |
| `10.`                 | 重复上次修改 10 次 |

> **注意：** `5dj` 和 `d5j` 的区别 — `5dj` 表示"删除 5 次 = 5 行"，而 `d5j` 表示"删除操作应用于向下 5 行"。效果相同但语义不同。

---

## 6. 可视模式操作

| 快捷键                  | 说明                   |
| ----------------------- | ---------------------- |
| `v`                     | 进入字符可视模式       |
| `V`                     | 进入行可视模式         |
| `Ctrl+v`                | 进入块可视模式         |
| `gv`                    | 重新选择上次的可视区域 |
| `o` (visual 模式)       | 跳转到选择区域的另一端 |
| `O` (visual block 模式) | 跳转到对角线另一端     |
| `u` (visual 模式)       | 将选中文本转为小写     |
| `U` (visual 模式)       | 将选中文本转为大写     |
| `>`                     | 缩进选中文本           |
| `<`                     | 减少缩进选中文本       |
| `I` (block 模式)        | 在块前插入（多行同时） |
| `A` (block 模式)        | 在块后插入（多行同时） |
| `c` (visual 模式)       | 修改选中文本           |
| `d` / `x` (visual 模式) | 删除选中文本           |
| `y` (visual 模式)       | 复制选中文本           |

---

## 7. 搜索与替换

### 7.1 搜索

| 快捷键 | 说明                               |
| ------ | ---------------------------------- |
| `/`    | 向下搜索                           |
| `?`    | 向上搜索                           |
| `n`    | 下一个匹配项                       |
| `N`    | 上一个匹配项                       |
| `*`    | 搜索光标下的单词（向下）           |
| `#`    | 搜索光标下的单词（向上）           |
| `g*`   | 搜索光标下的单词（部分匹配，向下） |
| `g#`   | 搜索光标下的单词（部分匹配，向上） |
| `gn`   | 选中下一个匹配项                   |
| `gN`   | 选中上一个匹配项                   |
| `cgn`  | 修改下一个匹配项，然后 `.` 重复    |

> **搜索配置：**
>
> ```json
> {
>     "vim": {
>         "use_smartcase_find": true, // 智能大小写
>         "use_regex_search": true // 使用正则搜索
>     }
> }
> ```

### 7.2 替换

| 命令               | 说明                               |
| ------------------ | ---------------------------------- |
| `:s/old/new/`      | 替换当前行第一个匹配               |
| `:s/old/new/g`     | 替换当前行所有匹配                 |
| `:%s/old/new/`     | 替换全文（`/g` 在 Zed 中默认隐含） |
| `:%s/old/new/gc`   | 替换全文（逐个确认）               |
| `:5,10s/old/new/`  | 替换第 5-10 行                     |
| `:'<,'>s/old/new/` | 替换选中的范围                     |

> **Zed 正则差异** — 详见[第 20 节](#20-正则表达式差异)。

---

## 8. LSP / 代码导航

Zed 将 Vim 快捷键与 LSP 功能深度集成：

| 快捷键        | 说明                    |
| ------------- | ----------------------- |
| `g d`         | 跳转到定义              |
| `g D`         | 跳转到声明              |
| `g y`         | 跳转到类型定义          |
| `g I`         | 跳转到实现              |
| `c d`         | 重命名符号              |
| `g A`         | 查找所有引用            |
| `g s`         | 文件内查找符号          |
| `g S`         | 项目内查找符号          |
| `g ]` / `] d` | 下一个诊断（错误/警告） |
| `g [` / `[ d` | 上一个诊断              |
| `g h`         | 显示悬停信息            |
| `g .`         | 打开代码操作菜单        |
| `g r`         | 重命名（与 `c d` 类似） |

---

## 9. Git 集成

| 快捷键 | 说明                 |
| ------ | -------------------- |
| `] c`  | 下一个 Git 更改      |
| `[ c`  | 上一个 Git 更改      |
| `d o`  | 展开 diff hunk       |
| `d O`  | 切换暂存状态         |
| `d u`  | 暂存并移到下一个     |
| `d U`  | 取消暂存并移到下一个 |
| `d p`  | 还原更改             |

---

## 10. Tree-sitter 导航与文本对象

### 10.1 语法节点导航

| 快捷键        | 说明                         |
| ------------- | ---------------------------- |
| `] m` / `[ m` | 下一个/上一个方法            |
| `] M` / `[ M` | 下一个/上一个方法结尾        |
| `] ]` / `[ [` | 下一个/上一个段落（Section） |
| `] /` / `[ /` | 下一个/上一个注释            |
| `] *` / `[ *` | 下一个/上一个注释（同上）    |
| `[ x`         | 选择更大的语法节点           |
| `] x`         | 选择更小的语法节点           |

### 10.2 文本对象

| 快捷键                | 说明                |
| --------------------- | ------------------- |
| `a c` / `i c`         | 整个类 / 类内部     |
| `a f` / `i f`         | 整个函数 / 函数内部 |
| `g c`                 | 注释                |
| `i a` / `a a`         | 参数内部 / 整个参数 |
| `a t` / `i t`         | HTML 标签           |
| `a I` / `a i` / `i i` | 缩进级别            |
| `i w` / `a w`         | 单词                |
| `i W` / `a W`         | WORD                |
| `i s` / `a s`         | 句子                |
| `i p` / `a p`         | 段落                |
| `i (` / `a (`         | 圆括号内            |
| `i [` / `a [`         | 方括号内            |
| `i {` / `a {`         | 花括号内            |
| `i "` / `a "`         | 双引号内            |
| `i '` / `a '`         | 单引号内            |
| `i \`` / `a \``       | 反引号内            |
| `i t` / `a t`         | HTML/XML 标签内     |

### 10.3 缩进文本对象

| 快捷键      | 说明                      |
| ----------- | ------------------------- |
| `[-`        | 上一个缩进更少的行        |
| `]-`        | 下一个缩进更少的行        |
| `[+` / `]+` | 上一个/下一个缩进更多的行 |
| `[=` / `]=` | 上一个/下一个相同缩进的行 |

---

## 11. 多光标操作

Zed 在 Vim 模式下保留了多光标功能：

| 快捷键 | 说明                         |
| ------ | ---------------------------- |
| `g l`  | 添加光标到下一个相同单词     |
| `g L`  | 添加光标到上一个相同单词     |
| `g a`  | 选中当前单词的所有出现       |
| `g A`  | 在选中区域的每行末尾添加光标 |
| `g I`  | 在选中区域的每行开头添加光标 |
| `g >`  | 跳过并添加下一个             |
| `g <`  | 跳过并添加上一个             |
| `Esc`  | 退出多光标模式，回到 Normal  |

---

## 12. 窗格与窗口管理

### 12.1 分屏操作

| 快捷键            | 说明                       |
| ----------------- | -------------------------- |
| `:vs` / `:vsplit` | 垂直分屏                   |
| `:sp` / `:split`  | 水平分屏                   |
| `:new`            | 水平分屏打开新文件         |
| `:vnew`           | 垂直分屏打开新文件         |
| `Ctrl+w v`        | 垂直分屏                   |
| `Ctrl+w s`        | 水平分屏                   |
| `Ctrl+w q`        | 关闭当前窗格               |
| `Ctrl+w c`        | 关闭当前窗格               |
| `Ctrl+w o`        | 仅保留当前窗格（关闭其他） |

### 12.2 窗格间导航

| 快捷键                  | 说明             |
| ----------------------- | ---------------- |
| `Ctrl+w h` / `Ctrl+w j` | 左 / 下          |
| `Ctrl+w k` / `Ctrl+w l` | 上 / 右          |
| `Ctrl+w w`              | 循环切换窗格     |
| `Ctrl+w p`              | 切换到上一个窗格 |
| `Ctrl+w t`              | 切换到最顶部窗格 |
| `Ctrl+w b`              | 切换到最底部窗格 |

### 12.3 窗格调整

| 快捷键      | 说明         |
| ----------- | ------------ |
| `Ctrl+w =`  | 均分窗格大小 |
| `Ctrl+w >`  | 增加宽度     |
| `Ctrl+w <`  | 减少宽度     |
| `Ctrl+w +`  | 增加高度     |
| `Ctrl+w -`  | 减少高度     |
| `Ctrl+w _`  | 最大化高度   |
| `Ctrl+w \|` | 最大化宽度   |

### 12.4 标签页

| 快捷键                 | 说明                |
| ---------------------- | ------------------- |
| `:tabnew` / `:tabedit` | 新建标签页          |
| `:tabn` / `gt`         | 下一个标签页        |
| `:tabp` / `gT`         | 上一个标签页        |
| `:tabc`                | 关闭当前标签页      |
| `{n}gt`                | 跳转到第 n 个标签页 |

### 12.5 Zed 特有窗格快捷键

| 快捷键             | 说明                   |
| ------------------ | ---------------------- |
| `g /`              | 项目全局搜索           |
| `g <space>`        | 打开搜索结果面板       |
| `<ctrl-w> <space>` | 在分屏中打开搜索结果   |
| `<ctrl-w> g d`     | 在分屏中跳转到定义     |
| `<ctrl-w> g D`     | 在分屏中跳转到类型定义 |

---

## 13. Insert 模式命令

| 快捷键          | 说明                |
| --------------- | ------------------- |
| `Ctrl+x Ctrl+o` | 打开补全菜单        |
| `Ctrl+x Ctrl+c` | 请求 GitHub Copilot |
| `Ctrl+x Ctrl+a` | 打开 AI 助手        |
| `Ctrl+x Ctrl+l` | 打开代码操作        |
| `Ctrl+x Ctrl+z` | 隐藏建议            |
| `Ctrl+n`        | 下一个补全项        |
| `Ctrl+p`        | 上一个补全项        |
| `Ctrl+e`        | 取消补全            |
| `Ctrl+h`        | 删除前一个字符      |
| `Ctrl+w`        | 删除前一个单词      |
| `Ctrl+u`        | 删除到行首          |

---

## 14. Ex 命令（`:` 命令）

Zed 的 Vim 模式将 `:` 映射到命令面板，提供 Vim 风格的别名：

### 14.1 文件操作

| 命令               | 说明           |
| ------------------ | -------------- |
| `:w` / `:write`    | 保存           |
| `:wa` / `:wall`    | 保存所有文件   |
| `:q` / `:quit`     | 关闭           |
| `:wq`              | 保存并关闭     |
| `:wqa` / `:wqall`  | 保存全部并关闭 |
| `:qa` / `:qall`    | 关闭所有       |
| `:cq`              | 强制关闭       |
| `:bd` / `:bdelete` | 关闭 Buffer    |
| `:e {file}`        | 打开文件       |
| `:ls`              | 列出 Buffers   |

### 14.2 分屏与标签

| 命令                   | 说明           |
| ---------------------- | -------------- |
| `:vs` / `:vsplit`      | 垂直分屏       |
| `:sp` / `:split`       | 水平分屏       |
| `:new`                 | 水平分屏新文件 |
| `:vnew`                | 垂直分屏新文件 |
| `:tabedit` / `:tabnew` | 新标签页       |
| `:tabn` / `:tabnext`   | 下一个标签页   |
| `:tabp` / `:tabprev`   | 上一个标签页   |
| `:tabc` / `:tabclose`  | 关闭标签页     |

### 14.3 跳转与导航

| 命令        | 说明             |
| ----------- | ---------------- |
| `:{number}` | 跳转到指定行     |
| `:$`        | 跳转到文件末尾   |
| `:/foo`     | 搜索 foo（向下） |
| `:?foo`     | 搜索 foo（向上） |

### 14.4 诊断

| 命令             | 说明           |
| ---------------- | -------------- |
| `:cn` / `:cnext` | 下一个诊断     |
| `:cp` / `:cprev` | 上一个诊断     |
| `:cc`            | 显示当前诊断   |
| `:clist` / `:cl` | 列出所有诊断   |
| `:ln` / `:lnext` | 下一个位置     |
| `:lp` / `:lprev` | 上一个位置     |
| `:ll`            | 当前定位列表项 |

### 14.5 替换

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `:s/foo/bar/`     | 替换当前行（Zed 默认全局） |
| `:%s/foo/bar/`    | 全文替换                   |
| `:5,10s/foo/bar/` | 范围替换                   |

### 14.6 编辑命令

| 命令             | 说明                         |
| ---------------- | ---------------------------- |
| `:j` / `:join`   | 连接行                       |
| `:d` / `:delete` | 删除行                       |
| `:s` / `:sort`   | 排序（`:sort i` 忽略大小写） |
| `:y` / `:yank`   | 复制行                       |

### 14.7 设置选项

| 命令                        | 说明       |
| --------------------------- | ---------- |
| `:set wrap` / `:set nowrap` | 自动换行   |
| `:set nu` / `:set nonu`     | 行号       |
| `:set rnu` / `:set nornu`   | 相对行号   |
| `:set ic` / `:set noic`     | 忽略大小写 |

### 14.8 Zed 特有命令

| 命令                   | 说明               |
| ---------------------- | ------------------ |
| `:E` / `:Explore`      | 打开项目面板       |
| `:C` / `:Collab`       | 协作面板           |
| `:Ch` / `:Chat`        | 聊天面板           |
| `:A` / `:AI`           | AI 助手            |
| `:No` / `:Notif`       | 通知面板           |
| `:fe` / `:feedback`    | 反馈               |
| `:te` / `:term`        | 终端               |
| `:Ext` / `:Extensions` | 扩展面板           |
| `:G` / `:Git`          | Git 面板           |
| `:diffs`               | 切换 Hunk Diff     |
| `:cpp`                 | 复制路径           |
| `:crp`                 | 复制相对路径       |
| `:reveal`              | 在文件管理器中显示 |
| `:zlog`                | 打开 Zed 日志      |
| `:clank`               | 取消语言服务器任务 |

### 14.9 自定义命令别名

```json
{
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q",
        "Wa": "wa"
    }
}
```

---

## 15. 内置插件功能

Zed 内置了常用 Vim 插件功能的等价实现：

### 15.1 Surround（vim-surround）

| 快捷键             | 说明                                     |
| ------------------ | ---------------------------------------- |
| `ys{motion}{char}` | 添加包围（如 `ysiw"` 用双引号包围单词）  |
| `cs{old}{new}`     | 修改包围（如 `cs"'` 将双引号改为单引号） |
| `ds{char}`         | 删除包围（如 `ds[` 删除方括号）          |
| `S{char}` (visual) | 用字符包围选中文本                       |

> Visual 模式下需要自定义绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
>     }
> }
> ```

### 15.2 Comment（vim-commentary）

| 快捷键        | 说明                            |
| ------------- | ------------------------------- |
| `gcc`         | 切换当前行注释                  |
| `gc` (visual) | 切换选中行注释                  |
| `gc{motion}`  | 对移动范围切换注释（如 `gc3j`） |

### 15.3 Exchange（vim-exchange）

| 快捷键        | 说明             |
| ------------- | ---------------- |
| `cx{motion}`  | 标记要交换的文本 |
| `cx` (第二次) | 执行交换         |

> Visual 模式下需手动绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-x": "vim::Exchange"
>     }
> }
> ```

### 15.4 ReplaceWithRegister

| 快捷键       | 说明                                 |
| ------------ | ------------------------------------ |
| `gR{motion}` | 用寄存器内容替换文本（不覆盖寄存器） |

### 15.5 netrw 风格项目面板

在项目面板中使用 `hjkl` 导航：

| 快捷键                | 说明           |
| --------------------- | -------------- |
| `h` / `j` / `k` / `l` | 导航           |
| `o`                   | 打开文件       |
| `t`                   | 在新标签页打开 |

### 15.6 Sneak 导航

需自定义绑定：

```json
{
    "context": "vim_mode == normal || vim_mode == visual",
    "bindings": {
        "s": "vim::PushSneak",
        "shift-s": "vim::PushSneakBackward"
    }
}
```

### 15.7 AnyQuotes / AnyBrackets 文本对象

```json
{
    "context": "vim_operator == a || vim_operator == i || vim_operator == cs",
    "bindings": {
        "q": "vim::AnyQuotes",
        "b": "vim::AnyBrackets",
        "Q": "vim::MiniQuotes",
        "B": "vim::MiniBrackets"
    }
}
```

---

## 16. 标记与寄存器

### 16.1 标记（Marks）

| 快捷键       | 说明                       |
| ------------ | -------------------------- |
| `m{a-z}`     | 设置缓冲区本地标记         |
| `'{a-z}`     | 跳转到标记所在行           |
| `` `{a-z} `` | 跳转到标记精确位置         |
| `''`         | 跳转到上次位置             |
| `'<` / `'>`  | 上次可视选择的开始/结束    |
| `'[` / `']`  | 上次修改/复制的开始/结束   |
| `'{` / `'}`  | 段落跳转标记               |
| `'.`         | 上次修改位置               |
| `'^`         | 上次 Insert 模式退出的位置 |

### 16.2 寄存器（Registers）

| 寄存器      | 说明                                  |
| ----------- | ------------------------------------- |
| `""`        | 无名寄存器（默认）                    |
| `"0`        | 最近复制的文本                        |
| `"1` - `"9` | 最近删除的文本（从新到旧）            |
| `"a` - `"z` | 命名寄存器                            |
| `"+`        | 系统剪贴板                            |
| `"*`        | 选择剪贴板（Linux/Windows 等同 `"+`） |
| `"_`        | 黑洞寄存器（删除不保存）              |
| `"%`        | 当前文件名                            |
| `".`        | 上次插入的文本                        |

使用方式：

- `"ayw` — 将单词复制到寄存器 a
- `"ap` — 粘贴寄存器 a 的内容
- `"+y` — 复制到系统剪贴板
- `"_dd` — 删除行但不保存到寄存器

### 16.3 剪贴板配置

```json
{
    "vim": {
        "use_system_clipboard": "always"
        // "always"  — 始终使用系统剪贴板
        // "never"   — 永远不使用
        // "on_yank" — 仅复制时使用
    }
}
```

---

## 17. 宏

Zed 使用自己的录制系统实现 Vim 宏功能：

| 快捷键       | 说明                    |
| ------------ | ----------------------- |
| `q{a-z}`     | 开始录制到寄存器 {a-z}  |
| `q` (录制中) | 停止录制                |
| `Q`          | 重复上次录制的宏        |
| `@{a-z}`     | 执行寄存器 {a-z} 中的宏 |
| `@@`         | 重复上次执行的宏        |
| `{n}@{a-z}`  | 执行宏 n 次             |

> **注意：** Zed 的宏系统与原生 Vim 不同，宏存储与复制寄存器是独立的命名空间。

---

## 18. 配置

### 18.1 Vim 专属配置

在 `settings.json` 的 `"vim"` 键下配置：

```json
{
    "vim": {
        // 系统剪贴板策略："always" | "never" | "on_yank"
        "use_system_clipboard": "always",

        // 多行搜索
        "use_multiline_find": true,

        // 智能大小写搜索（全小写时忽略大小写，含大写时精确匹配）
        "use_smartcase_find": true,

        // 切换相对行号
        "toggle_relative_line_numbers": true,

        // 自定义有向图（digraph），如 Ctrl+k f z → 🧟
        "custom_digraphs": { "fz": "🧟♀️" },

        // 启动时的默认模式
        "default_mode": "normal",

        // 复制后高亮持续时间（毫秒），0 禁用
        "highlight_on_yank_duration": 200,

        // 使用正则搜索
        "use_regex_search": true,

        // 替换默认全局（相当于始终 /g）
        "gdefault": false
    }
}
```

### 18.2 推荐的伴随设置

```json
{
    // 禁用光标闪烁（像原生 Vim）
    "cursor_blink": false,

    // 相对行号
    "relative_line_numbers": true,

    // 滚动条
    "scrollbar": { "show": "never" },

    // 不允许滚动超过最后一行
    "scroll_beyond_last_line": "off",

    // 垂直滚动边距
    "vertical_scroll_margin": 0,

    // 行号
    "gutter": { "line_numbers": false },

    // 命令别名
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q"
    }
}
```

---

## 19. 自定义快捷键

### 19.1 Vim 上下文

自定义快捷键需要理解 Zed 的上下文系统：

| 上下文                 | 含义                                          |
| ---------------------- | --------------------------------------------- |
| `VimControl`           | `normal` \|\| `visual` \|\| `operator` 的别名 |
| `vim_mode == normal`   | Normal 模式                                   |
| `vim_mode == visual`   | Visual 模式                                   |
| `vim_mode == insert`   | Insert 模式                                   |
| `vim_mode == replace`  | Replace 模式                                  |
| `vim_mode == waiting`  | 等待（如 `f`/`t` 后等待字符）                 |
| `vim_mode == operator` | 操作符挂起（如 `c`/`d` 后等待移动）           |
| `vim_operator == d`    | 当前操作符为 `d`                              |
| `vim_operator == c`    | 当前操作符为 `c`                              |
| `vim_operator == y`    | 当前操作符为 `y`                              |

### 19.2 示例

```json
[
    // Normal 模式下的自定义
    {
        "context": "vim_mode == normal && !menu",
        "bindings": {
            "shift-y": ["workspace::SendKeystrokes", "y $"],
            "space w": "workspace::Save"
        }
    },

    // Visual 模式下的自定义
    {
        "context": "vim_mode == visual",
        "bindings": {
            "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
        }
    },

    // Insert 模式退出
    {
        "context": "vim_mode == insert",
        "bindings": {
            "j k": "vim::NormalBefore",
            "j j": "vim::NormalBefore"
        }
    },

    // 窗格中
    {
        "context": "EmptyPane || SharedScreen",
        "bindings": {
            "space f": "file_finder::Toggle"
        }
    }
]
```

### 19.3 恢复系统剪贴板快捷键（Linux/Windows）

```json
{
    "context": "Editor && !menu",
    "bindings": {
        "ctrl-c": "editor::Copy",
        "ctrl-x": "editor::Cut",
        "ctrl-v": "editor::Paste",
        "ctrl-y": "editor::Undo",
        "ctrl-f": "buffer_search::Deploy",
        "ctrl-o": "workspace::Open",
        "ctrl-a": "editor::SelectAll"
    }
}
```

---

## 20. 正则表达式差异

Zed 使用不同的正则引擎（Rust `regex` crate），与 Vim 的语法有差异：

| 特性         | Vim       | Zed                                |
| ------------ | --------- | ---------------------------------- |
| 捕获组       | `\(` `\)` | `(` `)`                            |
| 字面括号     | `(` `)`   | `\(` `\)`                          |
| 完整匹配引用 | `\0`      | `$0`                               |
| 编号组引用   | `\1`      | `$1`                               |
| 全局标志     | 需 `/g`   | 默认全局（可设 `gdefault: false`） |
| 大小写不敏感 | `/i` 标志 | `(?i)` 或 `cmd-option-c`           |

> `:s` 命令面板会自动转换 Vim 风格的语法。

---

## 21. 补充：文档未提及的 Vim 常用命令

以下命令在 Zed 官方文档中未明确列出，但在日常 Vim 使用中非常重要。**部分在 Zed 中可直接使用，部分可能需要验证。**

### 21.1 Normal 模式进阶命令

| 快捷键             | 说明                              | Zed 支持      |
| ------------------ | --------------------------------- | ------------- |
| `g_`               | 跳转到行尾最后一个非空白字符      | ⚠️ 待验证     |
| `g^`               | 跳转到行首第一个非空白字符        | ⚠️ 待验证     |
| `gm`               | 跳转到行中间                      | ⚠️ 待验证     |
| `g0`               | 跳转到行首（即使是换行行）        | ⚠️ 待验证     |
| `g$`               | 跳转到行尾（即使是换行行）        | ⚠️ 待验证     |
| `g~` / `gu` / `gU` | 切换/小写/大写 + 移动             | ✅ 基本支持   |
| `g?`               | Rot13 编码                        | ❌ 通常不支持 |
| `ga`               | 显示光标下字符的 ASCII/Unicode 值 | ⚠️ 待验证     |
| `g Ctrl+g`         | 显示光标位置统计                  | ⚠️ 待验证     |
| `Ctrl+g`           | 显示文件信息和当前行号            | ⚠️ 待验证     |
| `del`              | 删除光标后字符（同 `x`）          | ⚠️ 待验证     |
| `D`                | 删除到行尾                        | ✅ 支持       |
| `Y`                | 复制整行（同 `yy`）               | ✅ 支持       |
| `C`                | 修改到行尾                        | ✅ 支持       |
| `s`                | 删除字符并进入 Insert             | ✅ 支持       |
| `S` / `cc`         | 删除整行并进入 Insert             | ✅ 支持       |
| `~`                | 切换字符大小写                    | ✅ 支持       |

### 21.2 窗口/标签页进阶

| 快捷键                   | 说明                   | Zed 支持           |
| ------------------------ | ---------------------- | ------------------ |
| `Ctrl+w T`               | 将当前窗格移到新标签页 | ⚠️ 待验证          |
| `Ctrl+w x`               | 交换窗格位置           | ⚠️ 待验证          |
| `Ctrl+w r`               | 旋转窗格               | ⚠️ 待验证          |
| `Ctrl+w H`               | 将窗格移到左侧（全高） | ⚠️ 待验证          |
| `Ctrl+w J`               | 将窗格移到底部（全宽） | ⚠️ 待验证          |
| `Ctrl+w K`               | 将窗格移到顶部（全宽） | ⚠️ 待验证          |
| `Ctrl+w L`               | 将窗格移到右侧（全高） | ⚠️ 待验证          |
| `:on` / `:only`          | 仅保留当前窗格         | ✅ 通过 `Ctrl+w o` |
| `:tabm {n}`              | 移动标签页到位置 n     | ⚠️ 待验证          |
| `:tabfirst` / `:tablast` | 第一个/最后一个标签页  | ⚠️ 待验证          |

### 21.3 折叠命令

Zed 使用 Tree-sitter 的代码折叠，以下命令**可能需要验证**：

| 快捷键       | 说明                     |
| ------------ | ------------------------ |
| `za`         | 切换当前折叠             |
| `zA`         | 递归切换折叠             |
| `zc`         | 关闭折叠                 |
| `zC`         | 递归关闭折叠             |
| `zo`         | 打开折叠                 |
| `zO`         | 递归打开折叠             |
| `zm`         | 增加折叠级别（关闭更多） |
| `zM`         | 关闭所有折叠             |
| `zr`         | 减少折叠级别（打开更多） |
| `zR`         | 打开所有折叠             |
| `zf{motion}` | 手动创建折叠             |
| `zd`         | 删除折叠                 |
| `zE`         | 删除所有折叠             |

### 21.4 插入模式进阶

| 快捷键              | 说明                                | Zed 支持           |
| ------------------- | ----------------------------------- | ------------------ |
| `Ctrl+v {code}`     | 插入任意字符（如 `Ctrl+v 065` → A） | ⚠️ 待验证          |
| `Ctrl+k {digraph}`  | 插入有向图（如 `Ctrl+k a:` → ä）    | ✅ 自定义 digraphs |
| `Ctrl+a`            | 插入上次 Insert 模式的文本          | ⚠️ 待验证          |
| `Ctrl+@`            | 插入上次 Insert 文本并退出          | ⚠️ 待验证          |
| `Ctrl+d`            | 减少缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+t`            | 增加缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+y`            | 插入上一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+e`            | 插入下一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+j` / `Ctrl+m` | 换行（同 Enter）                    | ✅ 支持            |
| `Ctrl+r {reg}`      | 插入寄存器内容                      | ⚠️ 待验证          |

### 21.5 有用但常被忽视的命令

| 快捷键      | 说明                      | Zed 支持        |
| ----------- | ------------------------- | --------------- |
| `g&`        | 在所有行上重复上次替换    | ⚠️ 待验证       |
| `&`         | 在当前行重复上次替换      | ⚠️ 待验证       |
| `@:`        | 重复上次 Ex 命令          | ⚠️ 待验证       |
| `q:`        | 打开命令历史窗口          | ❌ Zed 无此概念 |
| `q/` / `q?` | 打开搜索历史窗口          | ❌ Zed 无此概念 |
| `K`         | 查看光标下关键字的 man 页 | ❌ 被重映射     |
| `zi`        | 切换折叠启用/禁用         | ⚠️ 待验证       |
| `zv`        | 打开足够折叠以显示光标    | ⚠️ 待验证       |
| `zx` / `zX` | 更新折叠/重新应用折叠     | ⚠️ 待验证       |
| `:retab`    | 转换 tab 为空格（或反之） | ⚠️ 待验证       |

### 21.6 快速修复窗口操作

| 快捷键               | 说明                 | Zed 支持        |
| -------------------- | -------------------- | --------------- |
| `:copen`             | 打开快速修复窗口     | ❌ 诊断面板替代 |
| `:cclose`            | 关闭快速修复窗口     | ❌              |
| `:cfirst` / `:clast` | 第一个/最后一个错误  | ⚠️ 待验证       |
| `:cdo {cmd}`         | 对每个错误项执行命令 | ❌              |

### 21.7 Buffer 管理进阶

| 快捷键               | 说明                   | Zed 支持  |
| -------------------- | ---------------------- | --------- |
| `:bufdo {cmd}`       | 对所有 Buffer 执行命令 | ❌        |
| `:b {num}`           | 切换到指定编号 Buffer  | ⚠️ 待验证 |
| `:bnext` / `:bprev`  | 同 `:bn` / `:bp`       | ✅ 支持   |
| `:bfirst` / `:blast` | 第一个/最后一个 Buffer | ⚠️ 待验证 |
| `ctrl+^` / `ctrl+6`  | 切换交替文件           | ⚠️ 待验证 |

### 21.8 实用技巧组合

这些都是 Vim 中常用的高效编辑组合：

| 组合        | 效果                             |
| ----------- | -------------------------------- |
| `ci"`       | 修改双引号内容                   |
| `ca"`       | 修改双引号包含引号本身           |
| `yi(`       | 复制括号内容                     |
| `da]`       | 删除方括号及内容                 |
| `vit` + `c` | 修改 HTML 标签内容               |
| `vat` + `d` | 删除整个 HTML 标签               |
| `gUiw`      | 将单词转为大写                   |
| `guiw`      | 将单词转为小写                   |
| `g~iw`      | 切换单词大小写                   |
| `guG`       | 从当前位置到文件末转为小写       |
| `gUgg`      | 从当前位置到文件头转为大写       |
| `=%`        | 缩进整个匹配块（光标在括号上时） |
| `>i{`       | 缩进花括号内容                   |
| `<a{`       | 减少花括号块缩进                 |

---

## 参考资源

- [Zed 官方 Vim 文档](https://zed.dev/docs/vim)
- [Zed 官方 Key Bindings 文档](https://zed.dev/docs/key-bindings)
- [Zed Decoded: Vim (博客)](https://zed.dev/blog/zed-decoded-vim)
- [Zed Vim Roadmap 2025](https://zed.dev/blog/vim-2025)
- [Zed GitHub 仓库](https://github.com/zed-industries/zed)
- [Zed Vim 模式源码文档](https://git.iko.soy/zed-industries/zed/src/commit/174e12568669e70abb15934597a3f5df335eaf08/docs/src/vim.md)

> **注意：** 标记为 ⚠️ 的条目需要在实际使用中验证。Zed 的 Vim 模式在持续更新中，如有缺失功能，可在 [Zed GitHub Issues](https://github.com/zed-industries/zed/issues) 中查找或提交。
>
> 本文档基于 2025 年 7 月的 Zed Vim 模式状态整理。# Zed 中 Vim 模式完整使用文档

> 基于 [Zed 官方 Vim 文档](https://zed.dev/docs/vim) 整理，并补充文档中未提及的 Vim 常用命令。

---

## 目录

1. [概述](#1-概述)
2. [启用 Vim 模式](#2-启用-vim-模式)
3. [模式切换](#3-模式切换)
4. [光标移动](#4-光标移动)
5. [编辑操作](#5-编辑操作)
6. [可视模式操作](#6-可视模式操作)
7. [搜索与替换](#7-搜索与替换)
8. [LSP / 代码导航](#8-lsp--代码导航)
9. [Git 集成](#9-git-集成)
10. [Tree-sitter 导航与文本对象](#10-tree-sitter-导航与文本对象)
11. [多光标操作](#11-多光标操作)
12. [窗格与窗口管理](#12-窗格与窗口管理)
13. [Insert 模式命令](#13-insert-模式命令)
14. [Ex 命令](#14-ex-命令)
15. [内置插件功能](#15-内置插件功能)
16. [标记与寄存器](#16-标记与寄存器)
17. [宏](#17-宏)
18. [配置](#18-配置)
19. [自定义快捷键](#19-自定义快捷键)
20. [正则表达式差异](#20-正则表达式差异)
21. [补充：文档未提及的 Vim 常用命令](#21-补充文档未提及的-vim-常用命令)

---

## 1. 概述

Zed 内置了 Vim 模拟层（Vim mode），目标是：

- **"做你期望的事"**：在合理的地方精确复制 Vim 的行为
- **利用 Zed 的现代特性**：Tree-sitter 语法解析、LSP、多光标、CRDT 协作等
- **不是 100% Vim 兼容**，但追求 **100% Vim 熟悉感**

### 与原生 Vim 的核心差异

| 方面       | Vim          | Zed                     |
| ---------- | ------------ | ----------------------- |
| 光标模型   | 定位在字符上 | 定位在字符之间          |
| 视觉块模式 | 原生块选择   | 通过多光标模拟          |
| 宏         | `q` / `@`    | 使用 Zed 的录制系统     |
| 搜索替换   | Vim 正则     | Zed 正则引擎            |
| 词语定义   | 固定规则     | 语言感知（Tree-sitter） |

---

## 2. 启用 Vim 模式

### 方法一：欢迎屏幕

首次启动 Zed 时，勾选 "Vim Mode" 复选框。

### 方法二：命令面板

`Ctrl+Shift+P` → 输入 `toggle vim mode` → 回车。

### 方法三：配置文件

在 `settings.json` 中添加：

```json
{
    "vim_mode": true
}
```

### 按发布渠道启用

```json
{
    "vim_mode": false,
    "preview": {
        "vim_mode": true
    },
    "nightly": {
        "vim_mode": true
    }
}
```

---

## 3. 模式切换

| 快捷键     | 模式         | 说明                       |
| ---------- | ------------ | -------------------------- |
| `Esc`      | Normal       | 从任何模式返回 Normal 模式 |
| `Ctrl+c`   | Normal       | 替代 Esc                   |
| `i`        | Insert       | 在当前字符前插入           |
| `a`        | Insert       | 在当前字符后插入           |
| `I`        | Insert       | 在行首第一个非空字符前插入 |
| `A`        | Insert       | 在行尾插入                 |
| `o`        | Insert       | 在下方新建一行             |
| `O`        | Insert       | 在上方新建一行             |
| `v`        | Visual       | 字符选择模式               |
| `V`        | Visual (行)  | 整行选择模式               |
| `Ctrl+v`   | Visual Block | 块选择模式（多光标模拟）   |
| `R`        | Replace      | 替换模式（覆盖输入）       |
| `r`        | Normal       | 替换单个字符               |
| `s`        | Insert       | 删除当前字符并进入 Insert  |
| `S` / `cc` | Insert       | 删除整行并进入 Insert      |
| `C`        | Insert       | 删除到行尾并进入 Insert    |

### Insert 模式下返回 Normal

除 `Esc` 和 `Ctrl+c` 外，可自定义快捷键：

```json
{
    "context": "vim_mode == insert",
    "bindings": {
        "j k": "vim::NormalBefore",
        "j j": "vim::NormalBefore"
    }
}
```

---

## 4. 光标移动

### 4.1 基本移动

| 快捷键                | 说明                    |
| --------------------- | ----------------------- |
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右       |
| `w`                   | 下一个单词开头          |
| `W`                   | 下一个 WORD（空白分隔） |
| `b`                   | 上一个单词开头          |
| `B`                   | 上一个 WORD             |
| `e`                   | 下一个单词结尾          |
| `E`                   | 下一个 WORD 结尾        |
| `ge`                  | 上一个单词结尾          |
| `gE`                  | 上一个 WORD 结尾        |

### 4.2 行内移动

| 快捷键    | 说明                              |
| --------- | --------------------------------- |
| `0`       | 行首（含空白）                    |
| `^`       | 行首第一个非空白字符              |
| `$`       | 行尾                              |
| `f{char}` | 向右查找字符（光标停在字符上）    |
| `F{char}` | 向左查找字符                      |
| `t{char}` | 向右查找字符（光标停在字符前）    |
| `T{char}` | 向左查找字符（光标停在字符后）    |
| `;`       | 重复上次 `f` / `t` 查找（同方向） |
| `,`       | 重复上次 `f` / `t` 查找（反方向） |

### 4.3 页面滚动

| 快捷键   | 说明                 |
| -------- | -------------------- |
| `Ctrl+d` | 向下翻半页           |
| `Ctrl+u` | 向上翻半页           |
| `Ctrl+f` | 向下翻一页           |
| `Ctrl+b` | 向上翻一页           |
| `zt`     | 将当前行置于屏幕顶部 |
| `zz`     | 将当前行置于屏幕中央 |
| `zb`     | 将当前行置于屏幕底部 |

### 4.4 文件级移动

| 快捷键           | 说明                                             |
| ---------------- | ------------------------------------------------ |
| `gg`             | 跳到文件第一行                                   |
| `G`              | 跳到文件最后一行                                 |
| `{n}gg` / `{n}G` | 跳到第 n 行                                      |
| `H`              | 屏幕顶部                                         |
| `M`              | 屏幕中间                                         |
| `L`              | 屏幕底部                                         |
| `{`              | 上一段（空行分隔）                               |
| `}`              | 下一段                                           |
| `%`              | 跳转到匹配的括号（语言感知，Rust 中可匹配 `\|`） |

### 4.5 跳转列表

| 快捷键   | 说明             |
| -------- | ---------------- |
| `Ctrl+o` | 跳到上一个位置   |
| `Ctrl+i` | 跳到下一个位置   |
| `Ctrl+t` | 从标签栈弹出     |
| `g;`     | 跳到上次修改位置 |
| `g,`     | 跳到下次修改位置 |

### 4.6 自定义子词移动（CamelCase / snake_case）

```json
{
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
        "w": "vim::NextSubwordStart",
        "b": "vim::PreviousSubwordStart",
        "e": "vim::NextSubwordEnd",
        "g e": "vim::PreviousSubwordEnd"
    }
}
```

---

## 5. 编辑操作

### 5.1 删除

| 快捷键     | 说明                       |
| ---------- | -------------------------- |
| `x`        | 删除当前字符               |
| `X`        | 删除前一个字符（退格）     |
| `dd`       | 删除当前行                 |
| `D`        | 删除到行尾                 |
| `dw`       | 删除到单词结尾             |
| `db`       | 删除到单词开头             |
| `daw`      | 删除整个单词（含周围空格） |
| `diw`      | 删除单词内部               |
| `d$` / `D` | 删除到行尾                 |
| `d0`       | 删除到行首                 |
| `dG`       | 删除到文件末尾             |
| `dgg`      | 删除到文件开头             |
| `dt{char}` | 删除直到指定字符           |
| `df{char}` | 删除到指定字符（含）       |

### 5.2 复制（Yank）

| 快捷键     | 说明           |
| ---------- | -------------- |
| `yy` / `Y` | 复制当前行     |
| `yw`       | 复制单词       |
| `yaw`      | 复制整个单词   |
| `y$`       | 复制到行尾     |
| `y0`       | 复制到行首     |
| `yG`       | 复制到文件末尾 |
| `ygg`      | 复制到文件开头 |

### 5.3 粘贴

| 快捷键 | 说明                       |
| ------ | -------------------------- |
| `p`    | 在光标后粘贴               |
| `P`    | 在光标前粘贴               |
| `gp`   | 粘贴并将光标移到粘贴内容后 |
| `gP`   | 粘贴并将光标移到粘贴内容前 |

### 5.4 修改（Change）

| 快捷键                | 说明                     |
| --------------------- | ------------------------ |
| `cw`                  | 修改到单词结尾           |
| `ciw`                 | 修改整个单词             |
| `caw`                 | 修改整个单词（含空格）   |
| `cc` / `S`            | 修改整行                 |
| `C`                   | 修改到行尾               |
| `ci(` / `ci[` / `ci{` | 修改括号内内容           |
| `ci"` / `ci'`         | 修改引号内内容           |
| `cit`                 | 修改 HTML/XML 标签内内容 |

### 5.5 其他编辑

| 快捷键     | 说明                     |
| ---------- | ------------------------ |
| `u`        | 撤销                     |
| `Ctrl+r`   | 重做                     |
| `.`        | 重复上次修改             |
| `>>`       | 增加缩进                 |
| `<<`       | 减少缩进                 |
| `>` + 移动 | 缩进指定范围             |
| `<` + 移动 | 减少缩进指定范围         |
| `~`        | 切换大小写               |
| `J`        | 将下一行连接到当前行     |
| `gJ`       | 连接行（不加空格）       |
| `r{char}`  | 替换当前字符             |
| `xp`       | 交换当前字符与下一个字符 |
| `ddp`      | 交换当前行与下一行       |

### 5.6 数字前缀（Count）

几乎所有命令都支持数字前缀：

| 示例                  | 效果               |
| --------------------- | ------------------ |
| `5j`                  | 向下移动 5 行      |
| `3dd`                 | 删除 3 行          |
| `4x`                  | 删除 4 个字符      |
| `2dw`                 | 删除 2 个单词      |
| `5i` + `text` + `Esc` | 插入 `text` 5 次   |
| `10.`                 | 重复上次修改 10 次 |

> **注意：** `5dj` 和 `d5j` 的区别 — `5dj` 表示"删除 5 次 = 5 行"，而 `d5j` 表示"删除操作应用于向下 5 行"。效果相同但语义不同。

---

## 6. 可视模式操作

| 快捷键                  | 说明                   |
| ----------------------- | ---------------------- |
| `v`                     | 进入字符可视模式       |
| `V`                     | 进入行可视模式         |
| `Ctrl+v`                | 进入块可视模式         |
| `gv`                    | 重新选择上次的可视区域 |
| `o` (visual 模式)       | 跳转到选择区域的另一端 |
| `O` (visual block 模式) | 跳转到对角线另一端     |
| `u` (visual 模式)       | 将选中文本转为小写     |
| `U` (visual 模式)       | 将选中文本转为大写     |
| `>`                     | 缩进选中文本           |
| `<`                     | 减少缩进选中文本       |
| `I` (block 模式)        | 在块前插入（多行同时） |
| `A` (block 模式)        | 在块后插入（多行同时） |
| `c` (visual 模式)       | 修改选中文本           |
| `d` / `x` (visual 模式) | 删除选中文本           |
| `y` (visual 模式)       | 复制选中文本           |

---

## 7. 搜索与替换

### 7.1 搜索

| 快捷键 | 说明                               |
| ------ | ---------------------------------- |
| `/`    | 向下搜索                           |
| `?`    | 向上搜索                           |
| `n`    | 下一个匹配项                       |
| `N`    | 上一个匹配项                       |
| `*`    | 搜索光标下的单词（向下）           |
| `#`    | 搜索光标下的单词（向上）           |
| `g*`   | 搜索光标下的单词（部分匹配，向下） |
| `g#`   | 搜索光标下的单词（部分匹配，向上） |
| `gn`   | 选中下一个匹配项                   |
| `gN`   | 选中上一个匹配项                   |
| `cgn`  | 修改下一个匹配项，然后 `.` 重复    |

> **搜索配置：**
>
> ```json
> {
>     "vim": {
>         "use_smartcase_find": true, // 智能大小写
>         "use_regex_search": true // 使用正则搜索
>     }
> }
> ```

### 7.2 替换

| 命令               | 说明                               |
| ------------------ | ---------------------------------- |
| `:s/old/new/`      | 替换当前行第一个匹配               |
| `:s/old/new/g`     | 替换当前行所有匹配                 |
| `:%s/old/new/`     | 替换全文（`/g` 在 Zed 中默认隐含） |
| `:%s/old/new/gc`   | 替换全文（逐个确认）               |
| `:5,10s/old/new/`  | 替换第 5-10 行                     |
| `:'<,'>s/old/new/` | 替换选中的范围                     |

> **Zed 正则差异** — 详见[第 20 节](#20-正则表达式差异)。

---

## 8. LSP / 代码导航

Zed 将 Vim 快捷键与 LSP 功能深度集成：

| 快捷键        | 说明                    |
| ------------- | ----------------------- |
| `g d`         | 跳转到定义              |
| `g D`         | 跳转到声明              |
| `g y`         | 跳转到类型定义          |
| `g I`         | 跳转到实现              |
| `c d`         | 重命名符号              |
| `g A`         | 查找所有引用            |
| `g s`         | 文件内查找符号          |
| `g S`         | 项目内查找符号          |
| `g ]` / `] d` | 下一个诊断（错误/警告） |
| `g [` / `[ d` | 上一个诊断              |
| `g h`         | 显示悬停信息            |
| `g .`         | 打开代码操作菜单        |
| `g r`         | 重命名（与 `c d` 类似） |

---

## 9. Git 集成

| 快捷键 | 说明                 |
| ------ | -------------------- |
| `] c`  | 下一个 Git 更改      |
| `[ c`  | 上一个 Git 更改      |
| `d o`  | 展开 diff hunk       |
| `d O`  | 切换暂存状态         |
| `d u`  | 暂存并移到下一个     |
| `d U`  | 取消暂存并移到下一个 |
| `d p`  | 还原更改             |

---

## 10. Tree-sitter 导航与文本对象

### 10.1 语法节点导航

| 快捷键        | 说明                         |
| ------------- | ---------------------------- |
| `] m` / `[ m` | 下一个/上一个方法            |
| `] M` / `[ M` | 下一个/上一个方法结尾        |
| `] ]` / `[ [` | 下一个/上一个段落（Section） |
| `] /` / `[ /` | 下一个/上一个注释            |
| `] *` / `[ *` | 下一个/上一个注释（同上）    |
| `[ x`         | 选择更大的语法节点           |
| `] x`         | 选择更小的语法节点           |

### 10.2 文本对象

| 快捷键                | 说明                |
| --------------------- | ------------------- |
| `a c` / `i c`         | 整个类 / 类内部     |
| `a f` / `i f`         | 整个函数 / 函数内部 |
| `g c`                 | 注释                |
| `i a` / `a a`         | 参数内部 / 整个参数 |
| `a t` / `i t`         | HTML 标签           |
| `a I` / `a i` / `i i` | 缩进级别            |
| `i w` / `a w`         | 单词                |
| `i W` / `a W`         | WORD                |
| `i s` / `a s`         | 句子                |
| `i p` / `a p`         | 段落                |
| `i (` / `a (`         | 圆括号内            |
| `i [` / `a [`         | 方括号内            |
| `i {` / `a {`         | 花括号内            |
| `i "` / `a "`         | 双引号内            |
| `i '` / `a '`         | 单引号内            |
| `i \`` / `a \``       | 反引号内            |
| `i t` / `a t`         | HTML/XML 标签内     |

### 10.3 缩进文本对象

| 快捷键      | 说明                      |
| ----------- | ------------------------- |
| `[-`        | 上一个缩进更少的行        |
| `]-`        | 下一个缩进更少的行        |
| `[+` / `]+` | 上一个/下一个缩进更多的行 |
| `[=` / `]=` | 上一个/下一个相同缩进的行 |

---

## 11. 多光标操作

Zed 在 Vim 模式下保留了多光标功能：

| 快捷键 | 说明                         |
| ------ | ---------------------------- |
| `g l`  | 添加光标到下一个相同单词     |
| `g L`  | 添加光标到上一个相同单词     |
| `g a`  | 选中当前单词的所有出现       |
| `g A`  | 在选中区域的每行末尾添加光标 |
| `g I`  | 在选中区域的每行开头添加光标 |
| `g >`  | 跳过并添加下一个             |
| `g <`  | 跳过并添加上一个             |
| `Esc`  | 退出多光标模式，回到 Normal  |

---

## 12. 窗格与窗口管理

### 12.1 分屏操作

| 快捷键            | 说明                       |
| ----------------- | -------------------------- |
| `:vs` / `:vsplit` | 垂直分屏                   |
| `:sp` / `:split`  | 水平分屏                   |
| `:new`            | 水平分屏打开新文件         |
| `:vnew`           | 垂直分屏打开新文件         |
| `Ctrl+w v`        | 垂直分屏                   |
| `Ctrl+w s`        | 水平分屏                   |
| `Ctrl+w q`        | 关闭当前窗格               |
| `Ctrl+w c`        | 关闭当前窗格               |
| `Ctrl+w o`        | 仅保留当前窗格（关闭其他） |

### 12.2 窗格间导航

| 快捷键                  | 说明             |
| ----------------------- | ---------------- |
| `Ctrl+w h` / `Ctrl+w j` | 左 / 下          |
| `Ctrl+w k` / `Ctrl+w l` | 上 / 右          |
| `Ctrl+w w`              | 循环切换窗格     |
| `Ctrl+w p`              | 切换到上一个窗格 |
| `Ctrl+w t`              | 切换到最顶部窗格 |
| `Ctrl+w b`              | 切换到最底部窗格 |

### 12.3 窗格调整

| 快捷键      | 说明         |
| ----------- | ------------ |
| `Ctrl+w =`  | 均分窗格大小 |
| `Ctrl+w >`  | 增加宽度     |
| `Ctrl+w <`  | 减少宽度     |
| `Ctrl+w +`  | 增加高度     |
| `Ctrl+w -`  | 减少高度     |
| `Ctrl+w _`  | 最大化高度   |
| `Ctrl+w \|` | 最大化宽度   |

### 12.4 标签页

| 快捷键                 | 说明                |
| ---------------------- | ------------------- |
| `:tabnew` / `:tabedit` | 新建标签页          |
| `:tabn` / `gt`         | 下一个标签页        |
| `:tabp` / `gT`         | 上一个标签页        |
| `:tabc`                | 关闭当前标签页      |
| `{n}gt`                | 跳转到第 n 个标签页 |

### 12.5 Zed 特有窗格快捷键

| 快捷键             | 说明                   |
| ------------------ | ---------------------- |
| `g /`              | 项目全局搜索           |
| `g <space>`        | 打开搜索结果面板       |
| `<ctrl-w> <space>` | 在分屏中打开搜索结果   |
| `<ctrl-w> g d`     | 在分屏中跳转到定义     |
| `<ctrl-w> g D`     | 在分屏中跳转到类型定义 |

---

## 13. Insert 模式命令

| 快捷键          | 说明                |
| --------------- | ------------------- |
| `Ctrl+x Ctrl+o` | 打开补全菜单        |
| `Ctrl+x Ctrl+c` | 请求 GitHub Copilot |
| `Ctrl+x Ctrl+a` | 打开 AI 助手        |
| `Ctrl+x Ctrl+l` | 打开代码操作        |
| `Ctrl+x Ctrl+z` | 隐藏建议            |
| `Ctrl+n`        | 下一个补全项        |
| `Ctrl+p`        | 上一个补全项        |
| `Ctrl+e`        | 取消补全            |
| `Ctrl+h`        | 删除前一个字符      |
| `Ctrl+w`        | 删除前一个单词      |
| `Ctrl+u`        | 删除到行首          |

---

## 14. Ex 命令（`:` 命令）

Zed 的 Vim 模式将 `:` 映射到命令面板，提供 Vim 风格的别名：

### 14.1 文件操作

| 命令               | 说明           |
| ------------------ | -------------- |
| `:w` / `:write`    | 保存           |
| `:wa` / `:wall`    | 保存所有文件   |
| `:q` / `:quit`     | 关闭           |
| `:wq`              | 保存并关闭     |
| `:wqa` / `:wqall`  | 保存全部并关闭 |
| `:qa` / `:qall`    | 关闭所有       |
| `:cq`              | 强制关闭       |
| `:bd` / `:bdelete` | 关闭 Buffer    |
| `:e {file}`        | 打开文件       |
| `:ls`              | 列出 Buffers   |

### 14.2 分屏与标签

| 命令                   | 说明           |
| ---------------------- | -------------- |
| `:vs` / `:vsplit`      | 垂直分屏       |
| `:sp` / `:split`       | 水平分屏       |
| `:new`                 | 水平分屏新文件 |
| `:vnew`                | 垂直分屏新文件 |
| `:tabedit` / `:tabnew` | 新标签页       |
| `:tabn` / `:tabnext`   | 下一个标签页   |
| `:tabp` / `:tabprev`   | 上一个标签页   |
| `:tabc` / `:tabclose`  | 关闭标签页     |

### 14.3 跳转与导航

| 命令        | 说明             |
| ----------- | ---------------- |
| `:{number}` | 跳转到指定行     |
| `:$`        | 跳转到文件末尾   |
| `:/foo`     | 搜索 foo（向下） |
| `:?foo`     | 搜索 foo（向上） |

### 14.4 诊断

| 命令             | 说明           |
| ---------------- | -------------- |
| `:cn` / `:cnext` | 下一个诊断     |
| `:cp` / `:cprev` | 上一个诊断     |
| `:cc`            | 显示当前诊断   |
| `:clist` / `:cl` | 列出所有诊断   |
| `:ln` / `:lnext` | 下一个位置     |
| `:lp` / `:lprev` | 上一个位置     |
| `:ll`            | 当前定位列表项 |

### 14.5 替换

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `:s/foo/bar/`     | 替换当前行（Zed 默认全局） |
| `:%s/foo/bar/`    | 全文替换                   |
| `:5,10s/foo/bar/` | 范围替换                   |

### 14.6 编辑命令

| 命令             | 说明                         |
| ---------------- | ---------------------------- |
| `:j` / `:join`   | 连接行                       |
| `:d` / `:delete` | 删除行                       |
| `:s` / `:sort`   | 排序（`:sort i` 忽略大小写） |
| `:y` / `:yank`   | 复制行                       |

### 14.7 设置选项

| 命令                        | 说明       |
| --------------------------- | ---------- |
| `:set wrap` / `:set nowrap` | 自动换行   |
| `:set nu` / `:set nonu`     | 行号       |
| `:set rnu` / `:set nornu`   | 相对行号   |
| `:set ic` / `:set noic`     | 忽略大小写 |

### 14.8 Zed 特有命令

| 命令                   | 说明               |
| ---------------------- | ------------------ |
| `:E` / `:Explore`      | 打开项目面板       |
| `:C` / `:Collab`       | 协作面板           |
| `:Ch` / `:Chat`        | 聊天面板           |
| `:A` / `:AI`           | AI 助手            |
| `:No` / `:Notif`       | 通知面板           |
| `:fe` / `:feedback`    | 反馈               |
| `:te` / `:term`        | 终端               |
| `:Ext` / `:Extensions` | 扩展面板           |
| `:G` / `:Git`          | Git 面板           |
| `:diffs`               | 切换 Hunk Diff     |
| `:cpp`                 | 复制路径           |
| `:crp`                 | 复制相对路径       |
| `:reveal`              | 在文件管理器中显示 |
| `:zlog`                | 打开 Zed 日志      |
| `:clank`               | 取消语言服务器任务 |

### 14.9 自定义命令别名

```json
{
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q",
        "Wa": "wa"
    }
}
```

---

## 15. 内置插件功能

Zed 内置了常用 Vim 插件功能的等价实现：

### 15.1 Surround（vim-surround）

| 快捷键             | 说明                                     |
| ------------------ | ---------------------------------------- |
| `ys{motion}{char}` | 添加包围（如 `ysiw"` 用双引号包围单词）  |
| `cs{old}{new}`     | 修改包围（如 `cs"'` 将双引号改为单引号） |
| `ds{char}`         | 删除包围（如 `ds[` 删除方括号）          |
| `S{char}` (visual) | 用字符包围选中文本                       |

> Visual 模式下需要自定义绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
>     }
> }
> ```

### 15.2 Comment（vim-commentary）

| 快捷键        | 说明                            |
| ------------- | ------------------------------- |
| `gcc`         | 切换当前行注释                  |
| `gc` (visual) | 切换选中行注释                  |
| `gc{motion}`  | 对移动范围切换注释（如 `gc3j`） |

### 15.3 Exchange（vim-exchange）

| 快捷键        | 说明             |
| ------------- | ---------------- |
| `cx{motion}`  | 标记要交换的文本 |
| `cx` (第二次) | 执行交换         |

> Visual 模式下需手动绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-x": "vim::Exchange"
>     }
> }
> ```

### 15.4 ReplaceWithRegister

| 快捷键       | 说明                                 |
| ------------ | ------------------------------------ |
| `gR{motion}` | 用寄存器内容替换文本（不覆盖寄存器） |

### 15.5 netrw 风格项目面板

在项目面板中使用 `hjkl` 导航：

| 快捷键                | 说明           |
| --------------------- | -------------- |
| `h` / `j` / `k` / `l` | 导航           |
| `o`                   | 打开文件       |
| `t`                   | 在新标签页打开 |

### 15.6 Sneak 导航

需自定义绑定：

```json
{
    "context": "vim_mode == normal || vim_mode == visual",
    "bindings": {
        "s": "vim::PushSneak",
        "shift-s": "vim::PushSneakBackward"
    }
}
```

### 15.7 AnyQuotes / AnyBrackets 文本对象

```json
{
    "context": "vim_operator == a || vim_operator == i || vim_operator == cs",
    "bindings": {
        "q": "vim::AnyQuotes",
        "b": "vim::AnyBrackets",
        "Q": "vim::MiniQuotes",
        "B": "vim::MiniBrackets"
    }
}
```

---

## 16. 标记与寄存器

### 16.1 标记（Marks）

| 快捷键       | 说明                       |
| ------------ | -------------------------- |
| `m{a-z}`     | 设置缓冲区本地标记         |
| `'{a-z}`     | 跳转到标记所在行           |
| `` `{a-z} `` | 跳转到标记精确位置         |
| `''`         | 跳转到上次位置             |
| `'<` / `'>`  | 上次可视选择的开始/结束    |
| `'[` / `']`  | 上次修改/复制的开始/结束   |
| `'{` / `'}`  | 段落跳转标记               |
| `'.`         | 上次修改位置               |
| `'^`         | 上次 Insert 模式退出的位置 |

### 16.2 寄存器（Registers）

| 寄存器      | 说明                                  |
| ----------- | ------------------------------------- |
| `""`        | 无名寄存器（默认）                    |
| `"0`        | 最近复制的文本                        |
| `"1` - `"9` | 最近删除的文本（从新到旧）            |
| `"a` - `"z` | 命名寄存器                            |
| `"+`        | 系统剪贴板                            |
| `"*`        | 选择剪贴板（Linux/Windows 等同 `"+`） |
| `"_`        | 黑洞寄存器（删除不保存）              |
| `"%`        | 当前文件名                            |
| `".`        | 上次插入的文本                        |

使用方式：

- `"ayw` — 将单词复制到寄存器 a
- `"ap` — 粘贴寄存器 a 的内容
- `"+y` — 复制到系统剪贴板
- `"_dd` — 删除行但不保存到寄存器

### 16.3 剪贴板配置

```json
{
    "vim": {
        "use_system_clipboard": "always"
        // "always"  — 始终使用系统剪贴板
        // "never"   — 永远不使用
        // "on_yank" — 仅复制时使用
    }
}
```

---

## 17. 宏

Zed 使用自己的录制系统实现 Vim 宏功能：

| 快捷键       | 说明                    |
| ------------ | ----------------------- |
| `q{a-z}`     | 开始录制到寄存器 {a-z}  |
| `q` (录制中) | 停止录制                |
| `Q`          | 重复上次录制的宏        |
| `@{a-z}`     | 执行寄存器 {a-z} 中的宏 |
| `@@`         | 重复上次执行的宏        |
| `{n}@{a-z}`  | 执行宏 n 次             |

> **注意：** Zed 的宏系统与原生 Vim 不同，宏存储与复制寄存器是独立的命名空间。

---

## 18. 配置

### 18.1 Vim 专属配置

在 `settings.json` 的 `"vim"` 键下配置：

```json
{
    "vim": {
        // 系统剪贴板策略："always" | "never" | "on_yank"
        "use_system_clipboard": "always",

        // 多行搜索
        "use_multiline_find": true,

        // 智能大小写搜索（全小写时忽略大小写，含大写时精确匹配）
        "use_smartcase_find": true,

        // 切换相对行号
        "toggle_relative_line_numbers": true,

        // 自定义有向图（digraph），如 Ctrl+k f z → 🧟
        "custom_digraphs": { "fz": "🧟♀️" },

        // 启动时的默认模式
        "default_mode": "normal",

        // 复制后高亮持续时间（毫秒），0 禁用
        "highlight_on_yank_duration": 200,

        // 使用正则搜索
        "use_regex_search": true,

        // 替换默认全局（相当于始终 /g）
        "gdefault": false
    }
}
```

### 18.2 推荐的伴随设置

```json
{
    // 禁用光标闪烁（像原生 Vim）
    "cursor_blink": false,

    // 相对行号
    "relative_line_numbers": true,

    // 滚动条
    "scrollbar": { "show": "never" },

    // 不允许滚动超过最后一行
    "scroll_beyond_last_line": "off",

    // 垂直滚动边距
    "vertical_scroll_margin": 0,

    // 行号
    "gutter": { "line_numbers": false },

    // 命令别名
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q"
    }
}
```

---

## 19. 自定义快捷键

### 19.1 Vim 上下文

自定义快捷键需要理解 Zed 的上下文系统：

| 上下文                 | 含义                                          |
| ---------------------- | --------------------------------------------- |
| `VimControl`           | `normal` \|\| `visual` \|\| `operator` 的别名 |
| `vim_mode == normal`   | Normal 模式                                   |
| `vim_mode == visual`   | Visual 模式                                   |
| `vim_mode == insert`   | Insert 模式                                   |
| `vim_mode == replace`  | Replace 模式                                  |
| `vim_mode == waiting`  | 等待（如 `f`/`t` 后等待字符）                 |
| `vim_mode == operator` | 操作符挂起（如 `c`/`d` 后等待移动）           |
| `vim_operator == d`    | 当前操作符为 `d`                              |
| `vim_operator == c`    | 当前操作符为 `c`                              |
| `vim_operator == y`    | 当前操作符为 `y`                              |

### 19.2 示例

```json
[
    // Normal 模式下的自定义
    {
        "context": "vim_mode == normal && !menu",
        "bindings": {
            "shift-y": ["workspace::SendKeystrokes", "y $"],
            "space w": "workspace::Save"
        }
    },

    // Visual 模式下的自定义
    {
        "context": "vim_mode == visual",
        "bindings": {
            "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
        }
    },

    // Insert 模式退出
    {
        "context": "vim_mode == insert",
        "bindings": {
            "j k": "vim::NormalBefore",
            "j j": "vim::NormalBefore"
        }
    },

    // 窗格中
    {
        "context": "EmptyPane || SharedScreen",
        "bindings": {
            "space f": "file_finder::Toggle"
        }
    }
]
```

### 19.3 恢复系统剪贴板快捷键（Linux/Windows）

```json
{
    "context": "Editor && !menu",
    "bindings": {
        "ctrl-c": "editor::Copy",
        "ctrl-x": "editor::Cut",
        "ctrl-v": "editor::Paste",
        "ctrl-y": "editor::Undo",
        "ctrl-f": "buffer_search::Deploy",
        "ctrl-o": "workspace::Open",
        "ctrl-a": "editor::SelectAll"
    }
}
```

---

## 20. 正则表达式差异

Zed 使用不同的正则引擎（Rust `regex` crate），与 Vim 的语法有差异：

| 特性         | Vim       | Zed                                |
| ------------ | --------- | ---------------------------------- |
| 捕获组       | `\(` `\)` | `(` `)`                            |
| 字面括号     | `(` `)`   | `\(` `\)`                          |
| 完整匹配引用 | `\0`      | `$0`                               |
| 编号组引用   | `\1`      | `$1`                               |
| 全局标志     | 需 `/g`   | 默认全局（可设 `gdefault: false`） |
| 大小写不敏感 | `/i` 标志 | `(?i)` 或 `cmd-option-c`           |

> `:s` 命令面板会自动转换 Vim 风格的语法。

---

## 21. 补充：文档未提及的 Vim 常用命令

以下命令在 Zed 官方文档中未明确列出，但在日常 Vim 使用中非常重要。**部分在 Zed 中可直接使用，部分可能需要验证。**

### 21.1 Normal 模式进阶命令

| 快捷键             | 说明                              | Zed 支持      |
| ------------------ | --------------------------------- | ------------- |
| `g_`               | 跳转到行尾最后一个非空白字符      | ⚠️ 待验证     |
| `g^`               | 跳转到行首第一个非空白字符        | ⚠️ 待验证     |
| `gm`               | 跳转到行中间                      | ⚠️ 待验证     |
| `g0`               | 跳转到行首（即使是换行行）        | ⚠️ 待验证     |
| `g$`               | 跳转到行尾（即使是换行行）        | ⚠️ 待验证     |
| `g~` / `gu` / `gU` | 切换/小写/大写 + 移动             | ✅ 基本支持   |
| `g?`               | Rot13 编码                        | ❌ 通常不支持 |
| `ga`               | 显示光标下字符的 ASCII/Unicode 值 | ⚠️ 待验证     |
| `g Ctrl+g`         | 显示光标位置统计                  | ⚠️ 待验证     |
| `Ctrl+g`           | 显示文件信息和当前行号            | ⚠️ 待验证     |
| `del`              | 删除光标后字符（同 `x`）          | ⚠️ 待验证     |
| `D`                | 删除到行尾                        | ✅ 支持       |
| `Y`                | 复制整行（同 `yy`）               | ✅ 支持       |
| `C`                | 修改到行尾                        | ✅ 支持       |
| `s`                | 删除字符并进入 Insert             | ✅ 支持       |
| `S` / `cc`         | 删除整行并进入 Insert             | ✅ 支持       |
| `~`                | 切换字符大小写                    | ✅ 支持       |

### 21.2 窗口/标签页进阶

| 快捷键                   | 说明                   | Zed 支持           |
| ------------------------ | ---------------------- | ------------------ |
| `Ctrl+w T`               | 将当前窗格移到新标签页 | ⚠️ 待验证          |
| `Ctrl+w x`               | 交换窗格位置           | ⚠️ 待验证          |
| `Ctrl+w r`               | 旋转窗格               | ⚠️ 待验证          |
| `Ctrl+w H`               | 将窗格移到左侧（全高） | ⚠️ 待验证          |
| `Ctrl+w J`               | 将窗格移到底部（全宽） | ⚠️ 待验证          |
| `Ctrl+w K`               | 将窗格移到顶部（全宽） | ⚠️ 待验证          |
| `Ctrl+w L`               | 将窗格移到右侧（全高） | ⚠️ 待验证          |
| `:on` / `:only`          | 仅保留当前窗格         | ✅ 通过 `Ctrl+w o` |
| `:tabm {n}`              | 移动标签页到位置 n     | ⚠️ 待验证          |
| `:tabfirst` / `:tablast` | 第一个/最后一个标签页  | ⚠️ 待验证          |

### 21.3 折叠命令

Zed 使用 Tree-sitter 的代码折叠，以下命令**可能需要验证**：

| 快捷键       | 说明                     |
| ------------ | ------------------------ |
| `za`         | 切换当前折叠             |
| `zA`         | 递归切换折叠             |
| `zc`         | 关闭折叠                 |
| `zC`         | 递归关闭折叠             |
| `zo`         | 打开折叠                 |
| `zO`         | 递归打开折叠             |
| `zm`         | 增加折叠级别（关闭更多） |
| `zM`         | 关闭所有折叠             |
| `zr`         | 减少折叠级别（打开更多） |
| `zR`         | 打开所有折叠             |
| `zf{motion}` | 手动创建折叠             |
| `zd`         | 删除折叠                 |
| `zE`         | 删除所有折叠             |

### 21.4 插入模式进阶

| 快捷键              | 说明                                | Zed 支持           |
| ------------------- | ----------------------------------- | ------------------ |
| `Ctrl+v {code}`     | 插入任意字符（如 `Ctrl+v 065` → A） | ⚠️ 待验证          |
| `Ctrl+k {digraph}`  | 插入有向图（如 `Ctrl+k a:` → ä）    | ✅ 自定义 digraphs |
| `Ctrl+a`            | 插入上次 Insert 模式的文本          | ⚠️ 待验证          |
| `Ctrl+@`            | 插入上次 Insert 文本并退出          | ⚠️ 待验证          |
| `Ctrl+d`            | 减少缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+t`            | 增加缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+y`            | 插入上一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+e`            | 插入下一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+j` / `Ctrl+m` | 换行（同 Enter）                    | ✅ 支持            |
| `Ctrl+r {reg}`      | 插入寄存器内容                      | ⚠️ 待验证          |

### 21.5 有用但常被忽视的命令

| 快捷键      | 说明                      | Zed 支持        |
| ----------- | ------------------------- | --------------- |
| `g&`        | 在所有行上重复上次替换    | ⚠️ 待验证       |
| `&`         | 在当前行重复上次替换      | ⚠️ 待验证       |
| `@:`        | 重复上次 Ex 命令          | ⚠️ 待验证       |
| `q:`        | 打开命令历史窗口          | ❌ Zed 无此概念 |
| `q/` / `q?` | 打开搜索历史窗口          | ❌ Zed 无此概念 |
| `K`         | 查看光标下关键字的 man 页 | ❌ 被重映射     |
| `zi`        | 切换折叠启用/禁用         | ⚠️ 待验证       |
| `zv`        | 打开足够折叠以显示光标    | ⚠️ 待验证       |
| `zx` / `zX` | 更新折叠/重新应用折叠     | ⚠️ 待验证       |
| `:retab`    | 转换 tab 为空格（或反之） | ⚠️ 待验证       |

### 21.6 快速修复窗口操作

| 快捷键               | 说明                 | Zed 支持        |
| -------------------- | -------------------- | --------------- |
| `:copen`             | 打开快速修复窗口     | ❌ 诊断面板替代 |
| `:cclose`            | 关闭快速修复窗口     | ❌              |
| `:cfirst` / `:clast` | 第一个/最后一个错误  | ⚠️ 待验证       |
| `:cdo {cmd}`         | 对每个错误项执行命令 | ❌              |

### 21.7 Buffer 管理进阶

| 快捷键               | 说明                   | Zed 支持  |
| -------------------- | ---------------------- | --------- |
| `:bufdo {cmd}`       | 对所有 Buffer 执行命令 | ❌        |
| `:b {num}`           | 切换到指定编号 Buffer  | ⚠️ 待验证 |
| `:bnext` / `:bprev`  | 同 `:bn` / `:bp`       | ✅ 支持   |
| `:bfirst` / `:blast` | 第一个/最后一个 Buffer | ⚠️ 待验证 |
| `ctrl+^` / `ctrl+6`  | 切换交替文件           | ⚠️ 待验证 |

### 21.8 实用技巧组合

这些都是 Vim 中常用的高效编辑组合：

| 组合        | 效果                             |
| ----------- | -------------------------------- |
| `ci"`       | 修改双引号内容                   |
| `ca"`       | 修改双引号包含引号本身           |
| `yi(`       | 复制括号内容                     |
| `da]`       | 删除方括号及内容                 |
| `vit` + `c` | 修改 HTML 标签内容               |
| `vat` + `d` | 删除整个 HTML 标签               |
| `gUiw`      | 将单词转为大写                   |
| `guiw`      | 将单词转为小写                   |
| `g~iw`      | 切换单词大小写                   |
| `guG`       | 从当前位置到文件末转为小写       |
| `gUgg`      | 从当前位置到文件头转为大写       |
| `=%`        | 缩进整个匹配块（光标在括号上时） |
| `>i{`       | 缩进花括号内容                   |
| `<a{`       | 减少花括号块缩进                 |

---

## 参考资源

- [Zed 官方 Vim 文档](https://zed.dev/docs/vim)
- [Zed 官方 Key Bindings 文档](https://zed.dev/docs/key-bindings)
- [Zed Decoded: Vim (博客)](https://zed.dev/blog/zed-decoded-vim)
- [Zed Vim Roadmap 2025](https://zed.dev/blog/vim-2025)
- [Zed GitHub 仓库](https://github.com/zed-industries/zed)
- [Zed Vim 模式源码文档](https://git.iko.soy/zed-industries/zed/src/commit/174e12568669e70abb15934597a3f5df335eaf08/docs/src/vim.md)

> **注意：** 标记为 ⚠️ 的条目需要在实际使用中验证。Zed 的 Vim 模式在持续更新中，如有缺失功能，可在 [Zed GitHub Issues](https://github.com/zed-industries/zed/issues) 中查找或提交。
>
> 本文档基于 2025 年 7 月的 Zed Vim 模式状态整理。# Zed 中 Vim 模式完整使用文档

> 基于 [Zed 官方 Vim 文档](https://zed.dev/docs/vim) 整理，并补充文档中未提及的 Vim 常用命令。

---

## 目录

1. [概述](#1-概述)
2. [启用 Vim 模式](#2-启用-vim-模式)
3. [模式切换](#3-模式切换)
4. [光标移动](#4-光标移动)
5. [编辑操作](#5-编辑操作)
6. [可视模式操作](#6-可视模式操作)
7. [搜索与替换](#7-搜索与替换)
8. [LSP / 代码导航](#8-lsp--代码导航)
9. [Git 集成](#9-git-集成)
10. [Tree-sitter 导航与文本对象](#10-tree-sitter-导航与文本对象)
11. [多光标操作](#11-多光标操作)
12. [窗格与窗口管理](#12-窗格与窗口管理)
13. [Insert 模式命令](#13-insert-模式命令)
14. [Ex 命令](#14-ex-命令)
15. [内置插件功能](#15-内置插件功能)
16. [标记与寄存器](#16-标记与寄存器)
17. [宏](#17-宏)
18. [配置](#18-配置)
19. [自定义快捷键](#19-自定义快捷键)
20. [正则表达式差异](#20-正则表达式差异)
21. [补充：文档未提及的 Vim 常用命令](#21-补充文档未提及的-vim-常用命令)

---

## 1. 概述

Zed 内置了 Vim 模拟层（Vim mode），目标是：

- **"做你期望的事"**：在合理的地方精确复制 Vim 的行为
- **利用 Zed 的现代特性**：Tree-sitter 语法解析、LSP、多光标、CRDT 协作等
- **不是 100% Vim 兼容**，但追求 **100% Vim 熟悉感**

### 与原生 Vim 的核心差异

| 方面       | Vim          | Zed                     |
| ---------- | ------------ | ----------------------- |
| 光标模型   | 定位在字符上 | 定位在字符之间          |
| 视觉块模式 | 原生块选择   | 通过多光标模拟          |
| 宏         | `q` / `@`    | 使用 Zed 的录制系统     |
| 搜索替换   | Vim 正则     | Zed 正则引擎            |
| 词语定义   | 固定规则     | 语言感知（Tree-sitter） |

---

## 2. 启用 Vim 模式

### 方法一：欢迎屏幕

首次启动 Zed 时，勾选 "Vim Mode" 复选框。

### 方法二：命令面板

`Ctrl+Shift+P` → 输入 `toggle vim mode` → 回车。

### 方法三：配置文件

在 `settings.json` 中添加：

```json
{
    "vim_mode": true
}
```

### 按发布渠道启用

```json
{
    "vim_mode": false,
    "preview": {
        "vim_mode": true
    },
    "nightly": {
        "vim_mode": true
    }
}
```

---

## 3. 模式切换

| 快捷键     | 模式         | 说明                       |
| ---------- | ------------ | -------------------------- |
| `Esc`      | Normal       | 从任何模式返回 Normal 模式 |
| `Ctrl+c`   | Normal       | 替代 Esc                   |
| `i`        | Insert       | 在当前字符前插入           |
| `a`        | Insert       | 在当前字符后插入           |
| `I`        | Insert       | 在行首第一个非空字符前插入 |
| `A`        | Insert       | 在行尾插入                 |
| `o`        | Insert       | 在下方新建一行             |
| `O`        | Insert       | 在上方新建一行             |
| `v`        | Visual       | 字符选择模式               |
| `V`        | Visual (行)  | 整行选择模式               |
| `Ctrl+v`   | Visual Block | 块选择模式（多光标模拟）   |
| `R`        | Replace      | 替换模式（覆盖输入）       |
| `r`        | Normal       | 替换单个字符               |
| `s`        | Insert       | 删除当前字符并进入 Insert  |
| `S` / `cc` | Insert       | 删除整行并进入 Insert      |
| `C`        | Insert       | 删除到行尾并进入 Insert    |

### Insert 模式下返回 Normal

除 `Esc` 和 `Ctrl+c` 外，可自定义快捷键：

```json
{
    "context": "vim_mode == insert",
    "bindings": {
        "j k": "vim::NormalBefore",
        "j j": "vim::NormalBefore"
    }
}
```

---

## 4. 光标移动

### 4.1 基本移动

| 快捷键                | 说明                    |
| --------------------- | ----------------------- |
| `h` / `j` / `k` / `l` | 左 / 下 / 上 / 右       |
| `w`                   | 下一个单词开头          |
| `W`                   | 下一个 WORD（空白分隔） |
| `b`                   | 上一个单词开头          |
| `B`                   | 上一个 WORD             |
| `e`                   | 下一个单词结尾          |
| `E`                   | 下一个 WORD 结尾        |
| `ge`                  | 上一个单词结尾          |
| `gE`                  | 上一个 WORD 结尾        |

### 4.2 行内移动

| 快捷键    | 说明                              |
| --------- | --------------------------------- |
| `0`       | 行首（含空白）                    |
| `^`       | 行首第一个非空白字符              |
| `$`       | 行尾                              |
| `f{char}` | 向右查找字符（光标停在字符上）    |
| `F{char}` | 向左查找字符                      |
| `t{char}` | 向右查找字符（光标停在字符前）    |
| `T{char}` | 向左查找字符（光标停在字符后）    |
| `;`       | 重复上次 `f` / `t` 查找（同方向） |
| `,`       | 重复上次 `f` / `t` 查找（反方向） |

### 4.3 页面滚动

| 快捷键   | 说明                 |
| -------- | -------------------- |
| `Ctrl+d` | 向下翻半页           |
| `Ctrl+u` | 向上翻半页           |
| `Ctrl+f` | 向下翻一页           |
| `Ctrl+b` | 向上翻一页           |
| `zt`     | 将当前行置于屏幕顶部 |
| `zz`     | 将当前行置于屏幕中央 |
| `zb`     | 将当前行置于屏幕底部 |

### 4.4 文件级移动

| 快捷键           | 说明                                             |
| ---------------- | ------------------------------------------------ |
| `gg`             | 跳到文件第一行                                   |
| `G`              | 跳到文件最后一行                                 |
| `{n}gg` / `{n}G` | 跳到第 n 行                                      |
| `H`              | 屏幕顶部                                         |
| `M`              | 屏幕中间                                         |
| `L`              | 屏幕底部                                         |
| `{`              | 上一段（空行分隔）                               |
| `}`              | 下一段                                           |
| `%`              | 跳转到匹配的括号（语言感知，Rust 中可匹配 `\|`） |

### 4.5 跳转列表

| 快捷键   | 说明             |
| -------- | ---------------- |
| `Ctrl+o` | 跳到上一个位置   |
| `Ctrl+i` | 跳到下一个位置   |
| `Ctrl+t` | 从标签栈弹出     |
| `g;`     | 跳到上次修改位置 |
| `g,`     | 跳到下次修改位置 |

### 4.6 自定义子词移动（CamelCase / snake_case）

```json
{
    "context": "VimControl && !menu && vim_mode != operator",
    "bindings": {
        "w": "vim::NextSubwordStart",
        "b": "vim::PreviousSubwordStart",
        "e": "vim::NextSubwordEnd",
        "g e": "vim::PreviousSubwordEnd"
    }
}
```

---

## 5. 编辑操作

### 5.1 删除

| 快捷键     | 说明                       |
| ---------- | -------------------------- |
| `x`        | 删除当前字符               |
| `X`        | 删除前一个字符（退格）     |
| `dd`       | 删除当前行                 |
| `D`        | 删除到行尾                 |
| `dw`       | 删除到单词结尾             |
| `db`       | 删除到单词开头             |
| `daw`      | 删除整个单词（含周围空格） |
| `diw`      | 删除单词内部               |
| `d$` / `D` | 删除到行尾                 |
| `d0`       | 删除到行首                 |
| `dG`       | 删除到文件末尾             |
| `dgg`      | 删除到文件开头             |
| `dt{char}` | 删除直到指定字符           |
| `df{char}` | 删除到指定字符（含）       |

### 5.2 复制（Yank）

| 快捷键     | 说明           |
| ---------- | -------------- |
| `yy` / `Y` | 复制当前行     |
| `yw`       | 复制单词       |
| `yaw`      | 复制整个单词   |
| `y$`       | 复制到行尾     |
| `y0`       | 复制到行首     |
| `yG`       | 复制到文件末尾 |
| `ygg`      | 复制到文件开头 |

### 5.3 粘贴

| 快捷键 | 说明                       |
| ------ | -------------------------- |
| `p`    | 在光标后粘贴               |
| `P`    | 在光标前粘贴               |
| `gp`   | 粘贴并将光标移到粘贴内容后 |
| `gP`   | 粘贴并将光标移到粘贴内容前 |

### 5.4 修改（Change）

| 快捷键                | 说明                     |
| --------------------- | ------------------------ |
| `cw`                  | 修改到单词结尾           |
| `ciw`                 | 修改整个单词             |
| `caw`                 | 修改整个单词（含空格）   |
| `cc` / `S`            | 修改整行                 |
| `C`                   | 修改到行尾               |
| `ci(` / `ci[` / `ci{` | 修改括号内内容           |
| `ci"` / `ci'`         | 修改引号内内容           |
| `cit`                 | 修改 HTML/XML 标签内内容 |

### 5.5 其他编辑

| 快捷键     | 说明                     |
| ---------- | ------------------------ |
| `u`        | 撤销                     |
| `Ctrl+r`   | 重做                     |
| `.`        | 重复上次修改             |
| `>>`       | 增加缩进                 |
| `<<`       | 减少缩进                 |
| `>` + 移动 | 缩进指定范围             |
| `<` + 移动 | 减少缩进指定范围         |
| `~`        | 切换大小写               |
| `J`        | 将下一行连接到当前行     |
| `gJ`       | 连接行（不加空格）       |
| `r{char}`  | 替换当前字符             |
| `xp`       | 交换当前字符与下一个字符 |
| `ddp`      | 交换当前行与下一行       |

### 5.6 数字前缀（Count）

几乎所有命令都支持数字前缀：

| 示例                  | 效果               |
| --------------------- | ------------------ |
| `5j`                  | 向下移动 5 行      |
| `3dd`                 | 删除 3 行          |
| `4x`                  | 删除 4 个字符      |
| `2dw`                 | 删除 2 个单词      |
| `5i` + `text` + `Esc` | 插入 `text` 5 次   |
| `10.`                 | 重复上次修改 10 次 |

> **注意：** `5dj` 和 `d5j` 的区别 — `5dj` 表示"删除 5 次 = 5 行"，而 `d5j` 表示"删除操作应用于向下 5 行"。效果相同但语义不同。

---

## 6. 可视模式操作

| 快捷键                  | 说明                   |
| ----------------------- | ---------------------- |
| `v`                     | 进入字符可视模式       |
| `V`                     | 进入行可视模式         |
| `Ctrl+v`                | 进入块可视模式         |
| `gv`                    | 重新选择上次的可视区域 |
| `o` (visual 模式)       | 跳转到选择区域的另一端 |
| `O` (visual block 模式) | 跳转到对角线另一端     |
| `u` (visual 模式)       | 将选中文本转为小写     |
| `U` (visual 模式)       | 将选中文本转为大写     |
| `>`                     | 缩进选中文本           |
| `<`                     | 减少缩进选中文本       |
| `I` (block 模式)        | 在块前插入（多行同时） |
| `A` (block 模式)        | 在块后插入（多行同时） |
| `c` (visual 模式)       | 修改选中文本           |
| `d` / `x` (visual 模式) | 删除选中文本           |
| `y` (visual 模式)       | 复制选中文本           |

---

## 7. 搜索与替换

### 7.1 搜索

| 快捷键 | 说明                               |
| ------ | ---------------------------------- |
| `/`    | 向下搜索                           |
| `?`    | 向上搜索                           |
| `n`    | 下一个匹配项                       |
| `N`    | 上一个匹配项                       |
| `*`    | 搜索光标下的单词（向下）           |
| `#`    | 搜索光标下的单词（向上）           |
| `g*`   | 搜索光标下的单词（部分匹配，向下） |
| `g#`   | 搜索光标下的单词（部分匹配，向上） |
| `gn`   | 选中下一个匹配项                   |
| `gN`   | 选中上一个匹配项                   |
| `cgn`  | 修改下一个匹配项，然后 `.` 重复    |

> **搜索配置：**
>
> ```json
> {
>     "vim": {
>         "use_smartcase_find": true, // 智能大小写
>         "use_regex_search": true // 使用正则搜索
>     }
> }
> ```

### 7.2 替换

| 命令               | 说明                               |
| ------------------ | ---------------------------------- |
| `:s/old/new/`      | 替换当前行第一个匹配               |
| `:s/old/new/g`     | 替换当前行所有匹配                 |
| `:%s/old/new/`     | 替换全文（`/g` 在 Zed 中默认隐含） |
| `:%s/old/new/gc`   | 替换全文（逐个确认）               |
| `:5,10s/old/new/`  | 替换第 5-10 行                     |
| `:'<,'>s/old/new/` | 替换选中的范围                     |

> **Zed 正则差异** — 详见[第 20 节](#20-正则表达式差异)。

---

## 8. LSP / 代码导航

Zed 将 Vim 快捷键与 LSP 功能深度集成：

| 快捷键        | 说明                    |
| ------------- | ----------------------- |
| `g d`         | 跳转到定义              |
| `g D`         | 跳转到声明              |
| `g y`         | 跳转到类型定义          |
| `g I`         | 跳转到实现              |
| `c d`         | 重命名符号              |
| `g A`         | 查找所有引用            |
| `g s`         | 文件内查找符号          |
| `g S`         | 项目内查找符号          |
| `g ]` / `] d` | 下一个诊断（错误/警告） |
| `g [` / `[ d` | 上一个诊断              |
| `g h`         | 显示悬停信息            |
| `g .`         | 打开代码操作菜单        |
| `g r`         | 重命名（与 `c d` 类似） |

---

## 9. Git 集成

| 快捷键 | 说明                 |
| ------ | -------------------- |
| `] c`  | 下一个 Git 更改      |
| `[ c`  | 上一个 Git 更改      |
| `d o`  | 展开 diff hunk       |
| `d O`  | 切换暂存状态         |
| `d u`  | 暂存并移到下一个     |
| `d U`  | 取消暂存并移到下一个 |
| `d p`  | 还原更改             |

---

## 10. Tree-sitter 导航与文本对象

### 10.1 语法节点导航

| 快捷键        | 说明                         |
| ------------- | ---------------------------- |
| `] m` / `[ m` | 下一个/上一个方法            |
| `] M` / `[ M` | 下一个/上一个方法结尾        |
| `] ]` / `[ [` | 下一个/上一个段落（Section） |
| `] /` / `[ /` | 下一个/上一个注释            |
| `] *` / `[ *` | 下一个/上一个注释（同上）    |
| `[ x`         | 选择更大的语法节点           |
| `] x`         | 选择更小的语法节点           |

### 10.2 文本对象

| 快捷键                | 说明                |
| --------------------- | ------------------- |
| `a c` / `i c`         | 整个类 / 类内部     |
| `a f` / `i f`         | 整个函数 / 函数内部 |
| `g c`                 | 注释                |
| `i a` / `a a`         | 参数内部 / 整个参数 |
| `a t` / `i t`         | HTML 标签           |
| `a I` / `a i` / `i i` | 缩进级别            |
| `i w` / `a w`         | 单词                |
| `i W` / `a W`         | WORD                |
| `i s` / `a s`         | 句子                |
| `i p` / `a p`         | 段落                |
| `i (` / `a (`         | 圆括号内            |
| `i [` / `a [`         | 方括号内            |
| `i {` / `a {`         | 花括号内            |
| `i "` / `a "`         | 双引号内            |
| `i '` / `a '`         | 单引号内            |
| `i \`` / `a \``       | 反引号内            |
| `i t` / `a t`         | HTML/XML 标签内     |

### 10.3 缩进文本对象

| 快捷键      | 说明                      |
| ----------- | ------------------------- |
| `[-`        | 上一个缩进更少的行        |
| `]-`        | 下一个缩进更少的行        |
| `[+` / `]+` | 上一个/下一个缩进更多的行 |
| `[=` / `]=` | 上一个/下一个相同缩进的行 |

---

## 11. 多光标操作

Zed 在 Vim 模式下保留了多光标功能：

| 快捷键 | 说明                         |
| ------ | ---------------------------- |
| `g l`  | 添加光标到下一个相同单词     |
| `g L`  | 添加光标到上一个相同单词     |
| `g a`  | 选中当前单词的所有出现       |
| `g A`  | 在选中区域的每行末尾添加光标 |
| `g I`  | 在选中区域的每行开头添加光标 |
| `g >`  | 跳过并添加下一个             |
| `g <`  | 跳过并添加上一个             |
| `Esc`  | 退出多光标模式，回到 Normal  |

---

## 12. 窗格与窗口管理

### 12.1 分屏操作

| 快捷键            | 说明                       |
| ----------------- | -------------------------- |
| `:vs` / `:vsplit` | 垂直分屏                   |
| `:sp` / `:split`  | 水平分屏                   |
| `:new`            | 水平分屏打开新文件         |
| `:vnew`           | 垂直分屏打开新文件         |
| `Ctrl+w v`        | 垂直分屏                   |
| `Ctrl+w s`        | 水平分屏                   |
| `Ctrl+w q`        | 关闭当前窗格               |
| `Ctrl+w c`        | 关闭当前窗格               |
| `Ctrl+w o`        | 仅保留当前窗格（关闭其他） |

### 12.2 窗格间导航

| 快捷键                  | 说明             |
| ----------------------- | ---------------- |
| `Ctrl+w h` / `Ctrl+w j` | 左 / 下          |
| `Ctrl+w k` / `Ctrl+w l` | 上 / 右          |
| `Ctrl+w w`              | 循环切换窗格     |
| `Ctrl+w p`              | 切换到上一个窗格 |
| `Ctrl+w t`              | 切换到最顶部窗格 |
| `Ctrl+w b`              | 切换到最底部窗格 |

### 12.3 窗格调整

| 快捷键      | 说明         |
| ----------- | ------------ |
| `Ctrl+w =`  | 均分窗格大小 |
| `Ctrl+w >`  | 增加宽度     |
| `Ctrl+w <`  | 减少宽度     |
| `Ctrl+w +`  | 增加高度     |
| `Ctrl+w -`  | 减少高度     |
| `Ctrl+w _`  | 最大化高度   |
| `Ctrl+w \|` | 最大化宽度   |

### 12.4 标签页

| 快捷键                 | 说明                |
| ---------------------- | ------------------- |
| `:tabnew` / `:tabedit` | 新建标签页          |
| `:tabn` / `gt`         | 下一个标签页        |
| `:tabp` / `gT`         | 上一个标签页        |
| `:tabc`                | 关闭当前标签页      |
| `{n}gt`                | 跳转到第 n 个标签页 |

### 12.5 Zed 特有窗格快捷键

| 快捷键             | 说明                   |
| ------------------ | ---------------------- |
| `g /`              | 项目全局搜索           |
| `g <space>`        | 打开搜索结果面板       |
| `<ctrl-w> <space>` | 在分屏中打开搜索结果   |
| `<ctrl-w> g d`     | 在分屏中跳转到定义     |
| `<ctrl-w> g D`     | 在分屏中跳转到类型定义 |

---

## 13. Insert 模式命令

| 快捷键          | 说明                |
| --------------- | ------------------- |
| `Ctrl+x Ctrl+o` | 打开补全菜单        |
| `Ctrl+x Ctrl+c` | 请求 GitHub Copilot |
| `Ctrl+x Ctrl+a` | 打开 AI 助手        |
| `Ctrl+x Ctrl+l` | 打开代码操作        |
| `Ctrl+x Ctrl+z` | 隐藏建议            |
| `Ctrl+n`        | 下一个补全项        |
| `Ctrl+p`        | 上一个补全项        |
| `Ctrl+e`        | 取消补全            |
| `Ctrl+h`        | 删除前一个字符      |
| `Ctrl+w`        | 删除前一个单词      |
| `Ctrl+u`        | 删除到行首          |

---

## 14. Ex 命令（`:` 命令）

Zed 的 Vim 模式将 `:` 映射到命令面板，提供 Vim 风格的别名：

### 14.1 文件操作

| 命令               | 说明           |
| ------------------ | -------------- |
| `:w` / `:write`    | 保存           |
| `:wa` / `:wall`    | 保存所有文件   |
| `:q` / `:quit`     | 关闭           |
| `:wq`              | 保存并关闭     |
| `:wqa` / `:wqall`  | 保存全部并关闭 |
| `:qa` / `:qall`    | 关闭所有       |
| `:cq`              | 强制关闭       |
| `:bd` / `:bdelete` | 关闭 Buffer    |
| `:e {file}`        | 打开文件       |
| `:ls`              | 列出 Buffers   |

### 14.2 分屏与标签

| 命令                   | 说明           |
| ---------------------- | -------------- |
| `:vs` / `:vsplit`      | 垂直分屏       |
| `:sp` / `:split`       | 水平分屏       |
| `:new`                 | 水平分屏新文件 |
| `:vnew`                | 垂直分屏新文件 |
| `:tabedit` / `:tabnew` | 新标签页       |
| `:tabn` / `:tabnext`   | 下一个标签页   |
| `:tabp` / `:tabprev`   | 上一个标签页   |
| `:tabc` / `:tabclose`  | 关闭标签页     |

### 14.3 跳转与导航

| 命令        | 说明             |
| ----------- | ---------------- |
| `:{number}` | 跳转到指定行     |
| `:$`        | 跳转到文件末尾   |
| `:/foo`     | 搜索 foo（向下） |
| `:?foo`     | 搜索 foo（向上） |

### 14.4 诊断

| 命令             | 说明           |
| ---------------- | -------------- |
| `:cn` / `:cnext` | 下一个诊断     |
| `:cp` / `:cprev` | 上一个诊断     |
| `:cc`            | 显示当前诊断   |
| `:clist` / `:cl` | 列出所有诊断   |
| `:ln` / `:lnext` | 下一个位置     |
| `:lp` / `:lprev` | 上一个位置     |
| `:ll`            | 当前定位列表项 |

### 14.5 替换

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `:s/foo/bar/`     | 替换当前行（Zed 默认全局） |
| `:%s/foo/bar/`    | 全文替换                   |
| `:5,10s/foo/bar/` | 范围替换                   |

### 14.6 编辑命令

| 命令             | 说明                         |
| ---------------- | ---------------------------- |
| `:j` / `:join`   | 连接行                       |
| `:d` / `:delete` | 删除行                       |
| `:s` / `:sort`   | 排序（`:sort i` 忽略大小写） |
| `:y` / `:yank`   | 复制行                       |

### 14.7 设置选项

| 命令                        | 说明       |
| --------------------------- | ---------- |
| `:set wrap` / `:set nowrap` | 自动换行   |
| `:set nu` / `:set nonu`     | 行号       |
| `:set rnu` / `:set nornu`   | 相对行号   |
| `:set ic` / `:set noic`     | 忽略大小写 |

### 14.8 Zed 特有命令

| 命令                   | 说明               |
| ---------------------- | ------------------ |
| `:E` / `:Explore`      | 打开项目面板       |
| `:C` / `:Collab`       | 协作面板           |
| `:Ch` / `:Chat`        | 聊天面板           |
| `:A` / `:AI`           | AI 助手            |
| `:No` / `:Notif`       | 通知面板           |
| `:fe` / `:feedback`    | 反馈               |
| `:te` / `:term`        | 终端               |
| `:Ext` / `:Extensions` | 扩展面板           |
| `:G` / `:Git`          | Git 面板           |
| `:diffs`               | 切换 Hunk Diff     |
| `:cpp`                 | 复制路径           |
| `:crp`                 | 复制相对路径       |
| `:reveal`              | 在文件管理器中显示 |
| `:zlog`                | 打开 Zed 日志      |
| `:clank`               | 取消语言服务器任务 |

### 14.9 自定义命令别名

```json
{
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q",
        "Wa": "wa"
    }
}
```

---

## 15. 内置插件功能

Zed 内置了常用 Vim 插件功能的等价实现：

### 15.1 Surround（vim-surround）

| 快捷键             | 说明                                     |
| ------------------ | ---------------------------------------- |
| `ys{motion}{char}` | 添加包围（如 `ysiw"` 用双引号包围单词）  |
| `cs{old}{new}`     | 修改包围（如 `cs"'` 将双引号改为单引号） |
| `ds{char}`         | 删除包围（如 `ds[` 删除方括号）          |
| `S{char}` (visual) | 用字符包围选中文本                       |

> Visual 模式下需要自定义绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
>     }
> }
> ```

### 15.2 Comment（vim-commentary）

| 快捷键        | 说明                            |
| ------------- | ------------------------------- |
| `gcc`         | 切换当前行注释                  |
| `gc` (visual) | 切换选中行注释                  |
| `gc{motion}`  | 对移动范围切换注释（如 `gc3j`） |

### 15.3 Exchange（vim-exchange）

| 快捷键        | 说明             |
| ------------- | ---------------- |
| `cx{motion}`  | 标记要交换的文本 |
| `cx` (第二次) | 执行交换         |

> Visual 模式下需手动绑定：
>
> ```json
> {
>     "context": "vim_mode == visual",
>     "bindings": {
>         "shift-x": "vim::Exchange"
>     }
> }
> ```

### 15.4 ReplaceWithRegister

| 快捷键       | 说明                                 |
| ------------ | ------------------------------------ |
| `gR{motion}` | 用寄存器内容替换文本（不覆盖寄存器） |

### 15.5 netrw 风格项目面板

在项目面板中使用 `hjkl` 导航：

| 快捷键                | 说明           |
| --------------------- | -------------- |
| `h` / `j` / `k` / `l` | 导航           |
| `o`                   | 打开文件       |
| `t`                   | 在新标签页打开 |

### 15.6 Sneak 导航

需自定义绑定：

```json
{
    "context": "vim_mode == normal || vim_mode == visual",
    "bindings": {
        "s": "vim::PushSneak",
        "shift-s": "vim::PushSneakBackward"
    }
}
```

### 15.7 AnyQuotes / AnyBrackets 文本对象

```json
{
    "context": "vim_operator == a || vim_operator == i || vim_operator == cs",
    "bindings": {
        "q": "vim::AnyQuotes",
        "b": "vim::AnyBrackets",
        "Q": "vim::MiniQuotes",
        "B": "vim::MiniBrackets"
    }
}
```

---

## 16. 标记与寄存器

### 16.1 标记（Marks）

| 快捷键       | 说明                       |
| ------------ | -------------------------- |
| `m{a-z}`     | 设置缓冲区本地标记         |
| `'{a-z}`     | 跳转到标记所在行           |
| `` `{a-z} `` | 跳转到标记精确位置         |
| `''`         | 跳转到上次位置             |
| `'<` / `'>`  | 上次可视选择的开始/结束    |
| `'[` / `']`  | 上次修改/复制的开始/结束   |
| `'{` / `'}`  | 段落跳转标记               |
| `'.`         | 上次修改位置               |
| `'^`         | 上次 Insert 模式退出的位置 |

### 16.2 寄存器（Registers）

| 寄存器      | 说明                                  |
| ----------- | ------------------------------------- |
| `""`        | 无名寄存器（默认）                    |
| `"0`        | 最近复制的文本                        |
| `"1` - `"9` | 最近删除的文本（从新到旧）            |
| `"a` - `"z` | 命名寄存器                            |
| `"+`        | 系统剪贴板                            |
| `"*`        | 选择剪贴板（Linux/Windows 等同 `"+`） |
| `"_`        | 黑洞寄存器（删除不保存）              |
| `"%`        | 当前文件名                            |
| `".`        | 上次插入的文本                        |

使用方式：

- `"ayw` — 将单词复制到寄存器 a
- `"ap` — 粘贴寄存器 a 的内容
- `"+y` — 复制到系统剪贴板
- `"_dd` — 删除行但不保存到寄存器

### 16.3 剪贴板配置

```json
{
    "vim": {
        "use_system_clipboard": "always"
        // "always"  — 始终使用系统剪贴板
        // "never"   — 永远不使用
        // "on_yank" — 仅复制时使用
    }
}
```

---

## 17. 宏

Zed 使用自己的录制系统实现 Vim 宏功能：

| 快捷键       | 说明                    |
| ------------ | ----------------------- |
| `q{a-z}`     | 开始录制到寄存器 {a-z}  |
| `q` (录制中) | 停止录制                |
| `Q`          | 重复上次录制的宏        |
| `@{a-z}`     | 执行寄存器 {a-z} 中的宏 |
| `@@`         | 重复上次执行的宏        |
| `{n}@{a-z}`  | 执行宏 n 次             |

> **注意：** Zed 的宏系统与原生 Vim 不同，宏存储与复制寄存器是独立的命名空间。

---

## 18. 配置

### 18.1 Vim 专属配置

在 `settings.json` 的 `"vim"` 键下配置：

```json
{
    "vim": {
        // 系统剪贴板策略："always" | "never" | "on_yank"
        "use_system_clipboard": "always",

        // 多行搜索
        "use_multiline_find": true,

        // 智能大小写搜索（全小写时忽略大小写，含大写时精确匹配）
        "use_smartcase_find": true,

        // 切换相对行号
        "toggle_relative_line_numbers": true,

        // 自定义有向图（digraph），如 Ctrl+k f z → 🧟
        "custom_digraphs": { "fz": "🧟♀️" },

        // 启动时的默认模式
        "default_mode": "normal",

        // 复制后高亮持续时间（毫秒），0 禁用
        "highlight_on_yank_duration": 200,

        // 使用正则搜索
        "use_regex_search": true,

        // 替换默认全局（相当于始终 /g）
        "gdefault": false
    }
}
```

### 18.2 推荐的伴随设置

```json
{
    // 禁用光标闪烁（像原生 Vim）
    "cursor_blink": false,

    // 相对行号
    "relative_line_numbers": true,

    // 滚动条
    "scrollbar": { "show": "never" },

    // 不允许滚动超过最后一行
    "scroll_beyond_last_line": "off",

    // 垂直滚动边距
    "vertical_scroll_margin": 0,

    // 行号
    "gutter": { "line_numbers": false },

    // 命令别名
    "command_aliases": {
        "W": "w",
        "Wq": "wq",
        "Q": "q"
    }
}
```

---

## 19. 自定义快捷键

### 19.1 Vim 上下文

自定义快捷键需要理解 Zed 的上下文系统：

| 上下文                 | 含义                                          |
| ---------------------- | --------------------------------------------- |
| `VimControl`           | `normal` \|\| `visual` \|\| `operator` 的别名 |
| `vim_mode == normal`   | Normal 模式                                   |
| `vim_mode == visual`   | Visual 模式                                   |
| `vim_mode == insert`   | Insert 模式                                   |
| `vim_mode == replace`  | Replace 模式                                  |
| `vim_mode == waiting`  | 等待（如 `f`/`t` 后等待字符）                 |
| `vim_mode == operator` | 操作符挂起（如 `c`/`d` 后等待移动）           |
| `vim_operator == d`    | 当前操作符为 `d`                              |
| `vim_operator == c`    | 当前操作符为 `c`                              |
| `vim_operator == y`    | 当前操作符为 `y`                              |

### 19.2 示例

```json
[
    // Normal 模式下的自定义
    {
        "context": "vim_mode == normal && !menu",
        "bindings": {
            "shift-y": ["workspace::SendKeystrokes", "y $"],
            "space w": "workspace::Save"
        }
    },

    // Visual 模式下的自定义
    {
        "context": "vim_mode == visual",
        "bindings": {
            "shift-s": ["vim::PushOperator", { "AddSurrounds": {} }]
        }
    },

    // Insert 模式退出
    {
        "context": "vim_mode == insert",
        "bindings": {
            "j k": "vim::NormalBefore",
            "j j": "vim::NormalBefore"
        }
    },

    // 窗格中
    {
        "context": "EmptyPane || SharedScreen",
        "bindings": {
            "space f": "file_finder::Toggle"
        }
    }
]
```

### 19.3 恢复系统剪贴板快捷键（Linux/Windows）

```json
{
    "context": "Editor && !menu",
    "bindings": {
        "ctrl-c": "editor::Copy",
        "ctrl-x": "editor::Cut",
        "ctrl-v": "editor::Paste",
        "ctrl-y": "editor::Undo",
        "ctrl-f": "buffer_search::Deploy",
        "ctrl-o": "workspace::Open",
        "ctrl-a": "editor::SelectAll"
    }
}
```

---

## 20. 正则表达式差异

Zed 使用不同的正则引擎（Rust `regex` crate），与 Vim 的语法有差异：

| 特性         | Vim       | Zed                                |
| ------------ | --------- | ---------------------------------- |
| 捕获组       | `\(` `\)` | `(` `)`                            |
| 字面括号     | `(` `)`   | `\(` `\)`                          |
| 完整匹配引用 | `\0`      | `$0`                               |
| 编号组引用   | `\1`      | `$1`                               |
| 全局标志     | 需 `/g`   | 默认全局（可设 `gdefault: false`） |
| 大小写不敏感 | `/i` 标志 | `(?i)` 或 `cmd-option-c`           |

> `:s` 命令面板会自动转换 Vim 风格的语法。

---

## 21. 补充：文档未提及的 Vim 常用命令

以下命令在 Zed 官方文档中未明确列出，但在日常 Vim 使用中非常重要。**部分在 Zed 中可直接使用，部分可能需要验证。**

### 21.1 Normal 模式进阶命令

| 快捷键             | 说明                              | Zed 支持      |
| ------------------ | --------------------------------- | ------------- |
| `g_`               | 跳转到行尾最后一个非空白字符      | ⚠️ 待验证     |
| `g^`               | 跳转到行首第一个非空白字符        | ⚠️ 待验证     |
| `gm`               | 跳转到行中间                      | ⚠️ 待验证     |
| `g0`               | 跳转到行首（即使是换行行）        | ⚠️ 待验证     |
| `g$`               | 跳转到行尾（即使是换行行）        | ⚠️ 待验证     |
| `g~` / `gu` / `gU` | 切换/小写/大写 + 移动             | ✅ 基本支持   |
| `g?`               | Rot13 编码                        | ❌ 通常不支持 |
| `ga`               | 显示光标下字符的 ASCII/Unicode 值 | ⚠️ 待验证     |
| `g Ctrl+g`         | 显示光标位置统计                  | ⚠️ 待验证     |
| `Ctrl+g`           | 显示文件信息和当前行号            | ⚠️ 待验证     |
| `del`              | 删除光标后字符（同 `x`）          | ⚠️ 待验证     |
| `D`                | 删除到行尾                        | ✅ 支持       |
| `Y`                | 复制整行（同 `yy`）               | ✅ 支持       |
| `C`                | 修改到行尾                        | ✅ 支持       |
| `s`                | 删除字符并进入 Insert             | ✅ 支持       |
| `S` / `cc`         | 删除整行并进入 Insert             | ✅ 支持       |
| `~`                | 切换字符大小写                    | ✅ 支持       |

### 21.2 窗口/标签页进阶

| 快捷键                   | 说明                   | Zed 支持           |
| ------------------------ | ---------------------- | ------------------ |
| `Ctrl+w T`               | 将当前窗格移到新标签页 | ⚠️ 待验证          |
| `Ctrl+w x`               | 交换窗格位置           | ⚠️ 待验证          |
| `Ctrl+w r`               | 旋转窗格               | ⚠️ 待验证          |
| `Ctrl+w H`               | 将窗格移到左侧（全高） | ⚠️ 待验证          |
| `Ctrl+w J`               | 将窗格移到底部（全宽） | ⚠️ 待验证          |
| `Ctrl+w K`               | 将窗格移到顶部（全宽） | ⚠️ 待验证          |
| `Ctrl+w L`               | 将窗格移到右侧（全高） | ⚠️ 待验证          |
| `:on` / `:only`          | 仅保留当前窗格         | ✅ 通过 `Ctrl+w o` |
| `:tabm {n}`              | 移动标签页到位置 n     | ⚠️ 待验证          |
| `:tabfirst` / `:tablast` | 第一个/最后一个标签页  | ⚠️ 待验证          |

### 21.3 折叠命令

Zed 使用 Tree-sitter 的代码折叠，以下命令**可能需要验证**：

| 快捷键       | 说明                     |
| ------------ | ------------------------ |
| `za`         | 切换当前折叠             |
| `zA`         | 递归切换折叠             |
| `zc`         | 关闭折叠                 |
| `zC`         | 递归关闭折叠             |
| `zo`         | 打开折叠                 |
| `zO`         | 递归打开折叠             |
| `zm`         | 增加折叠级别（关闭更多） |
| `zM`         | 关闭所有折叠             |
| `zr`         | 减少折叠级别（打开更多） |
| `zR`         | 打开所有折叠             |
| `zf{motion}` | 手动创建折叠             |
| `zd`         | 删除折叠                 |
| `zE`         | 删除所有折叠             |

### 21.4 插入模式进阶

| 快捷键              | 说明                                | Zed 支持           |
| ------------------- | ----------------------------------- | ------------------ |
| `Ctrl+v {code}`     | 插入任意字符（如 `Ctrl+v 065` → A） | ⚠️ 待验证          |
| `Ctrl+k {digraph}`  | 插入有向图（如 `Ctrl+k a:` → ä）    | ✅ 自定义 digraphs |
| `Ctrl+a`            | 插入上次 Insert 模式的文本          | ⚠️ 待验证          |
| `Ctrl+@`            | 插入上次 Insert 文本并退出          | ⚠️ 待验证          |
| `Ctrl+d`            | 减少缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+t`            | 增加缩进（Insert 模式）             | ⚠️ 待验证          |
| `Ctrl+y`            | 插入上一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+e`            | 插入下一行对应列字符                | ⚠️ 待验证          |
| `Ctrl+j` / `Ctrl+m` | 换行（同 Enter）                    | ✅ 支持            |
| `Ctrl+r {reg}`      | 插入寄存器内容                      | ⚠️ 待验证          |

### 21.5 有用但常被忽视的命令

| 快捷键      | 说明                      | Zed 支持        |
| ----------- | ------------------------- | --------------- |
| `g&`        | 在所有行上重复上次替换    | ⚠️ 待验证       |
| `&`         | 在当前行重复上次替换      | ⚠️ 待验证       |
| `@:`        | 重复上次 Ex 命令          | ⚠️ 待验证       |
| `q:`        | 打开命令历史窗口          | ❌ Zed 无此概念 |
| `q/` / `q?` | 打开搜索历史窗口          | ❌ Zed 无此概念 |
| `K`         | 查看光标下关键字的 man 页 | ❌ 被重映射     |
| `zi`        | 切换折叠启用/禁用         | ⚠️ 待验证       |
| `zv`        | 打开足够折叠以显示光标    | ⚠️ 待验证       |
| `zx` / `zX` | 更新折叠/重新应用折叠     | ⚠️ 待验证       |
| `:retab`    | 转换 tab 为空格（或反之） | ⚠️ 待验证       |

### 21.6 快速修复窗口操作

| 快捷键               | 说明                 | Zed 支持        |
| -------------------- | -------------------- | --------------- |
| `:copen`             | 打开快速修复窗口     | ❌ 诊断面板替代 |
| `:cclose`            | 关闭快速修复窗口     | ❌              |
| `:cfirst` / `:clast` | 第一个/最后一个错误  | ⚠️ 待验证       |
| `:cdo {cmd}`         | 对每个错误项执行命令 | ❌              |

### 21.7 Buffer 管理进阶

| 快捷键               | 说明                   | Zed 支持  |
| -------------------- | ---------------------- | --------- |
| `:bufdo {cmd}`       | 对所有 Buffer 执行命令 | ❌        |
| `:b {num}`           | 切换到指定编号 Buffer  | ⚠️ 待验证 |
| `:bnext` / `:bprev`  | 同 `:bn` / `:bp`       | ✅ 支持   |
| `:bfirst` / `:blast` | 第一个/最后一个 Buffer | ⚠️ 待验证 |
| `ctrl+^` / `ctrl+6`  | 切换交替文件           | ⚠️ 待验证 |

### 21.8 实用技巧组合

这些都是 Vim 中常用的高效编辑组合：

| 组合        | 效果                             |
| ----------- | -------------------------------- |
| `ci"`       | 修改双引号内容                   |
| `ca"`       | 修改双引号包含引号本身           |
| `yi(`       | 复制括号内容                     |
| `da]`       | 删除方括号及内容                 |
| `vit` + `c` | 修改 HTML 标签内容               |
| `vat` + `d` | 删除整个 HTML 标签               |
| `gUiw`      | 将单词转为大写                   |
| `guiw`      | 将单词转为小写                   |
| `g~iw`      | 切换单词大小写                   |
| `guG`       | 从当前位置到文件末转为小写       |
| `gUgg`      | 从当前位置到文件头转为大写       |
| `=%`        | 缩进整个匹配块（光标在括号上时） |
| `>i{`       | 缩进花括号内容                   |
| `<a{`       | 减少花括号块缩进                 |

---

## 参考资源

- [Zed 官方 Vim 文档](https://zed.dev/docs/vim)
- [Zed 官方 Key Bindings 文档](https://zed.dev/docs/key-bindings)
- [Zed Decoded: Vim (博客)](https://zed.dev/blog/zed-decoded-vim)
- [Zed Vim Roadmap 2025](https://zed.dev/blog/vim-2025)
- [Zed GitHub 仓库](https://github.com/zed-industries/zed)
- [Zed Vim 模式源码文档](https://git.iko.soy/zed-industries/zed/src/commit/174e12568669e70abb15934597a3f5df335eaf08/docs/src/vim.md)

> **注意：** 标记为 ⚠️ 的条目需要在实际使用中验证。Zed 的 Vim 模式在持续更新中，如有缺失功能，可在 [Zed GitHub Issues](https://github.com/zed-industries/zed/issues) 中查找或提交。
>
> 本文档基于 2025 年 7 月的 Zed Vim 模式状态整理。
